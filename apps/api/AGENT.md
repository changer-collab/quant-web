# apps/api/AGENT.md

## 必须遵守

- 所有回复使用中文。
- 当前目录尚未实现，新增代码前必须先确认 API 范围。
- 保持 API 层薄，不把回测、训练、数据清洗逻辑塞进 API。
- 不引入权限系统、微服务或复杂网关，除非用户明确要求。
- 更新本目录能力或进度时，同步更新本目录 `README.md` 和 `AGENT.md`，并按需同步根级文档。

## 当前阶段

```text
未实现
```

## 边界

API 只做 HTTP 入口和业务编排，不做：

```text
行情存储
事件驱动回测
模型训练
真实下单
低延迟执行
```
