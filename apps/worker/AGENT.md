# apps/worker/AGENT.md

## 必须遵守

- 所有回复使用中文。
- 当前目录尚未实现，新增代码前必须先确认任务闭环。
- Worker 负责异步任务编排，不负责核心算法实现。
- 不把低延迟实盘下单放进普通 Worker。
- 更新本目录能力或进度时，同步更新本目录 `README.md` 和 `AGENT.md`，并按需同步根级文档。

## 当前阶段

```text
未实现
```

## 边界

Worker 可以编排：

```text
回测任务
训练任务
实验任务
报告产物生成
```

Worker 不直接负责：

```text
策略逻辑
撮合模拟
模型算法
行情清洗
实盘下单
```
