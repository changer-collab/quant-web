node_modules/
dist/
*.tsbuildinfo

.env
.env.*
!.env.example

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# pnpm
.pnpm-store/

# local data (SQLite, CSV, etc.)
data/*.db
data/*.csv

# SQLite WAL/journal sidecar files (better-sqlite3 WAL mode) anywhere in tree
*.db-journal
*.db-wal
*.db-shm

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# turbo
.turbo

# Superpowers local execution scratch
.superpowers/

# Ralph autonomous agent loop (runtime artifacts)
scripts/ralph/prd.json
scripts/ralph/progress.txt
scripts/ralph/.last-branch
scripts/ralph/archive/

# claude-mem project memory data (shared across team via git)
.claude/settings.json

# Ralph autonomous agent loop (runtime artifacts) - extended
scripts/ralph/.current-prompt.md
scripts/ralph/.last-error.json
scripts/ralph/.last-raw-output.txt
scripts/ralph/.last-output.txt
scripts/ralph/.prd.state.json

# CLAUDE.md (Claude Code local, not project-shared)
CLAUDE.md

# untrack local SQLite db before making repo public
*.db
