# 策略开发链条完整性审计（更新版）

> 审计日期：2026-06-28 | 方法：逐文件代码审计 + git diff 对照上期审计 | 上期审计：2026-06-27

## 总体结论

**状态变化：5 个致命缺陷中 4 个已修复，1 个仍缺失。** 自 6/27 审计以来，通过 7 个 story 的迭代，最关键的"断线"问题（市场规则未连接、因子评估是 stub、AI 训完就丢、存活偏差）已全部修复。涨跌停/停牌处理也已实现。但**过拟合防护仍然完全空白**，模拟交易、组合优化、因子分析扩展等中优先级缺口也未推进。

**当前状态：研究平台 (research platform)，正在向可信回测平台演进。** 上半段（数据→因子→策略→回测→报告）的关键缺陷大幅减少；后半段（模拟→实盘→风控→运维）按设计不做（AGENTS.md 明确声明）。

---

## 🔴 致命缺陷修复状态（vs 6/27 审计）

| #   | 缺陷 (6/27)                        | 6/28 状态     | 修复证据                                                                                   |
| --- | ---------------------------------- | ------------- | ------------------------------------------------------------------------------------------ |
| 1   | 回测 CLI 未接入市场规则            | ✅ **已修复** | `backtest.py:9` 导入 `ASHARE_RULES`，第 81/175 行传入 `BacktestRunner`/`MultiSymbolRunner` |
| 2   | `factor_eval._make_factor` 是 stub | ✅ **已修复** | `factor_eval.py:60-78` 返回 `FormulaFactor(definition)`，含完整 AST 公式解析器             |
| 3   | AI 训练是断头路                    | ✅ **已修复** | `predictor.py` 封装训练→保存→预测；`ai_predictor.py` 策略消费预测信号                      |
| 4   | 过拟合防护完全空白                 | 🔴 **仍缺失** | 无变化 — 见第九节                                                                          |
| 5   | 存活偏差未处理                     | ✅ **已修复** | `backtest.py:131-140` 启动时过滤；`client.py:101-120` 按 `list_date`/`delist_date` 过滤    |

---

## 一、数据采集与清洗 🟢 完整度 85%（↑5%）

| 环节               | 状态                 | 证据                                                                                                                            |
| ------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 多源适配器         | **已实现**           | 8 个适配器：AKShare、Baostock、Tushare、Efinance、Yfinance、MooTDX、Tencent、CSV                                                |
| 增量采集           | **已实现**           | Watermark 表记录采集进度                                                                                                        |
| 调度器             | **已实现**           | CollectorScheduler 支持定时/按需采集                                                                                            |
| 清洗               | **部分**             | Cleaner 模块存在，具体逻辑待验证                                                                                                |
| 复权因子           | **已实现（采集侧）** | Baostock 采集 `adjustment_factor`                                                                                               |
| 存活偏差防护       | ✅ **已实现**        | `backtest.py:131-140` 回测启动时按 `list_date`/`delist_date` 过滤标的；`client.py:101-120` 实现 `get_active_symbols()` SQL 查询 |
| Tushare/其他适配器 | **部分**             | 有结构但需验证可达性                                                                                                            |

**⚠️ 遗留 gap：盘中存活偏差。** `get_active_symbols()` 只在回测开始时过滤一次——对于回测窗口内退市的标的，引擎不会中途停止交易。需在 `runner.py` 中增加逐 bar 的 `status` 检查。

---

## 二、数据存储 🟢 完整度 85%（无变化）

同 6/27 审计。数据覆盖率（L2/宏观/事件表是否真正有数据）仍未知。

---

## 三、特征提取 🟡 完整度 30%（无变化）

`packages/ai-engine/quantforge_ai/features.py:9-44`

| 特征                                  | 状态                                                                 |
| ------------------------------------- | -------------------------------------------------------------------- |
| 收益率特征 (return_1/5/10/20)         | **已实现**                                                           |
| 波动率特征 (volatility_5/10/20)       | **已实现**                                                           |
| 成交量特征 (volume_ratio)             | **已实现**                                                           |
| 技术指标特征 (RSI/MACD/布林带等)      | **缺失** — indicators.py 在 strategies 包里，FeatureExtractor 没调用 |
| 基本面特征 (PE/PB/ROE/市值等)         | **缺失**                                                             |
| 另类数据特征 (新闻情绪/股东人数/宏观) | **缺失**                                                             |
| 特征存储 (feature store)              | **缺失**                                                             |
| 前视偏差保护                          | **缺失**                                                             |
| 缺失值/异常值处理                     | **缺失**                                                             |

无变化。仍然是 3 类基础特征。

---

## 四、因子挖掘 🟡 完整度 40%（↑40%）

**这是变化最大的环节之一。**

| 能力                       | 6/27 状态                 | 6/28 状态                                                                                                              |
| -------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 手工定义因子               | 🔴 stub（始终返回 close） | ✅ **FormulaFactor** — AST 公式解析器 `formula.py:40-170`                                                              |
| 公式解析器/AST 求值        | 缺失                      | ✅ **已实现** — 支持 `pct_change(n)`, `rolling_mean(series, n)`, `rolling_std`, `shift`, `log`, `rank` 及二元/一元运算 |
| 缺失公式处理               | 静默 fallback 到 close    | ✅ **已实现** — `factor_eval.py:65-66` 抛出 `ValueError("factor.formula is required")`                                 |
| 自动化因子发现（遗传规划） | 缺失                      | 缺失                                                                                                                   |
| 自动化因子发现（符号回归） | 缺失                      | 缺失                                                                                                                   |
| 自动化因子发现（LLM 驱动） | 缺失                      | 缺失                                                                                                                   |
| 因子注册/版本管理          | 部分                      | 部分 — factor_definitions 表有结构但无注册表实现                                                                       |
| 因子组合/复合因子          | 缺失                      | 缺失                                                                                                                   |

**FormulaFactor 支持的函数：** `pct_change(5)` (默认 close)、`pct_change(volume, 5)` (指定序列)、`rolling_mean(volume, 10)`、`rolling_std(close, 20)`、`shift(1)`、`log(close)`、`rank(close, 20)`。运算符：`+`, `-`, `*`, `/`, `>`, `<`, `>=`, `<=`, `==`, `!=`。列名校验白名单：`open`, `high`, `low`, `close`, `volume`, `turnover`。

---

## 五、因子分析/评价 🟡 完整度 35%（无变化）

`packages/factor-lab/quantforge_factor/evaluator.py:12-87`

| 指标                              | 状态                                                                                        |
| --------------------------------- | ------------------------------------------------------------------------------------------- |
| IC (Pearson)                      | **已实现**                                                                                  |
| Rank IC (Spearman)                | **已实现**                                                                                  |
| 分组收益 (quantile group returns) | **已实现**                                                                                  |
| 多空收益 (long-short)             | **已实现**                                                                                  |
| IC 胜率                           | **已实现**（但计算方式有缺陷：用 `factor_values * forward_returns > 0` 而非 IC 方向一致性） |
| **IC 衰减曲线**                   | **缺失**                                                                                    |
| **IC_IR**                         | **缺失**                                                                                    |
| **因子中性化/正交化**             | **缺失** — UI 有组件但无后端                                                                |
| **因子相关性矩阵**                | **缺失** — UI 有组件但无后端                                                                |
| **因子换手率**                    | **缺失** — `FactorMetrics` 有 `turnover` 字段但 evaluator 不填充                            |
| **分层回测（完整资金曲线）**      | **缺失**                                                                                    |
| **因子稳健性检验**                | **缺失**                                                                                    |

无变化。`evaluator.py` 和 `types.py` 均未被修改。

---

## 六、策略构建 🟢 完整度 75%（↑10%）

| 环节                             | 状态          | 证据                                                                        |
| -------------------------------- | ------------- | --------------------------------------------------------------------------- |
| 策略框架（Timer/Selector/Sizer） | **已实现**    | 三层分离，Composite 组合                                                    |
| 择时策略                         | **已实现**    | 双均线、MACD、RSI、KDJ、布林带 + **新增 `AIPredictorStrategy`**             |
| **AI 策略**                      | ✅ **已修复** | `ai_predictor.py:68-90` 加载持久化模型 → 消费预测 → 输出 Buy/Sell/Hold 信号 |
| 选股策略                         | **部分**      | 只有 MomentumSelector，无多因子/基本面选股                                  |
| 仓位管理                         | **部分**      | EqualWeight + FixedFraction，无 Kelly/风险平价/波动率目标                   |
| 组合优化                         | **缺失**      | 无 mean-variance、risk parity、约束优化                                     |
| 多策略组合                       | **部分**      | MultiStrategyRunner 静态权重分割，无共享现金/净额清算/动态再平衡            |

**AI 策略闭环：**

1. `ai_train.py` CLI → `AIPredictor.train()` → `joblib.dump()` 保存模型
2. `ai_predictor.py` 策略 → `AIPredictor.load()` 加载模型 → `predict(frame)` → 信号

---

## 七、回测引擎 🟢 完整度 80%（↑25%）

### 7a. 核心机制 ✅

| 机制                  | 状态          | 证据                                                                                 |
| --------------------- | ------------- | ------------------------------------------------------------------------------------ |
| Look-ahead bias 防范  | **已实现**    | 先撮合→再更新价格→再 on_bar                                                          |
| 订单撮合              | **已实现**    | Market 单按 next-bar close；Limit 单在 [low, high] 区间                              |
| T+1 逻辑              | ✅ **已接入** | `runner.py:119-125` 逐日解锁；`matcher.py:40-47` 卖出检查                            |
| 手续费模型            | ✅ **已接入** | 佣金万2.5最低5元、印花税卖出0.05%、过户费沪市0.001%                                  |
| 最小交易单位          | ✅ **已接入** | `matcher.py:60-66` 买入检查 100 股整数倍，不满足则拒绝                               |
| **涨跌停/停牌处理**   | ✅ **新增**   | `matcher.py:49-57` 停牌/涨停买/跌停卖拦截；`market_rules.py:56-59` 科创板20%/其他10% |
| **市场规则 CLI 连接** | ✅ **已修复** | `backtest.py:9,81,175` 始终传入 `ASHARE_RULES`                                       |

### 7b. 7a 缺陷修复确认

**6/27 审计：#1 致命缺陷"市场规则从未接入 CLI" → 已完全修复。** `backtest.py` 第 9 行导入 `ASHARE_RULES`，第 81 行传给 `BacktestRunner`，第 175 行传给 `MultiSymbolRunner`。`runner.py:63-64` 将规则传给 `Matcher` 和 `PortfolioManager`。`multi_runner.py:77-78` 同理。

**6/27 审计：#7 次优先缺口"涨跌停/停牌未处理" → 已完全修复。** `matcher.py:49-57` 实现停牌/涨停买/跌停卖三重拦截。`market_rules.py:56-59` 按 `symbol.startswith("68")` 区分科创板 20% vs 主板 10%。`runner.py:55-60` 和 `multi_runner.py:58-74` 在撮合前补全 bar 的涨跌停价。

### 7c. 仍缺失

| 机制                   | 状态                                                       |
| ---------------------- | ---------------------------------------------------------- |
| 复权处理（引擎侧）     | **缺失** — 依赖上游已复权数据                              |
| 滑点模型               | **部分** — 固定百分比滑点，默认 0.0                        |
| 可做空/融券            | **缺失**                                                   |
| 日内回测               | **缺失** — T+1 日期检测用 bar.timestamp 相等，分钟线会崩溃 |
| 多标的回测（传统策略） | **部分** — 传统策略多标的直接报错 `backtest.py:107-111`    |

---

## 八、绩效指标与归因 🟡 完整度 45%（↑5%）

### 8a. 已有指标（无变化）

同 6/27 审计。Sharpe、Sortino、Calmar、Max DD、胜率、盈亏比、平均持仓天数。

### 8b. 缺失指标（无变化）

信息比率、基准比较、年化换手率、因子暴露分析、Brinson 归因、滚动指标、Omega/Tail Ratio。

### 8c. 🆕 绩效归因 — Story-6

`multi_runner.py:88-98` 为每个标的创建独立现金桶，初始资金均分。`multi_runner.py:193-203` 成交时更新对应标的的现金桶。`multi_runner.py:218-228` 按标的记录权益曲线。`multi_runner.py:266` 在 `BacktestResult` 中输出 `sub_equity` 字段。

这是归因的基础设施，但离完整的 Brinson/因子/板块归因还差得远。API mapper (`report-mapper.ts:138-142`) 的 attribution 字段仍初始化为空/零值。

---

## 九、过拟合防护 🔴 完整度 0%（无变化）

**完全空白。这是 6/27 审计 5 个致命缺陷中唯一未被修复的。**

| 方法                                      | 状态                                                      |
| ----------------------------------------- | --------------------------------------------------------- |
| Walk-Forward 分析                         | **缺失** — UI 有类型/组件但无后端计算                     |
| 样本外验证                                | **缺失** — UI 有字段但 report-mapper 硬编码为 0           |
| 参数敏感性分析                            | **缺失** — UI 有热力图/表格但数据来自 mock                |
| 多重检验校正（Bonferroni/Holm）           | **缺失**                                                  |
| Deflated Sharpe Ratio                     | **缺失**                                                  |
| PBO (Probability of Backtest Overfitting) | **缺失**                                                  |
| Combinatorial Purged Cross-Validation     | **缺失**                                                  |
| 时间序列交叉验证                          | **缺失** — `model.py:38` 仍用 `train_test_split` 随机切分 |

**`model.py:38` 仍然是：**

```python
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=self.config.test_size, random_state=self.config.random_state,
)
```

随机 shuffle split，在金融时间序列上完全不适用。

---

## 十、模拟交易 (Paper Trading) 🔴 完整度 0%（无变化）

**完全缺失。** 无变化。没有前向模拟运行时、没有 paper/forward 命令、没有持仓跟踪或模拟成交引擎。

---

## 十一、实盘交易 🔴 完整度 0%（按设计）

同 6/27 审计。AGENTS.md `:159` 明确排除。

---

## 十二、实时风控 🟡 完整度 5%（无变化）

同 6/27 审计。只有自治循环内的 `DrawdownStop` 条件。

---

## 十三、自治闭环 🟡 完整度 25%（无变化）

| 组件                                  | 状态                                                                        |
| ------------------------------------- | --------------------------------------------------------------------------- |
| 循环引擎类型定义                      | **已实现**                                                                  |
| 终止条件                              | **已实现**                                                                  |
| LoopHandler（Worker 侧）              | **🔴 骨架** — `loop-handler.ts:63-70` 迭代循环仍被注释掉，始终返回 0 次迭代 |
| Ralph 自治 Harness                    | 开发任务跟踪工具，非交易 Agent                                              |
| 闭环（数据→因子→回测→报告→决策→迭代） | **未打通**                                                                  |

无变化。

---

## 十四、报告与输出 🟢 完整度 75%（↑5%）

| 组件                 | 状态                                                                         |
| -------------------- | ---------------------------------------------------------------------------- |
| 前端回测报告         | **已实现** — 17 个 Report\* 组件                                             |
| 前端因子报告         | **已实现** — 13 个 Factor\* 组件（部分渲染 mock）                            |
| **报告链路打通**     | ✅ **已修复** — Story-7：Python 分析器 → API mapper → 前端 `issues` 字段同步 |
| report_analysis 引擎 | **已实现** — 规则引擎 + 模板生成                                             |
| Obsidian Vault 同步  | **已实现**                                                                   |
| LLM 分析             | **预留接口**                                                                 |

---

## 十五、运维与监控 🟡 完整度 20%（无变化）

同 6/27 审计。CI/CD 已实现，Worker 进程管理为基础轮询，健康检查/日志告警/Docker 缺失。

---

## 🔴 当前最严重的缺陷（按紧急度排序）

| #   | 缺陷                                                                  | 影响                     | 修复难度 |
| --- | --------------------------------------------------------------------- | ------------------------ | -------- |
| 1   | **过拟合防护完全空白** — 无 walk-forward/OOS/deflated Sharpe/PBO/CPCV | 无法区分真信号和过拟合   | 高       |
| 2   | **特征提取仅有 3 类基础特征** — 无技术指标/基本面/另类数据特征        | ML 策略缺乏有效输入      | 中       |
| 3   | **因子分析缺 IC 衰减/中性化/正交化** — evaluator 未扩展               | 因子质量评估不完整       | 中       |
| 4   | **组合优化空白** — 只有 equal/fixed weight                            | 无法构建风险调整优化组合 | 中       |
| 5   | **模拟交易完全缺失** — 没有从回测到实盘的过渡                         | 回测→实盘之间有鸿沟      | 中       |

---

## 🟡 次优先缺口

| #   | 缺口                                                   |
| --- | ------------------------------------------------------ |
| 6   | 绩效归因仅基础多标的权益分解，无 Brinson/因子/板块归因 |
| 7   | 基准比较类型系统完备但从不填充真实数据                 |
| 8   | 盘中存活偏差 — 回测窗口内退市的标的不被中途排除        |
| 9   | 日内回测不支持 — T+1 日期相等检测对分钟线会崩溃        |
| 10  | Loop engine Worker 侧仍是骨架（迭代循环被注释）        |

---

## 📊 完整度总览

```
数据采集 █████████████████░░░  85%  (↑5%: 存活偏差过滤)
数据清洗 ██████████░░░░░░░░░░  50%
数据存储 █████████████████░░░  85%
特征提取 ██████░░░░░░░░░░░░░░  30%
因子挖掘 ████████░░░░░░░░░░░░  40%  (↑40%: FormulaFactor AST 引擎)
因子分析 ███████░░░░░░░░░░░░░  35%
策略构建 ███████████████░░░░░  75%  (↑10%: AI 策略闭环)
回测引擎 ████████████████░░░░  80%  (↑25%: 市场规则+T+1+涨跌停接入)
绩效指标 ████████░░░░░░░░░░░░  40%
绩效归因 ███░░░░░░░░░░░░░░░░░  15%  (↑15%: 多标的权益分解)
过拟合防护 ░░░░░░░░░░░░░░░░░░░░   0%
模拟交易 ░░░░░░░░░░░░░░░░░░░░   0%
实盘交易 ░░░░░░░░░░░░░░░░░░░░   0%  (按设计)
实时风控 █░░░░░░░░░░░░░░░░░░░   5%
自治闭环 █████░░░░░░░░░░░░░░░  25%
报告输出 ███████████████░░░░░  75%  (↑5%: 报告链路)
运维监控 ████░░░░░░░░░░░░░░░░  20%
```

---

## 📈 24 小时变化总结

自 2026-06-27 审计以来，通过 7 个 story 的迭代：

**✅ 已修复（4/5 致命缺陷）：**

- Story-1: 回测 CLI 接入市场规则（手续费/T+1/手数限制生效）
- Story-2: factor_eval 公式求值引擎（FormulaFactor AST 替代 close stub）
- Story-3: AI 模型持久化 + AI 预测策略消费闭环
- Story-5: 存活偏差防护（回测启动时按上市/退市日期过滤）

**✅ 新增能力：**

- Story-4: 涨跌停/停牌撮合拦截（科创板 20%/主板 10%）
- Story-6: 多标的权益分解（归因基础设施）
- Story-7: 报告链路打通（Python → API → 前端 issues 同步）

**🔴 仍缺失（1/5 致命缺陷 + 多项中优先级）：**

- 过拟合防护（致命缺陷 #4）— 零进展
- 模拟交易 — 零进展
- 特征提取扩展 — 零进展
- 因子分析扩展 — 零进展
- 组合优化 — 零进展

**投入产出比最高的下一步工作：** 过拟合防护是唯一剩余的致命缺陷，也是区分"能跑回测"和"回测结果可信"的关键分水岭。建议至少实现 TimeSeriesSplit + Walk-Forward + Deflated Sharpe Ratio 三条防线。
