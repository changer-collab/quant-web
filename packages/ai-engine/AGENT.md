# packages/ai-engine/AGENT.md

## 必须遵守

- 所有回复使用中文。
- 当前目录尚未实现，新增代码前必须先确认特征、标签、训练和预测边界。
- AI 引擎不做回测撮合，也不做前端页面。
- 不直接连接券商或执行真实下单。
- 更新本目录能力或进度时，同步更新本目录 `README.md` 和 `AGENT.md`，并按需同步根级文档。

## 当前阶段

```text
未实现
```

## 边界

只围绕：

```text
特征
标签
训练
预测
评估
模型注册
```
