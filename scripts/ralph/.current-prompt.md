# QuantForge 自治 Agent 指令

你是一个自治编码 Agent，正在 quant-web 项目中工作。

## 项目规则（必须遵守）

你正在操作的项目有严格的 Agent 角色边界。请先读取以下文件了解规则：

- `AGENTS.md` — 项目全局规则、角色定义、能力边界、协作接口
- `apps/*/AGENT.md` 或 `packages/*/AGENT.md` — 对应子项目的规则

**核心约束：**

- 每个 Agent 只在自己的工作范围内操作，不越界
- 先调研现有代码和目录，再提出方案或修改代码
- **优先新增文件或函数，修改已有代码前必须说明理由**——能加就不改，改就要有明确理由
- 不修改无关文件
- 不做真实下单、券商连接、实盘交易
- 所有回复使用中文
- 遵循 KISS 原则，非必要不要过度设计
- **你运行在非交互批处理模式（`--print`），没有人会回答你的问题。不要问"是否批准"、"是否允许"、"要不要继续"——读完 prd.json 后直接执行实现，不等待回复。不要调用要求用户交互的 skill（如 brainstorming/planning），直接写代码。**

## 跨迭代错误反馈

开始工作前，检查以下文件以了解上一轮的失败：

1. **`scripts/ralph/.last-error.json`** — 如果存在，说明上一轮有错误。先读取它，分析失败原因：
   - `detectedFailures` 告诉你了哪类检查失败（test_fail / lint_error / build_fail 等）
   - `summary` 是错误摘要
   - 不要重复同样的错误

## 你的任务

1. 读取 `scripts/ralph/.last-error.json`（如果存在）— 理解上一轮为什么失败
2. 读取 `scripts/ralph/prd.json` — 你的任务清单
3. 读取 `scripts/ralph/progress.txt` — 历史记忆和已发现的模式（先看 Codebase Patterns 部分）
4. 找到 `passes: false` 的**最高优先级**故事
5. 确认你在正确的分支上（参考 prd.json 的 `branchName`）
6. **只实现这一个故事**
7. 跑质量检查
8. 提交代码
9. 更新 prd.json（将完成的故事设为 `passes: true`）
10. 追加进度到 progress.txt

## 💡 核心规则：测试文件不可修改

**测试文件（`test_*.py` / `*.test.ts` / `*.spec.ts`）是正确行为的真相来源。**

- 测试失败 → 修复实现代码，**绝不修改测试来迎合错误的实现**
- 如果无法修复实现（超出 scope 或能力范围），保持测试红色
- 通过修改测试的断言值/跳过条件/判等逻辑来让测试通过，等同于制造假绿色

⚠️ **提交前自检**：如果本次修改的文件全是测试文件而没有对应的实现文件，这是一个危险信号。停下来检查。

⚠️ **关于修改测试的人为陷阱**：如果你发现自己同时修改了测试文件和实现文件，停下来问自己——这个测试真的是错的，还是我在找捷径让当前代码通过？如果测试预期值是8，实现返回10，这是测试错了还是实现错了？**正确答案：先修实现，测试不动。** 新的需求（预期值应该是10而不是8）属于新 story 的 scope，不要混在修复 story 里。

## 质量检查命令

完成故事后，必须运行以下检查（根据故事涉及的语言选择）：

### JavaScript/TypeScript 故事

```bash
pnpm lint
pnpm test
pnpm build
```

### Python 故事

```bash
cd packages/<相关包> && python -m pytest -v
```

### 涉及多个语言

```bash
pnpm lint && pnpm test && pnpm build
for pkg in strategy-runtime backtest-engine strategies data-client factor-lab ai-engine obsidian-sync; do
  (cd packages/$pkg && python -m pytest -v)
done
```

## 分支管理

- 检查 prd.json 中的 `branchName`
- 如果当前分支不是目标分支，切过去
- 每个故事完成后提交到当前分支
- 推送时用 `git push origin <branchName>`

## 提交规范

```
feat: [Story ID] - [Story Title]
```

## 进度日志格式

追加到 `scripts/ralph/progress.txt`（永远只追加，不替换）：

```
## [Date/Time] - [Story ID]
- 实现了什么
- 修改了哪些文件
- **后续迭代注意事项：**
  - 发现的模式（如 "这个项目用 X 做 Y"）
  - 遇到的坑（如 "改 W 的时候别忘了更新 Z"）
  - 有用的上下文（如 "评估面板在组件 X 里"）
---
```

## 代码模式汇总

如果你发现了**可复用的模式**，更新 `progress.txt` 顶部的 `## Codebase Patterns` 区域：

```
## Codebase Patterns
- 示例：所有 Python 包用 pyproject.toml + editable install
- 示例：前端测试通过 globalThis.fetch mock 脱离后端
- 示例：Worker 通过 PythonBridge 调用 Python CLI
```

## 更新 AGENTS.md

提交前，检查修改的文件所在目录是否有 AGENTS.md：

- 如果发现了有价值的可复用知识，更新对应的 AGENTS.md
- 不要添加故事特定的临时调试信息
- 不要重复 progress.txt 中已有的信息

## 前端故事验证

如果故事涉及 UI 修改：

1. 启动开发服务器：`pnpm --filter @quant/web dev`
2. 在浏览器中验证页面功能正常
3. 截图记录到 progress.txt

## 停止条件

完成一个故事后，检查 prd.json 中是否还有 `passes: false` 的故事：

- 如果**全部完成**（所有故事都是 `passes: true`），输出：`<promise>COMPLETE</promise>`
- 如果还有未完成的故事，正常结束（下一轮迭代会接手）

## 重要

- **每次迭代只做一个且仅一个故事。这是硬规则，不是建议。** 即使在同一次调用中你有能力多做，也必须停下来。原因：你是 `--print` 非交互模式，对话上下文会在长输出后被压缩(compact)，后面的故事会丢失关键代码细节导致质量下降。做完一个就提交退出，让下一轮 iteration 用干净上下文接下一个 story。
- 完成后 **5 分钟内提交并退出**，不要在非交互模式下无限扩展工作范围。
- 频繁提交
- 保持 CI 绿色
- 开始前先读 progress.txt 的 Codebase Patterns 部分


---

## 上一轮运行状态（由 Ralph Core 自动注入）

- 迭代次数: 1
- Feature: strategy-classification-refactor
- 连续无进展轮数: 0
- 上一轮有错误，请先分析失败原因再行动。

```json
{
  "iteration": 1,
  "exitCode": 1,
  "timestamp": "2026-06-28T09:47:42.175Z",
  "detectedFailures": [],
  "summary": ""
}
```

## 任务清单（含运行时状态）

```json
{
  "feature": "strategy-classification-refactor",
  "description": "策略分类体系重构：建立 Factor/Non-Factor/Transitional 三级分类，Python 类型扩展 + API 三层架构解耦 + 策略配置页（KV线预览）+ Workspace 两步工作流",
  "branchName": "ralph/strategy-classification",
  "userStories": [
    {
      "id": "story-1",
      "title": "Python 策略分类枚举 + StrategyMeta 扩展",
      "description": "在 strategy-runtime/types.py 中新增 StrategyCategory(str, Enum) 和 StrategySubcategory(str, Enum) 枚举。扩展 StrategyMeta：新增 category/subcategory/required_factors/factor_based 属性。扩展 StrategyParamDef：新增 chart_relevant(bool) 和 ui_constraints(list[UIConstraint]|None)。新增 UIConstraint dataclass（kind/target_field/target_value/action_value）。\n\n在 __init__.py 中导出新类型。\n\n更新全部 10 个存量策略的 meta()：\n- 5 个 combined 策略 (dual_ma/rsi/bollinger_band/macd/kdj) → NON_FACTOR / TREND_CTA\n- ai_predictor → NON_FACTOR / E2E_AI_TIMESERIES\n- momentum_selector → FACTOR_BASED / LINEAR_MULTI_FACTOR\n- ma_crossover → NON_FACTOR / TREND_CTA\n- equal_weight/fixed_fraction 不添加（仓位策略不独立展示）\n\nma_crossover 的 short_period/long_period 添加 chart_relevant=true。\n\n新建 tests/test_strategy_categories.py：测试枚举值、默认 category、factor_based 属性、ui_constraints 结构。",
      "acceptanceCriteria": [
        "StrategyCategory.FACTOR_BASED == 'factor_based'",
        "StrategySubcategory.TREND_CTA == 'trend_cta'",
        "StrategyMeta 默认 category == NON_FACTOR（向后兼容）",
        "StrategyMeta(category=FACTOR_BASED).factor_based == True",
        "UIConstraint 含 kind/target_field/target_value/action_value 四个字段",
        "10 个存量策略全部添加 category/subcategory",
        "cd packages/strategy-runtime && python -m pytest tests/test_strategy_categories.py -v 通过",
        "cd packages/strategies && python -m pytest -v 通过"
      ],
      "agent": "strategy-agent",
      "priority": 1,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-2",
      "title": "API 侧 TS 接口定义 + DB 表创建",
      "description": "在 apps/api/src/types.ts 中：TaskType 枚举追加 Diagnostics='diagnostics'，新增 StrategyCategory/StrategySubcategory 类型别名、StrategyConfig/ConfigSnapshot 接口。\n\n新建 apps/api/src/repositories/interfaces.ts：IConfigRepo(get/save)、IConfigHistoryRepo(append/list)、IDiagnosticRepo(save/getById/listByStrategy/purgeOlderThan)、DiagnosticResult 接口。\n\n在 apps/api/src/storage/schema.ts 中新增 3 张 Drizzle 表：strategy_configs(configJson/hash/updatedAt)、config_history(configJson/hash/createdAt + 索引)、diagnostic_results(configSnapshot/dataJson/createdAt + 索引)。\n\n修改 apps/api/src/storage/connection.ts：initApiDb() 内追加 pragma journal_mode=WAL、foreign_keys=ON、busy_timeout=5000。",
      "acceptanceCriteria": [
        "TaskType.Diagnostics = 'diagnostics'",
        "前端 StrategyCategory 类型值与 Python 枚举一致（factor_based/non_factor/transitional）",
        "IConfigRepo/IConfigHistoryRepo/IDiagnosticRepo 接口方法签名完整",
        "3 张表含 Drizzle schema 定义 + 索引",
        "connection.ts 启用 WAL + busy_timeout=5000",
        "cd apps/api && npx tsc --noEmit 无错误"
      ],
      "agent": "api-agent",
      "priority": 2,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-3",
      "title": "Repository 实现 + Service 层 + 三层架构接线",
      "description": "新建 apps/api/src/repositories/sqlite-config-repo.ts：SqliteConfigRepo implements IConfigRepo，构造函数接收 IConfigHistoryRepo，save() 内透明调用 history.append()。SqliteConfigHistoryRepo implements IConfigHistoryRepo。\n\n新建 apps/api/src/repositories/sqlite-diag-repo.ts：SqliteDiagnosticRepo implements IDiagnosticRepo，purgeOlderThan(days) 执行 DELETE WHERE created_at < 7天前。\n\n新建 apps/api/src/services/config-service.ts：StrategyConfigService 构造函数接收 IConfigRepo，暴露 getConfig/saveConfig 方法。\n\n新建 apps/api/src/services/diagnostic-service.ts：DiagnosticService 构造函数接收 IDiagnosticRepo，暴露 getById/listByStrategy/storeResult/purgeExpired 方法。\n\n修改 apps/api/src/index.ts（组合根）：初始化 configHistoryRepo → configRepo → diagRepo → configService → diagnosticService。\n\n修改 apps/api/src/app.ts：AppOptions 追加 configService/diagnosticService，buildApp 内 app.decorate。",
      "acceptanceCriteria": [
        "SqliteConfigRepo implements IConfigRepo",
        "SqliteConfigRepo.save() 后 config_history 表有新记录（透明写入）",
        "Service 层不 import Drizzle 或 better-sqlite3",
        "SqliteDiagnosticRepo.purgeOlderThan(7) 删除 7 天前记录",
        "index.ts 组合根创建所有实例 -> 注入 buildApp",
        "fastify instance 上可通过 app.configService / app.diagnosticService 访问",
        "cd apps/api && npx tsc --noEmit && npx tsc 编译通过"
      ],
      "agent": "api-agent",
      "priority": 3,
      "dependsOn": [
        "story-2"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 1,
        "lastError": null
      }
    },
    {
      "id": "story-4",
      "title": "Config CRUD 端点 GET/PUT /api/strategies/:name/config",
      "description": "新建 apps/api/src/routes/config.ts：Fastify 异步插件。GET /api/strategies/:name/config → configService.getConfig(name) → 404 if null。PUT /api/strategies/:name/config → body { config, hash } → configService.saveConfig() → 201 { saved: true, hash }。\n\n修改 apps/api/src/app.ts：app.register(configRoutes)。\n\n手动 curl 验证：GET 返回 404（未保存），PUT 保存后 GET 返回完整配置，SQLite 中 strategy_configs 和 config_history 表均有记录。",
      "acceptanceCriteria": [
        "curl GET /api/strategies/dual_ma/config → 404",
        "curl PUT + GET → 返回保存的 config JSON",
        "config_history 表同步写入变更记录",
        "cd apps/api && npx tsc --noEmit 无错误"
      ],
      "agent": "api-agent",
      "priority": 4,
      "dependsOn": [
        "story-3"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-5",
      "title": "GET /api/strategies 扩展 category/subcategory/workflowReady + Preview 端点",
      "description": "**5a. 策略列表扩展：**\n修改 apps/api/src/routes/strategy.ts：在 syncFromPython 响应映射中追加 category/subcategory/workflowReady/params(chartRelevant/uiConstraints) 字段。\n修改 apps/api/src/services/strategy-sync.ts：Python 子进程返回的 JSON 不含新字段时提供默认值（category='non_factor', subcategory=null）。\n\n**5b. Preview 端点：**\n新建 apps/api/src/services/preview-service.ts：PreviewService 类，纯 TypeScript 实现 SMA/EMA/RSI/MACD 四类指标计算 + 金叉死叉/超买超卖信号标注。computePreview(bars, params) 返回 { overlays, signals, fingerprint }。fingerprint = sha256(JSON(params) + 最后10根K线收盘价)。\n\n新建 apps/api/src/routes/preview.ts：POST /api/strategies/:name/preview。接收 body { symbol, timeframe, cursor, limit, preview_params }。从 dataCenter.providers.market.loadBars 查 K线 → previewService.computePreview() → 返回 { symbol, bars, overlays, signals, pagination, fingerprint, engine_version }。pagination.has_more 当 bars.length === limit 时为 true，next_cursor = 最旧 bar 的 timestamp。\n\n修改 apps/api/src/app.ts：app.register(previewRoutes)。",
      "acceptanceCriteria": [
        "GET /api/strategies 每项含 category/subcategory/workflowReady",
        "params 每项含 chartRelevant/uiConstraints",
        "POST preview 返回 bars/overlays/signals/fingerprint/pagination",
        "MACD 金叉(快线上穿慢线)信号 side='buy'",
        "MACD 死叉(快线下穿慢线)信号 side='sell'",
        "RSI 从 ≥30 跌破 30 → 标注买入信号",
        "RSI 从 ≤70 升破 70 → 标注卖出信号",
        "fingerprint = 'sha256:' + 16 位 hex",
        "pagination: cursor=null 返回最近 limit 根，has_more 正确",
        "cd apps/api && npx tsc --noEmit 无错误"
      ],
      "agent": "api-agent",
      "priority": 5,
      "dependsOn": [
        "story-4"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6",
      "title": "POST /api/tasks 改造 — configSnapshot 唯一真相源 + Diagnostics 端点",
      "description": "**6a. Task 改造：**\n修改 apps/api/src/routes/task.ts：POST /api/tasks 的处理中，payload.params 不再被路由使用（参数统一来自 payload.configSnapshot.params）。\n修改 apps/worker/src/handlers/backtest-handler.ts：构建 Python CLI 命令时，config 字段从 payload.configSnapshot.params 提取。\n修改 apps/worker/src/agents/backtest-agent.ts：回测请求格式化，params 来源改为 configSnapshot。\n\n**6b. Diagnostics 端点：**\n新建 apps/api/src/routes/diagnostics.ts：GET /api/diagnostics/:resultId → diagnosticService.getById()；GET /api/diagnostics?strategy=xxx → diagnosticService.listByStrategy()。\n修改 apps/api/src/routes/task.ts 的 SSE complete 处理：对于 diagnostics 类型任务，调用 diagnosticService.storeResult()，将 resultId 附加到 SSE result 事件体中。\n修改 apps/api/src/app.ts：app.register(diagnosticRoutes)。",
      "acceptanceCriteria": [
        "POST /api/tasks body 中接受但不使用顶层 params",
        "backtest-handler 从 configSnapshot.params 构建策略参数",
        "curl POST /api/tasks 返回 202",
        "SSE complete 事件含 resultId + resultType 字段",
        "GET /api/diagnostics/:resultId → 200 或 404",
        "GET /api/diagnostics?strategy=dual_ma → 诊断历史列表",
        "cd apps/api && npx tsc --noEmit 无错误"
      ],
      "agent": "api-agent",
      "priority": 6,
      "dependsOn": [
        "story-5"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-7",
      "title": "冒烟测试扩展 + P1-P2 全量验证",
      "description": "扩展 scripts/smoke-test.sh：在现有 3 个测试后追加 3 个新测试。Test 4: GET /api/strategies 返回含 'category' 字段。Test 5: PUT + GET /api/strategies/dual_ma/config 读写验证。Test 6: GET /api/diagnostics?strategy=dual_ma 返回空数组。\n\n运行全量验证：pnpm lint + pnpm test + pnpm build + bash scripts/smoke-test.sh + 全部 Python 包 pytest。\n\n如果有任何失败，分析错误日志、修复、重新验证。",
      "acceptanceCriteria": [
        "smoke-test.sh 覆盖 6 个端点（原 3 + 新 3）",
        "pnpm lint 全绿",
        "pnpm test 全绿（含 API 测试和前端测试）",
        "pnpm build 成功",
        "7 个 Python 包 pytest 全部通过",
        "bash scripts/smoke-test.sh 全部 PASS"
      ],
      "agent": "fullstack-agent",
      "priority": 7,
      "dependsOn": [
        "story-6"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-8",
      "title": "前端类型同步 + API 客户端层",
      "description": "修改 apps/web/src/data/types.ts：新增 StrategyCategory/StrategySubcategory 类型、StrategyParamDef（含 chartRelevant/uiConstraints）、StrategyRow（含 category/subcategory/workflowReady/summary）、PreviewResponse/BarData/ChartOverlay/PreviewSignal/Pagination/DiagnosticResult。\n\n新建 apps/web/src/api/strategies-config.ts：fetchStrategyConfig(name)、saveStrategyConfig(name, config, hash)。\n新建 apps/web/src/api/preview.ts：fetchPreview(strategyName, body)。\n新建 apps/web/src/api/diagnostics.ts：fetchDiagnostic(resultId)、fetchDiagnosticsByStrategy(strategyName)。\n\n修改 apps/web/src/api/strategies.ts：useStrategies hook 移除 hardcoded mode='traditional'，改用 API 返回的 category。\n\n修改 apps/web/tests/setup.ts：mock 覆盖 GET config(→null)、PUT config(→{saved:true})、POST preview(→{bars:[],overlays:[],signals:[],fingerprint:'',pagination:{has_more:false}})、GET diagnostics(→null/[])。",
      "acceptanceCriteria": [
        "前端 StrategyCategory 值与后端完全一致",
        "StrategyRow 含 category/subcategory/workflowReady",
        "PreviewResponse 含 bars/overlays/signals/pagination/fingerprint",
        "fetchStrategyConfig / saveStrategyConfig / fetchPreview / fetchDiagnostic 函数可用",
        "useStrategies 不硬编码 mode",
        "tests/setup.ts mock 覆盖所有 5 个新端点",
        "cd apps/web && npx tsc --noEmit 无错误",
        "cd apps/web && npm test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 8,
      "dependsOn": [
        "story-7"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-9",
      "title": "StrategyPage 容器 + StrategyGridNew 策略总览",
      "description": "新建 apps/web/src/components/strategy-page.tsx：双态容器（view='grid' | 'config'）。默认 grid 态渲染 StrategyGridNew，选中策略后切到 config 态（左边 ConfigPanel + 右边 KlineChart）。顶部「← 策略总览」按钮返回 grid。\n\n新建 apps/web/src/components/strategy-grid-new.tsx：按 category 三区分组（FACTOR_BASED/NON_FACTOR/TRANSITIONAL），每区按 subcategory 折叠分组显示策略卡片。卡片含 name/description/subcategory 标签/「进入工作区」按钮（仅 workflowReady 时可用）。\n\n修改 apps/web/src/App.tsx：PageId 联合类型追加 'strategy'，侧边栏追加 Strategy 导航项，条件渲染 StrategyPage。\n\n新建 apps/web/src/styles/strategy-page.module.css：页面布局样式。",
      "acceptanceCriteria": [
        "侧边栏含 Strategy 导航项，点击进入策略总览",
        "策略卡片按三区（因子型/非因子型/过渡形态）分组显示",
        "每区按 subcategory 折叠分组",
        "卡片含 name/description/subcategory tag",
        "workflowReady=false 时「进入工作区」按钮 disabled",
        "选中策略 → 页面切换到左右两栏布局（ConfigPanel + KlineChart）",
        "「← 策略总览」按钮可返回 grid 态",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 9,
      "dependsOn": [
        "story-8"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-10",
      "title": "ConfigPanel — 左侧动态配置表单",
      "description": "新建 apps/web/src/components/config-panel.tsx：动态渲染配置表单。\n- 一级 Tab 切换：因子型 / 非因子型 / 过渡形态\n- 二级 Tab 切换：当前 category 下的子类型\n- 基础参数区：根据 strategy.params 渲染输入控件（number→slider+数字框，select→下拉，bool→checkbox）\n- 因子型专属区块：因子池多选下拉树 + 数据预处理流水线（极值缩尾 slider ±3σ/±5σ + 中性化 checkbox + 标准化 radio）\n- 非因子型专属区块：时序窗口参数（lookback_window slider + hold_period 数字框）+ 指标工具箱（MACD/RSI/布林带勾选卡片组，勾选后显示微调参数）+ 动态参数区（按子类型条件渲染：高频→盘口深度档位/E2E→特征序列长度/事件→T-N 区间选择器）\n- 过渡形态专属区块：数据源绑定下拉 + 情感衰减半衰期 slider + 映射目标因子下拉\n\n表单联动校验：读取每个 param 的 ui_constraints，实现灰显（disable_when）、强制勾选（require_when）、设默认值（set_default_when）、范围限制（range_when）。\n\n底部操作按钮：保存配置（调 saveStrategyConfig）、预览回测、提交任务。\n\n300ms debounce：参数变化 → 等 300ms → 提取 chart_relevant=true 的字段 → 调 fetchPreview → 传递给 KlineChart（通过父组件 state 或 callback）。",
      "acceptanceCriteria": [
        "一级 Tab 可切换 category",
        "二级 Tab 可切换 subcategory",
        "param.type='int'/'float' 渲染为滑块+数字框",
        "param.type='select' 渲染为下拉框（options）",
        "param.type='bool' 渲染为复选框",
        "因子型显示因子池+预处理流水线（3步2组1单选）",
        "非因子型显示窗口参数+指标工具箱+动态参数区",
        "过渡形态显示数据源+衰减+映射",
        "ui_constraints.disable_when 触发后该字段灰显不可点击",
        "ui_constraints.require_when 触发后该字段强制勾选+提示文案",
        "参数变化 300ms 后自动调 fetchPreview（仅 chart_relevant 字段）",
        "保存配置按钮调 saveStrategyConfig → 成功反馈",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 10,
      "dependsOn": [
        "story-9"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-11",
      "title": "KlineChart — 右侧 Canvas K线图 + Preview 引擎集成",
      "description": "新建 apps/web/src/components/kline-chart.tsx：Canvas 渲染 K线图。\n\n**图层结构（从底到顶）：**\n1. K线蜡烛图（OHLC）：阳线红色/阴线绿色，鼠标 hover 显示 OHLC tooltip\n2. MA 均线叠加（从 preview overlays 取 MA 数据，颜色区分）\n3. 成交量柱状图（K线下方副图，红绿对应涨跌）\n4. 策略专属副图（根据当前 subcategory 动态切换）：因子型→IC序列折线/趋势→RSI曲线/高频→价差曲线/过渡→情感得分\n5. 信号/交易点位标注层：买卖箭头（绿色↑/红色↓）标注在K线上方，hover 显示 tooltip（含 reason + factor_snapshot）\n6. 时间轴拖拽选择器（底部）：左右拖动时间轴 → 检测到达边界时调 fetchPreview(cursor=next_cursor) → 追加 bars 到数组头部\n\n**功能：**\n- 品种搜索框（顶部）：输入 symbol 后调 preview 重新加载\n- K线图左上角淡灰色「[预览引擎]」标签：hover Tooltip \"信号由轻量引擎实时生成，用于参数灵敏度调节，精确绩效请以回测报告为准\"\n- fingerprint 变更检测：fetchPreview 返回的 fingerprint 与上次不同 → 右下角 Toast \"预览信号算法已更新，请刷新页面加载新版本\"（3 秒自动消失，不阻塞操作）\n- 响应 preview 数据变化 → Canvas 重绘\n\n新建 apps/web/src/styles/kline-chart.module.css：K线面板样式。",
      "acceptanceCriteria": [
        "Canvas 渲染 K线蜡烛图（红涨绿跌）",
        "MA 均线叠加在 K 线上方",
        "成交量副图在 K 线下方",
        "策略专属副图按 subcategory 动态切换内容",
        "买卖箭头标注在对应 bar 位置",
        "hover 箭头弹出 tooltip 显示开仓理由",
        "品种搜索框可切换标的",
        "时间轴左拖到边界自动加载更多历史 K 线",
        "[预览引擎] 标签 + Tooltip 内容正确",
        "fingerprint 变化 → Toast 提示 3 秒消失",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 11,
      "dependsOn": [
        "story-10"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-12",
      "title": "Workspace 页面 — 水平两步步骤条 + 动态诊断",
      "description": "新建 apps/web/src/components/workspace-page.tsx：从策略配置页「进入工作区」按钮进入。水平两步步骤条（步骤1: 策略诊断与研究 / 步骤2: 回测与绩效），数字圆圈+连接线，当前步骤高亮。\n\nWorkspaceStep1 按 subcategory 动态渲染诊断内容：\n- 因子型：因子 IC/IR 分析图表区 + 分层收益柱状图 + 因子相关性热力图 + 按钮「确认因子有效 → 进入回测」\n- 趋势/套利/高频：参数敏感性热力图 + 信号数量/质量分布直方图 + 滑点/手续费压力测试衰减曲线 + 按钮「确认参数鲁棒 → 进入回测」\n- 宏观量化：宏观变量与资产收益互相关图 + 经济周期阶段标注 + 按钮「确认宏观锚定 → 进入回测」\n- 事件驱动：事件前后累计异常收益 (CAR) 图 + 事件样本统计 + 按钮「确认事件效应 → 进入回测」\n- E2E AI：特征重要性排名 (SHAP) + 训练/验证 Loss 曲线 + 按钮「确认模型稳定 → 进入回测」\n\n诊断计算流程：POST /api/tasks { type: \"diagnostics\", payload: { strategy, configSnapshot } } → SSE 接收进度 → complete 事件拿到 resultId + data → 更新 URL searchParams → 渲染图表。\n\n点击确认按钮 → setStep(2) → 进入 WorkspaceStep2。\n\nWorkspaceStep2：回测配置摘要（只读展示，从 configSnapshot 读取）→ 提交回测任务 → SSE 进度条 → 绩效指标卡 + 权益曲线 + 交易明细。\n\nF5 刷新恢复：页面加载时检查 URL searchParams ?diagnosticId=xxx → fetchDiagnostic(id) → 恢复到步骤1并渲染诊断结果；若 404 → 提示「诊断结果已过期，请重新计算」。\n\n新建 apps/web/src/styles/workspace.module.css。\n修改 apps/web/src/App.tsx：追加 workspace 页路由。",
      "acceptanceCriteria": [
        "策略配置页「进入工作区」→ 跳转 Workspace 页面",
        "水平步骤条含步骤1+步骤2，数字圆圈+连接线",
        "步骤1 因子型渲染 IC/IR + 分层收益 + 相关性热力图",
        "步骤1 非因子型渲染参数敏感性 + 信号分布 + 滑点压力",
        "步骤1 宏观/事件/E2E 各子类型有对应诊断内容",
        "诊断数据通过 SSE 实时推送进度",
        "步骤1→步骤2 确认按钮可用",
        "步骤2 回测配置摘要只读展示",
        "步骤2 提交回测 → SSE 进度 → 绩效报告",
        "F5 刷新 → URL ?diagnosticId=xxx 恢复诊断结果",
        "诊断不存在时提示过期 + 可重新计算",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 12,
      "dependsOn": [
        "story-11"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-13",
      "title": "端到端接线 + 清理 + 国际化",
      "description": "清理 useResearchWorkflow.ts：删除 ResearchModeId 类型引用，activeMode 替换为 activeCategory。删除所有 hardcoded 'traditional'/'hft'/'ai' 分组逻辑。\n\n标记旧 strategy-grid.tsx 为 deprecated（被 strategy-grid-new.tsx 取代）。\n\n更新国际化：apps/web/src/data/zh.ts 和 en.ts 新增策略分类相关文案标签（factor_based/non_factor/transitional 及所有子类型）。\n\n全量验证：\n- pnpm lint 全绿\n- pnpm test 全绿（JS 侧全部测试）\n- pnpm build 成功\n- 7 个 Python 包 pytest 全部通过\n- bash scripts/smoke-test.sh 全 PASS\n\n确认无引入新依赖（检查 pnpm list --depth=0 无变化）。",
      "acceptanceCriteria": [
        "useResearchWorkflow 中无 'traditional' | 'hft' | 'ai' 硬编码",
        "strategy-grid.tsx 不再被任何组件导入",
        "en.ts/zh.ts 含 category/subcategory 相关文案",
        "前端不引入新 npm 依赖",
        "pnpm lint 全绿",
        "pnpm test 全绿",
        "pnpm build 成功",
        "7 个 Python 包 pytest 全部通过",
        "bash scripts/smoke-test.sh 全 PASS"
      ],
      "agent": "fullstack-agent",
      "priority": 13,
      "dependsOn": [
        "story-12"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    }
  ]
}
```
