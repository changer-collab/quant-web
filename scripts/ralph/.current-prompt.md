# QuantForge 自治 Agent 指令

你是一个自治编码 Agent，正在 quant-web 项目中工作。

## 项目规则（必须遵守）

你正在操作的项目有严格的 Agent 角色边界。请先读取以下文件了解规则：

- `AGENTS.md` — 项目全局规则、角色定义、能力边界、协作接口
- `apps/*/AGENT.md` 或 `packages/*/AGENT.md` — 对应子项目的规则

**核心约束：**

- 每个 Agent 只在自己的工作范围内操作，不越界
- 先调研现有代码和目录，再提出方案或修改代码
- **优先新增文件或函数，修改已有代码前必须说明理由**——能加就不改，改就要有明确理由
- 不修改无关文件
- 不做真实下单、券商连接、实盘交易
- 所有回复使用中文
- 遵循 KISS 原则，非必要不要过度设计

## 跨迭代错误反馈

开始工作前，检查以下文件以了解上一轮的失败：

1. **`scripts/ralph/.last-error.json`** — 如果存在，说明上一轮有错误。先读取它，分析失败原因：
   - `detectedFailures` 告诉你了哪类检查失败（test_fail / lint_error / build_fail 等）
   - `summary` 是错误摘要
   - 不要重复同样的错误
2. **`scripts/ralph/.prd.state.json`** — 如果存在，读取 `storyAttempts` 了解哪些故事已经尝试过

## 你的任务

1. 读取 `scripts/ralph/.last-error.json`（如果存在）— 理解上一轮为什么失败
2. 读取 `scripts/ralph/prd.json` — 你的任务清单
3. 读取 `scripts/ralph/progress.txt` — 历史记忆和已发现的模式（先看 Codebase Patterns 部分）
4. 找到 `passes: false` 的**最高优先级**故事
5. 确认你在正确的分支上（参考 prd.json 的 `branchName`）
6. **只实现这一个故事**
7. 跑质量检查
8. 提交代码
9. 更新 prd.json（将完成的故事设为 `passes: true`）
10. 追加进度到 progress.txt

## 质量检查命令

完成故事后，必须运行以下检查（根据故事涉及的语言选择）：

### JavaScript/TypeScript 故事

```bash
pnpm lint
pnpm test
pnpm build
```

### Python 故事

```bash
cd packages/<相关包> && python -m pytest -v
```

### 涉及多个语言

```bash
pnpm lint && pnpm test && pnpm build
for pkg in strategy-runtime backtest-engine strategies data-client factor-lab ai-engine obsidian-sync; do
  (cd packages/$pkg && python -m pytest -v)
done
```

## 分支管理

- 检查 prd.json 中的 `branchName`
- 如果当前分支不是目标分支，切过去
- 每个故事完成后提交到当前分支
- 推送时用 `git push origin <branchName>`

## 提交规范

```
feat: [Story ID] - [Story Title]
```

## 进度日志格式

追加到 `scripts/ralph/progress.txt`（永远只追加，不替换）：

```
## [Date/Time] - [Story ID]
- 实现了什么
- 修改了哪些文件
- **后续迭代注意事项：**
  - 发现的模式（如 "这个项目用 X 做 Y"）
  - 遇到的坑（如 "改 W 的时候别忘了更新 Z"）
  - 有用的上下文（如 "评估面板在组件 X 里"）
---
```

## 代码模式汇总

如果你发现了**可复用的模式**，更新 `progress.txt` 顶部的 `## Codebase Patterns` 区域：

```
## Codebase Patterns
- 示例：所有 Python 包用 pyproject.toml + editable install
- 示例：前端测试通过 globalThis.fetch mock 脱离后端
- 示例：Worker 通过 PythonBridge 调用 Python CLI
```

## 更新 AGENTS.md

提交前，检查修改的文件所在目录是否有 AGENTS.md：

- 如果发现了有价值的可复用知识，更新对应的 AGENTS.md
- 不要添加故事特定的临时调试信息
- 不要重复 progress.txt 中已有的信息

## 前端故事验证

如果故事涉及 UI 修改：

1. 启动开发服务器：`pnpm --filter @quant/web dev`
2. 在浏览器中验证页面功能正常
3. 截图记录到 progress.txt

## 停止条件

完成一个故事后，检查 prd.json 中是否还有 `passes: false` 的故事：

- 如果**全部完成**（所有故事都是 `passes: true`），输出：`<promise>COMPLETE</promise>`
- 如果还有未完成的故事，正常结束（下一轮迭代会接手）

## 重要

- 每次迭代只做**一个故事**
- 频繁提交
- 保持 CI 绿色
- 开始前先读 progress.txt 的 Codebase Patterns 部分


---

## 上一轮运行状态（由 Ralph Core 自动注入）

- 迭代次数: 1
- Feature: full-loop-real-data-obsidian
- 连续无进展轮数: 0
- 上一轮有错误，请先分析失败原因再行动。

```json
{
  "iteration": 3,
  "exitCode": 0,
  "timestamp": "2026-06-25T23:34:10.745Z",
  "detectedFailures": [
    "vitest_fail",
    "lint_error"
  ],
  "summary": "- 浠?`.last-error.json` 鐪嬪埌 `vitest_fail`銆乣lint_error`銆乣typescript_error`銆乣build_fail` 浣嗗湪寮€濮嬪墠锛岃鎴戝厛妫€鏌ュ綋鍓嶄唬鐮佺姸鎬侊紝鐞嗚В涓婁竴杞姤鐨?`vitest_fail` 閿欒銆傚ソ鐨勶紝鎴戞潵鍒嗘瀽褰撳墠鐘舵€併€? - story-1 鍒?story-4 宸插畬鎴愶紙`passes: true`锛?- 涓嬩竴涓渶楂樹紭鍏堢骇鏈畬鎴愮殑鏄?**story-5**锛堜紭鍏堢骇 5锛?- 涓婁竴杞姤閿?`vitest_fail / lint_error / typescript_error / build_fail`锛屼絾 summary 鏄贡鐮?- 浠?progress.txt 鐪嬪埌 story-4 宸茬粡瀹屾垚锛?2 涓祴璇曢€氳繃 - 鍐嶇湅 `.last-error.json` 鐨勪贡鐮侀儴鍒嗭紝瀹為檯涓婃槸涓婃杩唬鐨?agent 杈撳嚭鍐呭琚埅鏂簡 2. 涓婁竴杞殑 `vitest_fail` 鏉ヨ嚜 story-4 涔嬪墠 iteration2 "
}
```

## 任务清单（含运行时状态）

```json
{
  "feature": "full-loop-real-data-obsidian",
  "description": "拆件诊断修复回测全链路：每种故障隔离修复并验证后进入下一层。",
  "branchName": "ralph/real-backtest-report",
  "userStories": [
    {
      "id": "story-1",
      "title": "先观测：在 Worker 和 API 添加关键日志埋点，跑一次回测收集实际故障证据",
      "description": "当前不确定故障位置，不猜不修。先加诊断埋点，跑一次回测收集证据链。\n\n需埋点位置：\n1. `apps/worker/src/main.ts` processTask() 中：调用 handler.handle() 前后各加一行 console.log 打印传入 payload 和返回的 result key 集合\n2. `apps/worker/src/handlers/backtest-handler.ts`：handle() 中 Python CLI 调用成功后，console.log 打印 backtestData 的顶层 key 和 metrics 内容\n3. `apps/api/src/routes/task.ts` /complete 路由：收到请求后 console.log 打印 result 的顶层 key 和 backtestResult.metrics\n4. `apps/web/src/hooks/useResearchWorkflow.ts` handleRunResearch：SSE result 事件中 console.log 打印 event.data 的顶层 key\n\n验证方式：启动 API + Worker + 前端，提交一次 dual_ma 回测，查看各端控制台输出的日志。\n\n此 story 不改业务逻辑，只加日志。如果日志不足以定位，再加。",
      "acceptanceCriteria": [
        "日志覆盖 Worker 收到 payload → Python 回测结果 → API /complete → 前端 SSE result 的所有链路",
        "提交 dual_ma 回测后，能从日志看到每一层的输出",
        "无 TypeScript 构建错误（pnpm build 通过）"
      ],
      "agent": "fullstack-agent",
      "priority": 1,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-2",
      "title": "修复 Worker 失败问题（依据 story-1 日志定位）",
      "description": "依据 story-1 的日志分析 Worker 失败根因。可能是：\n- Python CLI 回测命令报错（如 dataRange.startTs 格式不对）\n- AI analyze 命令报错（如 LLM 调用失败导致异常未正确捕获）\n- Obsidian sync 命令报错（如 OBSIDIAN_API_URL 未设置时 Python 端抛异常而非优雅跳过）\n- PythonBridge 解析 NDJSON 失败\n- API /complete 接口不可达或返回非 2xx\n\n根据实际日志定位后修复（只修根因）。\n\n示例修复（根据实际情况选择）：\n- 如果 Python CLI 报错：修复 dataClient.query_bars 参数或 CLI 命令构建\n- 如果 AI analyze 报错：加强 try-catch 范围\n- 如果 sync 报错：加强 sync_backtest.py 的异常处理\n- 如果 API 不可达：检查 API_BASE_URL 配置或重试机制",
      "acceptanceCriteria": [
        "回测任务能从 pending → running → completed 正常流转",
        "Worker 控制台没有 ERROR 级别的输出",
        "pnpm --filter @quant/api test 通过",
        "pnpm build 通过"
      ],
      "agent": "fullstack-agent",
      "priority": 2,
      "dependsOn": [
        "story-1"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-3",
      "title": "修复前端 Job 列表名称显示异常（显示 '运行研究：backtest 草稿 #1'）",
      "description": "当前 job 名称显示为 '运行研究：backtest 草稿 #1' 而非策略名（如 '运行研究：dual_ma'）。\n\n根因分析：`useResearchWorkflow.ts` 的 `handleRunResearch()` 中通过 `.then(taskId => ...)` 闭包访问的 `selectedStrategyForLanguage` 可能为 undefined（因为 .then 是异步的，闭包中捕获的是 render 时的引用，而 `selectedStrategyForLanguage` 是一个 useMemo 计算值）。\n\n修复：\n1. 在 `createResearchJob` 调用处，直接使用 `selectedStrategy`（同步变量）而非 `selectedStrategyForLanguage`（useMemo 值）来传入 strategy\n2. 或者确保 `.then()` 闭包中使用的变量是 stable ref（如通过 useRef 保存最新值）\n\n具体修改 `apps/web/src/hooks/useResearchWorkflow.ts` 的 `handleRunResearch`：\n- 在 .then() 之前将 strategy 信息存为局部变量\n- 在 createResearchJob 和 createResearchReport 时使用局部变量\n\n验证：提交 dual_ma 回测，job 列表显示 '运行研究：dual_ma' 而非 'backtest 草稿'。",
      "acceptanceCriteria": [
        "提交 dual_ma 回测后，job 列表显示 '运行研究：dual_ma'",
        "提交 A 策略，job 列表显示 '运行研究：{A策略名}'",
        "pnpm --filter @quant/web test 通过",
        "pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 3,
      "dependsOn": [
        "story-2"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-4",
      "title": "修复 SSE error 事件后前端仍跳转报告页的问题",
      "description": "当前 Worker 失败后，前端 SSE 收到 error 事件，但 error 事件处理中只设置了 job 状态为 Failed，没有阻止跳转到报告页。`handleRunResearch` 中 submitBacktestTask 的 .then() 内先创建了 job（状态 Running），然后 submitAndStream 注册事件回调。如果 Worker 很快失败，SSE 可能在 .then() 完成时已发送了 status:failed → result:error，导致 createResearchReport 和 mapBacktestResultToReport 仍然执行。\n\n修复：\n1. `useResearchWorkflow.ts` 中，在 SSE error 事件处理中：清除已创建的 report（如果有）、并且不要跳转到报告页\n2. 或者更根本：只有在 SSE result 事件（而非 error 事件）时才创建 report 并 setActiveReportId\n3. SSE event 中增加 `data.error` 的兜底判断\n\n此外，如果任务失败但仍然想查看历史回测数据，应该在 report 页面显示「任务失败」的状态提示而非空页面。\n\n验证：提交一个必然会失败的回测（如不存在的策略名），确认不会跳转到报告页。",
      "acceptanceCriteria": [
        "Worker 失败后前端不跳转到报告页",
        "job 状态显示为 Failed（红色/错误状态）",
        "pnpm --filter @quant/web test 通过",
        "pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 4,
      "dependsOn": [
        "story-3"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-5",
      "title": "打通 Obsidian 同步链路并补齐同步内容",
      "description": "更新 `packages/strategy-runtime/quantforge_strategy/commands/sync_backtest.py` 的 `_dict_to_backtest_result()`：补齐 sortino_ratio/calmar_ratio/annualized_volatility/max_drawdown_duration/profit_loss_ratio/avg_holding_days/max_single_profit/max_single_loss 字段以及 drawdownCurve/monthlyReturns/annualReturns 数组。\n\n更新 `packages/obsidian-sync/quantforge_obsidian/builders/backtest.py` 的 `build_backtest_report()`：在绩效表格中添加新指标，添加回撤曲线/月度收益/年度收益的 markdown 表格。\n\n同时修复 obsidian-sync 在不设置 OBSIDIAN_API_URL 时优雅跳过（当前已有该逻辑但验证是否正常）。",
      "acceptanceCriteria": [
        "sync_backtest.py 的 _dict_to_backtest_result() 映射所有新指标字段和 equityData 数组",
        "build_backtest_report() 包含 sortinoRatio/calmarRatio/annualizedVolatility/profitLossRatio/avgHoldingDays 和 equityData 表格",
        "未设置 OBSIDIAN_API_URL 时不抛异常",
        "cd packages/obsidian-sync && python -m pytest -v 通过",
        "cd packages/backtest-engine && python -m pytest -v 通过"
      ],
      "agent": "obsidian-agent",
      "priority": 5,
      "dependsOn": [
        "story-2"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6",
      "title": "完善回测功能：前端 Workspace 面板添加数据范围选择器",
      "description": "在 `apps/web/src/components/workspace.tsx` 的 WorkspaceMockup 中添加日期范围选择输入框：\n- 起始日期：`<input type=\"date\">`\n- 结束日期：`<input type=\"date\">`\n- 默认值回填 2023-01-01 ~ 2024-12-31\n- onChange 时将日期字符串转为毫秒时间戳更新 backtestConfig.startTs/endTs\n- 添加工具函数 tsToDate/dateToTs",
      "acceptanceCriteria": [
        "Workspace 面板显示起始日期和结束日期输入框（type=date）",
        "选择日期后提交回测时 payload 包含正确的 startTs/endTs",
        "pnpm --filter @quant/web test 通过",
        "pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 6,
      "dependsOn": [
        "story-3"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-7",
      "title": "端到端验证：回测全链路展示真实数据",
      "description": "启动 API + Worker + 前端，跑一次 dual_ma 回测，验证：\n- SSE 正常推送（progress → status → result）\n- job 列表显示策略名称且状态为 Completed\n- 报告页权益曲线、回撤曲线、月度热力图、年度柱状图展示真实数据\n- 风险指标卡片展示非零真实值\n- 刷新页面后历史报告仍在\n- 控制台无 TypeScript 错误\n\n此 story 不改代码，只验证。",
      "acceptanceCriteria": [
        "前端报告页权益曲线/回撤曲线展示真实数据点",
        "月度热力图展示非空月份数据",
        "年度柱状图展示非空年度数据",
        "风险指标卡片 sortinoRatio/calmarRatio 为非零值",
        "交易统计卡片 profitLossRatio/avgHoldingDays 为非零值",
        "刷新页面后历史报告仍展示数据",
        "控制台无 TypeScript 类型错误"
      ],
      "agent": "fullstack-agent",
      "priority": 7,
      "dependsOn": [
        "story-4",
        "story-5",
        "story-6"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    }
  ]
}
```
