# QuantForge 自治 Agent 指令

你是一个自治编码 Agent，正在 quant-web 项目中工作。

## 项目规则（必须遵守）

你正在操作的项目有严格的 Agent 角色边界。请先读取以下文件了解规则：

- `AGENTS.md` — 项目全局规则、角色定义、能力边界、协作接口
- `apps/*/AGENT.md` 或 `packages/*/AGENT.md` — 对应子项目的规则

**核心约束：**

- 每个 Agent 只在自己的工作范围内操作，不越界
- 先调研现有代码和目录，再提出方案或修改代码
- **优先新增文件或函数，修改已有代码前必须说明理由**——能加就不改，改就要有明确理由
- 不修改无关文件
- 不做真实下单、券商连接、实盘交易
- 所有回复使用中文
- 遵循 KISS 原则，非必要不要过度设计
- **你运行在非交互批处理模式（`--print`），没有人会回答你的问题。不要问"是否批准"、"是否允许"、"要不要继续"——读完 prd.json 后直接执行实现，不等待回复。不要调用要求用户交互的 skill（如 brainstorming/planning），直接写代码。**

## 跨迭代错误反馈

开始工作前，检查以下文件以了解上一轮的失败：

1. **`scripts/ralph/.last-error.json`** — 如果存在，说明上一轮有错误。先读取它，分析失败原因：
   - `detectedFailures` 告诉你了哪类检查失败（test_fail / lint_error / build_fail 等）
   - `summary` 是错误摘要
   - 不要重复同样的错误

## 你的任务

1. 读取 `scripts/ralph/.last-error.json`（如果存在）— 理解上一轮为什么失败
2. 读取 `scripts/ralph/prd.json` — 你的任务清单
3. 读取 `scripts/ralph/progress.txt` — 历史记忆和已发现的模式（先看 Codebase Patterns 部分）
4. 找到 `passes: false` 的**最高优先级**故事
5. 确认你在正确的分支上（参考 prd.json 的 `branchName`）
6. **只实现这一个故事**
7. 跑质量检查
8. 执行 Doc Sync Gate：判断并更新相关 `docs/plans/*`、`docs/superpowers/specs/*`、`docs/development/*`、`.skills/ralph-harness/*`；如果不需要更新 docs，在 progress.txt 记录原因
9. 提交代码
10. 更新 prd.json（将完成的故事设为 `passes: true`）
11. 追加进度到 progress.txt

## 💡 核心规则：测试文件不可修改

**测试文件（`test_*.py` / `*.test.ts` / `*.spec.ts`）是正确行为的真相来源。**

- 测试失败 → 修复实现代码，**绝不修改测试来迎合错误的实现**
- 如果无法修复实现（超出 scope 或能力范围），保持测试红色
- 通过修改测试的断言值/跳过条件/判等逻辑来让测试通过，等同于制造假绿色

⚠️ **提交前自检**：如果本次修改的文件全是测试文件而没有对应的实现文件，这是一个危险信号。停下来检查。

⚠️ **关于修改测试的人为陷阱**：如果你发现自己同时修改了测试文件和实现文件，停下来问自己——这个测试真的是错的，还是我在找捷径让当前代码通过？如果测试预期值是8，实现返回10，这是测试错了还是实现错了？**正确答案：先修实现，测试不动。** 新的需求（预期值应该是10而不是8）属于新 story 的 scope，不要混在修复 story 里。

## 质量检查命令

完成故事后，必须运行以下检查（根据故事涉及的语言选择）：

### JavaScript/TypeScript 故事

```bash
pnpm lint
pnpm test
pnpm build
```

### Python 故事

```bash
cd packages/<相关包> && python -m pytest -v
```

### 涉及多个语言

```bash
pnpm lint && pnpm test && pnpm build
for pkg in strategy-runtime backtest-engine strategies data-client factor-lab ai-engine obsidian-sync; do
  (cd packages/$pkg && python -m pytest -v)
done
```

## 分支管理

- 检查 prd.json 中的 `branchName`
- 如果当前分支不是目标分支，切过去
- 每个故事完成后提交到当前分支
- 推送时用 `git push origin <branchName>`

## 提交规范

```
feat: [Story ID] - [Story Title]
```

## 进度日志格式

追加到 `scripts/ralph/progress.txt`（永远只追加，不替换）：

```
## [Date/Time] - [Story ID]
- 实现了什么
- 修改了哪些文件
- **后续迭代注意事项：**
  - 发现的模式（如 "这个项目用 X 做 Y"）
  - 遇到的坑（如 "改 W 的时候别忘了更新 Z"）
  - 有用的上下文（如 "评估面板在组件 X 里"）
---
```

## 代码模式汇总

如果你发现了**可复用的模式**，更新 `progress.txt` 顶部的 `## Codebase Patterns` 区域：

```
## Codebase Patterns
- 示例：所有 Python 包用 pyproject.toml + editable install
- 示例：前端测试通过 globalThis.fetch mock 脱离后端
- 示例：Worker 通过 PythonBridge 调用 Python CLI
```

## Doc Sync Gate

提交前，必须判断本 story 是否需要同步文档或开发基础设施：

- 改变执行状态、优先级、阻塞点、下一步 → 更新对应 `docs/plans/*` 的 Current Status / Decision Log / Doc Sync Log
- 改变 API payload、结果结构、DB schema、任务状态机、分类枚举、层间职责边界 → 更新对应 `docs/superpowers/specs/*`
- 发现长期开发标准或 Ralph 可复用规则 → 更新 `docs/development/*` 或 `.skills/ralph-harness/*`
- Ralph 执行的 story → 追加 `scripts/ralph/progress.txt`，并同步 `prd.json` passes
- 如果判断不需要更新 docs，也要在 `progress.txt` 中写明：`Doc Sync: no docs update needed because ...`

**不要等“之后再补文档”。文档同步是完成条件，不是收尾建议。**

## 更新 AGENTS.md

提交前，检查修改的文件所在目录是否有 AGENTS.md：

- 如果发现了有价值的可复用知识，更新对应的 AGENTS.md
- 不要添加故事特定的临时调试信息
- 不要重复 progress.txt 中已有的信息

## 前端故事验证

如果故事涉及 UI 修改：

1. 启动开发服务器：`pnpm --filter @quant/web dev`
2. 在浏览器中验证页面功能正常
3. 截图记录到 progress.txt

## 停止条件

完成一个故事后，检查 prd.json 中是否还有 `passes: false` 的故事：

- 如果**全部完成**（所有故事都是 `passes: true`），输出：`<promise>COMPLETE</promise>`
- 如果还有未完成的故事，正常结束（下一轮迭代会接手）

## 重要

- **每次迭代只做一个且仅一个故事。这是硬规则，不是建议。** 即使在同一次调用中你有能力多做，也必须停下来。原因：你是 `--print` 非交互模式，对话上下文会在长输出后被压缩(compact)，后面的故事会丢失关键代码细节导致质量下降。做完一个就提交退出，让下一轮 iteration 用干净上下文接下一个 story。
- 完成后 **5 分钟内提交并退出**，不要在非交互模式下无限扩展工作范围。
- 频繁提交
- 保持 CI 绿色
- 开始前先读 progress.txt 的 Codebase Patterns 部分


---

## 上一轮运行状态（由 Ralph Core 自动注入）

- 迭代次数: 11
- Feature: （未设置）
- 连续无进展轮数: 0
- 上一轮有错误，请先分析失败原因再行动。

```json
{
  "iteration": 10,
  "exitCode": 0,
  "timestamp": "2026-07-01T17:10:23.709Z",
  "detectedFailures": [
    "lint_error",
    "build_fail"
  ],
  "summary": "﻿璁╂垜鍒嗘瀽褰撳墠鐘舵€併€備笂娆¤繍琛岋紙iteration 9锛夊畬鎴愪簡 story-9锛屼絾 iteration 9 鐨勬姤鍛婃樉绀?`lint_error`銆俿tory-10 鏈?`attempts: 1` 璇存槑涔嬪墠灏濊瘯杩囦絾鏈€氳繃銆傜幇鍦ㄩ渶瑕佸疄鐜?story-10 鈥?Phase 3d DB schema additive migration銆? 淇敼娑夊強锛?1. **schema.ts** 鈥?涓?`strategyConfigs` 鍔?`category/subcategory/schemaVersion` 鍒楋紝涓?`diagnosticResults` 鍔?`subcategory/engineVersion/expiresAt` 鍒?2. **connection.ts** 鈥?ALTER TABLE 杩佺Щ閫昏緫锛堝吋瀹规棫 DB 鏂囦欢锛? 鏂拌〃 CREATE 璇彞鍚屾鐜板湪鏇存柊 connection.ts 鈥?鍔犲叆鏂板垪鐨?CREATE TABLE 鍜屾棫鏁版嵁搴撶殑 ALTER TABLE 杩佺Щ锛歂ow I need to "
}
```

## 任务清单（含运行时状态）

```json
{
  "feature": "backend-sync-realign",
  "description": "整合主线切片 2（18 story，高质量细粒度，Phase 2-5）。Phase 2 canonical taxonomy 5 故事（Python→API→前端 lockstep）→ Phase 3 ConfigSnapshot 7 故事（类型→Service→Route→DB→双 Repo）→ Phase 4 Preview 2 故事（校验+合并）→ Phase 5 Task/Worker 3 故事（验证+fail-closed）→ E2E 验收。不覆盖 Phase 6（transitional 算法）/Phase 7（回测 E2E，留后续切片）。整个切片不打断前端运行时：bar shape 留旧名、backtest handler 保留 params fallback（WARN deprecated）、config route 双 shape 兼容。",
  "branchName": "ralph/backend-sync-realign",
  "userStories": [
    {
      "id": "story-1",
      "title": "Phase 2a — Python StrategySubcategory 枚举迁移到 canonical 10 值",
      "description": "**实测**: types.py:89-102 StrategySubcategory 仍是旧 10 值。NONLINEAR_ML='nonlinear_ml'/HIGH_FREQUENCY='high_frequency'/MEAN_REVERSION/TAIL_RISK_HEDGING 需删/改。真实 8 策略只用 TREND_CTA/E2E_AI_TIMESERIES/LINEAR_MULTI_FACTOR（全是目标态稳定值），零引用。\n\n**任务**:\n1. types.py StrategySubcategory:保留 LINEAR_MULTI_FACTOR/TREND_CTA/ARBITRAGE/MACRO_QUANT/EVENT_DRIVEN/E2E_AI_TIMESERIES;改名 NONLINEAR_ML→ML_NONLINEAR_FACTOR('ml_nonlinear_factor')/HIGH_FREQUENCY→HFT_MICROSTRUCTURE('hft_microstructure');删除 MEAN_REVERSION/TAIL_RISK_HEDGING;新增 INDEX_ENHANCEMENT('index_enhancement')/EVENT_SENTIMENT_FACTOR('event_sentiment_factor')。\n2. 更新 test_strategy_categories.py:断言 canonical 10 存在+旧 4 不存在。\n3. grep -rn packages/ 确认无残留引用旧成员。\n\nINDEX_ENHANCEMENT/EVENT_SENTIMENT_FACTOR 仅补枚举入口，不建新策略。",
      "acceptanceCriteria": [
        "StrategySubcategory 恰好 canonical 10 成员，值字符串:linear_multi_factor/index_enhancement/ml_nonlinear_factor/trend_cta/arbitrage/hft_microstructure/macro_quant/event_driven/e2e_ai_timeseries/event_sentiment_factor",
        "访问 StrategySubcategory.MEAN_REVERSION→AttributeError（枚举不存在该成员）",
        "grep packages/ 无 HIGH_FREQUENCY/NONLINEAR_ML/MEAN_REVERSION/TAIL_RISK_HEDGING 残留",
        "现有 8 策略 meta 构造不报错（category/subcategory 用 enum 成员，改名自动跟随）",
        "cd packages/strategy-runtime && python -m pytest -v 通过；cd ../strategies && python -m pytest -v 通过"
      ],
      "agent": "strategy-runtime-agent",
      "priority": 1,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-2",
      "title": "Phase 2b — API types.ts StrategySubcategory union + strategy route workflowReady 收敛",
      "description": "**实测**: API types.ts:336-346 union 旧 10 值；routes/strategy.ts:30 与 :58 两处各自内联 workflowReady=`category==='transitional'||subcategory!=null`，重复且不符合 plan 的 canonical 成员校验。\n\n**任务**:\n1. types.ts StrategySubcategory union→canonical 10 值（与 story-1 逐字一致）。\n2. routes/strategy.ts 提取共享 isWorkflowReady(cat,sub):subcategory∈canonical 10 值集且 category∈canonical 3→true;subcategory=null→false。消除两处内联 `subcategory!==null`。\n3. 参数映射保持现状（key/min/max/chart_relevant/ui_constraints snake_case 不动）。\n4. strategy.test.ts 更新/新增用例。",
      "acceptanceCriteria": [
        "API StrategySubcategory union 恰为 canonical 10 值，与 Python types.py 逐字一致",
        "GET / 与 GET /:name 的 workflowReady 由同一 isWorkflowReady() helper 计算，不再有重复内联 `subcategory!==null`",
        "curl GET /api/strategies → subcategory='trend_cta' 策略 workflowReady=true；subcategory=null sizer workflowReady=false",
        "策略列表不出现 nonlinear_ml/high_frequency/mean_reversion/tail_risk_hedging",
        "失败路径：未知 subcategory 值 → workflowReady=false，不抛异常",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过；pnpm build 通过"
      ],
      "agent": "api-agent",
      "priority": 2,
      "dependsOn": [
        "story-1"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-3",
      "title": "Phase 2c — 前端 types.ts StrategySubcategory union + strategySubcategoryLabels 同步",
      "description": "**实测**: web/types.ts:123-133 union 旧 10 值；strategySubcategoryLabels（types.ts ~474）是 `Record<StrategySubcategory,string>` 硬对象——union 改名/删键不同步会直接断 tsc 编译。\n\n**任务**:\n1. types.ts StrategySubcategory union→canonical 10 值（与 story-2 逐字一致）。\n2. strategySubcategoryLabels 键集同步：删 mean_reversion/tail_risk_hedging 键，加 index_enhancement/event_sentiment_factor 键。\n3. appData.ts 重新导出新 union 和 labels。\n\n仅改 types 层，不改 mock 数据或组件。",
      "acceptanceCriteria": [
        "web StrategySubcategory union 恰为 canonical 10 值，与 API types.ts 逐字一致",
        "strategySubcategoryLabels 键集 = canonical 10，旧键 mean_reversion/tail_risk_hedging 已删除，新键 index_enhancement/event_sentiment_factor 存在且有 label 值",
        "cd apps/web && npx tsc --noEmit 无错误（union 改完后 Record 类型编译通过）",
        "pnpm --filter @quant/web test 通过；pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 3,
      "dependsOn": [
        "story-1"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-4",
      "title": "Phase 2d — 前端 en.ts/zh.ts mock 数据迁移旧 subcategory 值",
      "description": "**实测**: en.ts/zh.ts mock StrategyRow 含 mean_reversion(:204,225,266,336)、high_frequency(:284,349,362)、nonlinear_ml(:388)。虽然 story-3 改了枚举值，mock 数据是纯字符串字面量（非类型绑定），不阻断 build——但运行时 labels 查不到旧 key 会显示空白。\n\n**任务**:\n1. en.ts/zh.ts 所有 mock StrategyRow subcategory 重映射:high_frequency→hft_microstructure、nonlinear_ml→ml_nonlinear_factor；mean_reversion 无 canonical 对应→trend_cta（最近的 non_factor 择时风格）。tail_risk_hedging 在 mock 中定义为 label 但无 strategy 行使用，label 删（story-3 已删 labels 键），确认无残留。\n2. grep apps/web/src/data 确认旧字符串全消失。\n\n不改组件文件。",
      "acceptanceCriteria": [
        "grep 'mean_reversion|high_frequency|nonlinear_ml|tail_risk_hedging' apps/web/src/data/en.ts 无匹配",
        "grep 'mean_reversion|high_frequency|nonlinear_ml|tail_risk_hedging' apps/web/src/data/zh.ts 无匹配",
        "mock 数据中 hft_microstructure/ml_nonlinear_factor/trend_cta 行数与迁移前一致（仅改名，不删行）",
        "cd apps/web && npx tsc --noEmit 无错误；pnpm --filter @quant/web test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 4,
      "dependsOn": [
        "story-3"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-5",
      "title": "Phase 2e — 前端 config-panel + kline-chart 硬编码子分类字符串迁移",
      "description": "**实测**: config-panel.tsx 子分类列表(~17-28 行)含 nonlinear_ml/high_frequency/mean_reversion，及 high_frequency 条件分支(:263 盘口深度、:620 渲染、:706 排除列表)。kline-chart.tsx 副图路由(:294-335 硬编码 mean_reversion/high_frequency/nonlinear_ml，:555-557 条件)。story-3 改了 union 但组件仍用硬编码字符串字面量，CASE 不匹配会导致功能静默失效（如 high_frequency 订单簿深度不显示）。\n\n**任务**:\n1. config-panel.tsx:所有旧子分类字符串→canonical 值（nonlinear_ml→ml_nonlinear_factor、high_frequency→hft_microstructure）；mean_reversion 分支→trend_cta（择时参数区复用，去掉均值回复专属逻辑）；tail_risk_hedging 分支移除。\n2. kline-chart.tsx:副图路由子分类条件同步改名/删。\n\n不改 types/labels/mock 数据（已由 story-3/4 完成）。",
      "acceptanceCriteria": [
        "grep -n 'nonlinear_ml|high_frequency|mean_reversion|tail_risk_hedging' apps/web/src/components/config-panel.tsx 无匹配",
        "grep -n 'nonlinear_ml|high_frequency|mean_reversion|tail_risk_hedging' apps/web/src/components/kline-chart.tsx 无匹配",
        "config-panel 子分类列表显示 hft_microstructure/ml_nonlinear_factor/trend_cta（而非旧名）",
        "kline-chart 副图按 canonical 子分类路由（trend_cta/arbitrage/hft_microstructure 等各自有对应副图），不因字符串不匹配渲染空白",
        "失败路径：未知/新 canonical 子分类（如 index_enhancement）→渲染默认副图而非崩溃",
        "cd apps/web && npx tsc --noEmit 无错误；pnpm --filter @quant/web test 通过；pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 5,
      "dependsOn": [
        "story-3"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6",
      "title": "Phase 3a — API types.ts ConfigSnapshot 富化 + 三类 Param 接口",
      "description": "**实测**: API types.ts ConfigSnapshot={strategy,params}(无 schemaVersion/category/hash)。Plan §1.3 要求含 schemaVersion/strategyVersion/category/subcategory/params/hash/updatedAt 及三类 category-specific params。\n\n**任务**:\n1. types.ts 新增 ConfigSchemaVersion=1。扩展 ConfigSnapshot:{schemaVersion,strategy,strategyVersion,category,subcategory,params,hash,updatedAt}。新字段可选 ?（兼容现有引用不强制全量赋值）。\n2. 定义 FactorBasedConfigParams(factor_pool/winsorize/neutralization/standardization/interaction_terms/max_interaction_order)、NonFactorConfigParams(lookback_window/hold_period/indicators/indicator_params/dynamic_params)、TransitionalConfigParams(data_source/sentiment_decay_half_life/target_factor_pool)。\n3. 现有引用 ConfigSnapshot 的代码（DiagnosticResultWire/DiagnosticResult 等）需检查：新字段 ? 可选不引发 tsc 报错；若某处用 `as ConfigSnapshot` 断言可能丢失新字段（运行时 undefined，不报错）。\n\n仅类型层，不改 service/route/DB。",
      "acceptanceCriteria": [
        "API ConfigSnapshot 含 schemaVersion(1|number)/strategy/strategyVersion/category/subcategory/params/hash/updatedAt",
        "FactorBasedConfigParams 含 winsorize([number,number])/neutralization(string[])/standardization('zscore'|'quantile'|'rank')",
        "NonFactorConfigParams 含 lookback_window/hold_period/indicators/indicator_params/dynamic_params",
        "TransitionalConfigParams 含 data_source/sentiment_decay_half_life/target_factor_pool",
        "cd apps/api && npx tsc --noEmit 无新增错误（现有引用用可选字段兼容）"
      ],
      "agent": "api-agent",
      "priority": 6,
      "dependsOn": [
        "story-2"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-7",
      "title": "Phase 3a′ — 前端 types.ts ConfigSnapshot 镜像扩展",
      "description": "**实测**: web/types.ts ConfigSnapshot={strategy,params},与 API 当前一致但落后 story-6 的富化版本。前端 DiagnosticResult/ConfigPanel/WorkspacePage 引用 ConfigSnapshot。\n\n**任务**:\n1. web/types.ts ConfigSnapshot 镜像 API 新结构（含 schemaVersion/strategyVersion/category/subcategory/hash/updatedAt）。\n2. 同步三类 Param 接口声明（FactorBasedConfigParams 等）。\n3. appData.ts 重新导出。\n4. 现有引用处（workspace-page.tsx 的 configSnapshot state、config-panel.tsx 的 configPayload）检查：旧代码只读 params 和 strategy，不改动。\n\n仅类型层，前端不产生新的 configSnapshot 构造（仍只传 {strategy,params}，新字段 undefined，API 侧 story-8 补默认值）。",
      "acceptanceCriteria": [
        "web ConfigSnapshot 字段名与嵌套与 API ConfigSnapshot 逐字一致（schemaVersion/strategyVersion/category/subcategory/params/hash/updatedAt）",
        "appData.ts 导出 FactorBasedConfigParams/NonFactorConfigParams/TransitionalConfigParams",
        "cd apps/web && npx tsc --noEmit 无新增错误（现有赋值 {strategy,params} 兼容，新字段 ?）",
        "pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 7,
      "dependsOn": [
        "story-3",
        "story-6"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-8",
      "title": "Phase 3b — ConfigService：默认快照构建器 + canonical hash + getOrDefault + save(expectedHash)",
      "description": "**实测**: config-service.ts:9-21 仅有 getConfig→null（无默认值）和 saveConfig(configJson,hash)（无 hash 冲突校验）。Plan 要求 GET 无配置时返回 defaults、PUT 校验 expectedHash 冲突→409、canonical JSON hash。\n\n**任务**:\n1. 新增 buildDefaultSnapshot(strategy,strategyVersion,category?)→ConfigSnapshot:用 SHA256(canonicalJSON({strategy,strategyVersion,category,params:{}})) 生成 hash,schemaVersion=1,updatedAt=Date.now()。canonical JSON = 键排序 + 无空格 的 JSON.stringify。\n2. getConfig→getOrDefault(strategy,strategyVersion):调用 repo 查 DB→有则 {persisted:true,configSnapshot};无→{persisted:false,configSnapshot:buildDefaultSnapshot(...)}。\n3. saveConfig→save(snapshot,expectedHash?):若 expectedHash 提供且 DB 当前 hash 不同→throw ConfigHashConflictError(含 currentHash,currentSnapshot);匹配或首存→repo.save+返回保存后 ConfigSnapshot。\n4. 导出 ConfigHashConflictError 类供 route 返回 409。\n5. 添加 service unit test 覆盖 default/hash 冲突/保存。",
      "acceptanceCriteria": [
        "buildDefaultSnapshot('dual_ma','1.0')→{schemaVersion:1,strategy:'dual_ma',strategyVersion:'1.0',category:'non_factor',params:{},hash 非空 16 位 hex,updatedAt≈now}",
        "getOrDefault('dual_ma','1.0') 无 DB 数据→{persisted:false,configSnapshot:default};先 save→再 getOrDefault→{persisted:true,configSnapshot:保存值}",
        "canonical hash 同内容同 hash（键序不影响），不同内容不同 hash",
        "save(snapshot,expectedHash='sha256:old') 与 DB hash 不一致→throw ConfigHashConflictError;expectedHash=undefined→成功",
        "失败路径：category=invalid→throw validation error；strategy=''(空)→throw",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过"
      ],
      "agent": "api-agent",
      "priority": 8,
      "dependsOn": [
        "story-6"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-9",
      "title": "Phase 3c — Config routes GET/PUT 合约对齐（向后兼容旧 body）",
      "description": "**实测**: config.ts GET 无配置返回 404；PUT 接受 {config,hash}。Plan 要求 GET 200(defaults)、PUT {category,subcategory,params,expectedHash}、hash 冲突 409。\n\n**任务**:\n1. GET /:name/config:调 configService.getOrDefault(name,version)。version 从 strategySyncService.syncFromPython() 查对应策略 meta.version 获得（若策略不在 registry→404）。始终 200，不返回 404。\n2. PUT /:name/config:支持旧 shape({config,hash})与新 shape({category,subcategory,params,expectedHash})。旧 shape 自动封装为 ConfigSnapshot(schemaVersion=1,category 从 registry 推导降级 non_factor,params=config,hash 用请求体的 hash)。新 shape 直接构造成 ConfigSnapshot。expectedHash offer→pass to save;无→save 不校验。\n3. PUT 响应:201+{saved:true,configSnapshot}（旧 client 收到 saved:true 仍兼容，加 configSnapshot 不影响）。hash 冲突→409+{error,currentHash,currentSnapshot}。\n4. 添加 route test 覆盖旧 body 兼容性+GET defaults+hash 冲突。",
      "acceptanceCriteria": [
        "GET /api/strategies/dual_ma/config(无保存)→200,body={persisted:false,configSnapshot:{schemaVersion:1,category:'non_factor',params:{},hash,...}}",
        "PUT /config body={config:{period:20},hash:'sha256:abc'}→201+{saved:true,configSnapshot}（旧 client 的修复不打断）",
        "PUT body={category:'non_factor',subcategory:'trend_cta',params:{period:20},expectedHash:'...'}→201（新 shape 对称处理）",
        "PUT expectedHash 与 DB 当前 hash 不一致→409+{error:'Config hash conflict',currentHash,currentSnapshot}",
        "失败路径:不存在的 strategy→404;category='trash_cat'→422;空 body→400",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过；pnpm build 通过"
      ],
      "agent": "api-agent",
      "priority": 9,
      "dependsOn": [
        "story-8"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-10",
      "title": "Phase 3d — DB schema additive migration（strategy_configs + diagnostic_results 加列）",
      "description": "**实测**: strategy_configs 列=strategy/config_json/hash/updated_at(缺 category/subcategory/schemaVersion)。diagnostic_results 列=id/task_id/strategy/category/config_snapshot/data_json/created_at(缺 subcategory/engineVersion/expiresAt)。\n\n**任务**:\n1. schema.ts strategyConfigs Drizzle def 加 category(text,notNull,'non_factor')/subcategory(text,nullable)/schemaVersion(integer,notNull,1)。\n2. schema.ts diagnosticResults Drizzle def 加 subcategory(text,nullable)/engineVersion(text,notNull,'legacy')/expiresAt(integer,notNull)。\n3. connection.ts 同步新增列（ALTER TABLE ADD COLUMN 或 IF NOT EXISTS 在 CREATE TABLE 内）。\n\n只加列不改名/删列（safe additive），所有新列 DEFAULT 或 nullable，旧 rows 不受影响。",
      "acceptanceCriteria": [
        "strategy_configs 表含 category(TEXT NOT NULL DEFAULT 'non_factor')/subcategory(TEXT)/schemaVersion(INTEGER NOT NULL DEFAULT 1)",
        "diagnostic_results 表含 subcategory(TEXT)/engineVersion(TEXT NOT NULL DEFAULT 'legacy')/expiresAt(INTEGER NOT NULL)",
        "旧 DB 文件（无新列）启动后 schema 增补列，SELECT 返回 DEFAULT 值而非 NULL/crash",
        "sql.js 环境建表成功，无 native module 依赖引入",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过"
      ],
      "agent": "api-agent",
      "priority": 10,
      "dependsOn": [
        "story-9"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-11",
      "title": "Phase 3e — SqliteConfigRepo 读写新字段 + interfaces.ts 签名更新",
      "description": "**实测**: SqliteConfigRepo.save 写 strategy/config_json/hash/updated_at 4 列，get 返回 {config_json,hash,updated_at} 扁平结构。现在 DB 有 category/subcategory/schemaVersion 新列（story-10），repo 需同步读写。\n\n**任务**:\n1. SqliteConfigRepo.save:接收 ConfigSnapshot 对象（含 category/subcategory/schemaVersion/strategyVersion），写 strategy/category/subcategory/schemaVersion/config_json/hash/updated_at 列。strategyVersion 写在 config_json 内或新增列均可（建议新增 strategy_version TEXT）。\n2. SqliteConfigRepo.get:返回 ConfigSnapshot 对象（读 category/subcategory/schemaVersion 列，缺省补 'non_factor'/null/1）。\n3. SqliteConfigHistoryRepo.append:同步记录 category/subcategory/schemaVersion/strategyVersion。\n4. interfaces.ts IConfigRepo.save 签名从 (strategy,configJson,hash) 更新为 (snapshot:ConfigSnapshot,expectedHash?)。updateStrategy 接口 get 返回类型从 StrategyConfig|null 改为 {persisted:boolean,configSnapshot:ConfigSnapshot}。\n5. 类型改动波及的调用处(config-service.ts/config.ts)需同步更新（story-8/9 的产出与之一致）。",
      "acceptanceCriteria": [
        "SqliteConfigRepo.save(snapshot) 写 category/subcategory/schemaVersion 列，get 回读 field-by-field 一致",
        "SqliteConfigRepo.get 对旧 row(无新列)→category='non_factor'/subcategory=null/schemaVersion=1（mapper 补默认值）",
        "configHistory 表记录含 strategy_version/category/subcategory（history list 返回含这些字段）",
        "IConfigRepo 接口 save 参数为 ConfigSnapshot，get 返回 {persisted,configSnapshot}",
        "cd apps/api && npx tsc --noEmit 无错误（repo 改后所有按接口编程的 service/route 编译通过）",
        "pnpm --filter @quant/api test 通过"
      ],
      "agent": "api-agent",
      "priority": 11,
      "dependsOn": [
        "story-10"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 1,
        "lastError": null
      }
    },
    {
      "id": "story-12",
      "title": "Phase 3f — SqliteDiagnosticRepo 读写 subcategory/engineVersion/expiresAt",
      "description": "**实测**: SqliteDiagnosticRepo.save 写 DiagnosticResult(id/taskId/strategy/category/configSnapshot/dataJson/createdAt),getById/listByStrategy 读同样字段。DB 已有新列（story-10）但 repo 未读写。\n\n**任务**:\n1. SqliteDiagnosticRepo.save:补写 subcategory/engineVersion/expiresAt 列。expiresAt=createdAt+7*24*3600*1000;engineVersion 从 result-processor 传入（缺省 'legacy'）。\n2. SqliteDiagnosticRepo.getById/listByStrategy:mapper 读新列，旧 row 缺省 engineVersion='legacy'/expiresAt=createdAt+7天/subcategory=null。\n3. IDiagnosticRepo 接口:DiagnosticResult 类型增加 subcategory:string|null/engineVersion:string/expiresAt:number 字段。\n4. diagnostics.ts route mapper(toWire)利用 repo 返回的新字段直接投影（不再静态补 expiresAt=createdAt+7），子类 subcategory 若为 null 保持 null。\n5. 波及:diagnostics-result-processor.ts 构造 DiagnosticResult 时传 subcategory（从 configSnapshot.subcategory 或降级 null）/engineVersion（从 result.data.engine_version 或 'legacy'）。",
      "acceptanceCriteria": [
        "save(diagnosticResult)→DB row 含 subcategory/engineVersion/expiresAt,getById 回读一致",
        "旧 row(无新列)→mapper 补 engineVersion='legacy'/expiresAt=createdAt+7天/subcategory=null（不抛异常）",
        "diagnostics route GET /:resultId→response 含 engineVersion（永不为 undefined），expiresAt>createdAt",
        "IDiagnosticRepo 签名含 subcategory/engineVersion/expiresAt",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过；pnpm build 通过"
      ],
      "agent": "api-agent",
      "priority": 12,
      "dependsOn": [
        "story-10"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-13",
      "title": "Phase 4a — Preview chart_relevant 参数校验→422",
      "description": "**实测**: preview route 不做 preview_params 过滤，任意 key 均可传入（包括非图表相关参数）。Plan 要求非 chart_relevant 字段返回 422。\n\n**任务**:\n1. preview route 新增 validatePreviewParams(strategyName,preview_params):查 strategy registry meta(params 列表),若 preview_params 有任一 key 对应的 param 的 chart_relevant!==true→返回 422 {error:'preview_params contains non-chart-relevant fields',fields:[...]}。\n2. 若 preview_params 为空→跳过校验（空 params 不算违规）。\n3. 若策略不在 registry→404（不做校验）。\n4. 仅校验,不改变 PreviewService.computePreview 本身的逻辑。\n5. 添加 route test:chart_relevant=false 的参数被拒绝 422;chart_relevant=true 的通过。",
      "acceptanceCriteria": [
        "策略 dual_ma 有 chart_relevant=false 的 param='offset',preview_params={offset:5}→422,body.fields=['offset']",
        "chart_relevant=true 的 'period'→preview_params={period:20}→不触发 422,正常计算",
        "preview_params={}→跳过校验,正常返回 preview",
        "不存在的策略 → 404（不做校验，route 早返回）",
        "失败路径：preview_params 含多个非 chart 字段→422 列出所有违规字段名",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过；pnpm build 通过"
      ],
      "agent": "api-agent",
      "priority": 13,
      "dependsOn": [
        "story-9"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-14",
      "title": "Phase 4b — Preview config 合并（saved/default configSnapshot.params 为 baseline）",
      "description": "**实测**: preview route 只用 preview_params 调用 PreviewService，不合并已保存配置。如果用户已保存 config={period:20} 但只传 preview_params={offset:5}→computePreview 少收到 period=20。\n\n**任务**:\n1. preview route 执行 validatePreviewParams 后，从 configService.getOrDefault(strategyName,version) 获取 saved/default configSnapshot.params。用该 params 作 baseline→preview_params 覆盖（Object.assign 浅合即可）。合并后的 params 传给 PreviewService.computePreview。\n2. 若 configService 未注入（legacy app），降级只用 preview_params（日志 WARN），不崩溃。\n\n不改 PreviewService 本身。不改变 bar shape。",
      "acceptanceCriteria": [
        "已保存 config(params={period:20,offset:5}),preview_params={period:10}→合并后 params={period:10,offset:5}（客户端覆盖优先）",
        "无保存 config→baseline 为 default configSnapshot.params={}→preview_params 全量生效",
        "preview_params={}→只用 baseline params 计算（不因缺少参数而空白）",
        "configService 未注入(undefined)→降级只用 preview_params,日志 WARN,不崩溃",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过；pnpm build 通过"
      ],
      "agent": "api-agent",
      "priority": 14,
      "dependsOn": [
        "story-9",
        "story-13"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-15",
      "title": "Phase 5a — API 任务 payload ConfigSnapshot 校验",
      "description": "**实测**: API task.ts POST handler 对 payload 零校验（裸透传）。Plan 要求 backtest/diagnostics 任务强制含 configSnapshot、拒绝顶层 params、校验 strategy 一致+canonical category。\n\n**任务**:\n1. task.ts 新增 validateTaskPayload(type,payload):\n  - backtest/diagnostics:configSnapshot 必须存在且含 strategy(非空)→否则 400。\n  - payload.configSnapshot.strategy 必须 === payload.strategy→否则 400(mismatch)。\n  - payload.configSnapshot.category 必须 ∈ canonical 3→否则 400。\n  - 顶层 payload.params 拒绝(在 configSnapshot.params 之外再传顶层 params→400)。\n  - collect:放行（不校验 configSnapshot）。factor_compute/factor_eval/ai_train:拒绝 400('not supported in this workflow')。\n2. 校验集成到 POST /api/tasks 路由。\n3. 添加 task route test 覆盖所有拒绝分支。",
      "acceptanceCriteria": [
        "POST /api/tasks type=backtest 无 configSnapshot→400,error 含 'configSnapshot required'",
        "type=backtest,payload.strategy='dual_ma'≠configSnapshot.strategy='macd'→400,error 含 'strategy mismatch'",
        "type=backtest,configSnapshot.category='mean_reversion'（非 canonical 3 值）→400",
        "type=diagnostics,payload={params:{...},configSnapshot:{...}}→400,error 含 'params not allowed at top level'",
        "type=collect→放行,不触发 configSnapshot 校验",
        "失败路径:payload={} → 400（非 500/未捕获异常）",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过；pnpm build 通过"
      ],
      "agent": "api-agent",
      "priority": 15,
      "dependsOn": [
        "story-6"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-16",
      "title": "Phase 5b — Worker backtest-handler：传递 configSnapshot.category + 标记 params fallback deprecated",
      "description": "**实测**: backtest-handler.ts:45 `payload.configSnapshot?.params ?? payload.params ?? {}` 保留顶层 params fallback，且 configSnapshot.category/subcategory 完全未传给 Python。Python backtest.py 不知道策略分类。\n\n**任务**:\n1. backtest-handler.ts 构造 PythonBridge 请求时新增 config.category=configSnapshot.category/config.subcategory=configSnapshot.subcategory,config.snapshotParams=configSnapshot.params（与现有 config.strategyParams 双写，Python 本切片仍读 strategyParams）。\n2. payload.params fallback 改为：先读 configSnapshot.params→有则用;无则读 payload.params→用但 emit WARN 'deprecated: payload.params will be removed, use configSnapshot.params'（不抛异常,不阻塞）。\n3. 添加 handler unit test:断言 config.snapshotParams/category/subcategory 出现在 PythonBridge 请求中。\n\n不改 Python backtest.py 读参逻辑（双写保证 Python 用旧字段 strategyParams 仍能跑）。",
      "acceptanceCriteria": [
        "backtest-handler 构造请求:config.category=configSnapshot.category,config.subcategory=configSnapshot.subcategory,config.snapshotParams=configSnapshot.params（除旧 strategyParams 外新增三个字段）",
        "configSnapshot.params 存在时只用 configSnapshot.params（不读 payload.params）",
        "configSnapshot 缺失时读 payload.params 但日志 WARN 'deprecated'，不崩溃",
        "unit test:mock payload.configSnapshot.params={period:20}→bridge 请求 config.snapshotParams={period:20}、config.category='non_factor'",
        "失败路径:configSnapshot=undefined,payload.params={period:10}→请求含 snapshotParams={period:10}+日志 WARN",
        "cd apps/worker && npx tsc --noEmit 无错误；pnpm --filter @quant/worker test 通过；pnpm build 通过"
      ],
      "agent": "worker-agent",
      "priority": 16,
      "dependsOn": [
        "story-15"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-17",
      "title": "Phase 5c — Worker diagnostics-handler fail-closed（移除 echo fallback）",
      "description": "**实测**: diagnostics-handler.ts:51:58 在 Python 返回 !ok 或 !data 时 echo 输入（{rawPayload,note:'...not implemented, echoing input'}）→任务 completed（伪装成功）。Python factor/non_factor 诊断已全部真实算法（非 stub），echo 路径不再合理。\n\n**任务**:\n1. diagnostics-handler.ts:移除 echo fallback。streamCall/call 返回 {ok:false}→throw Error(UNCALLABLE_COMMAND 或 error.message);无 data→throw Error('empty diagnostics result')。\n2. handler 自己不 try-catch（由 main.ts processTask 的 catch 统一处理→调用 /fail 标记任务 failed）。\n3. 添加 handler unit test:mock bridge 返回 {ok:false}→handler throw;mock ok:true but data undefined→handler throw。\n\n不改 Python diagnostics 代码（它已经产出真实 data）。",
      "acceptanceCriteria": [
        "diagnostics handler PythonBridge 返回 {ok:false,error:{message:'boom'}}→throw Error('boom'),任务被 main.ts catch 标记 failed",
        "返回 {ok:true,data:undefined}→throw Error('empty diagnostics result'),任务 failed",
        "返回 {ok:true,data:{type:'factor_based',ic_series:[...]}}→正常返回 diagnostics data,任务 completed",
        "handler 源码不再出现 'not implemented' / 'echoing input' 字符串",
        "unit test 覆盖 fail-closed 两条路径（!ok / no data）",
        "cd apps/worker && npx tsc --noEmit 无错误；pnpm --filter @quant/worker test 通过"
      ],
      "agent": "worker-agent",
      "priority": 17,
      "dependsOn": [
        "story-15"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-18",
      "title": "端到端验收 — canonical taxonomy + configSnapshot + preview + task/worker 全链路一致",
      "description": "启动 api+worker，跑通策略列表/配置读写/预览/诊断任务四条流程，验证 story-1~17 的契约改造全链路一致且无回归。仅验证，不改代码。若发现问题按 prd-generation.md Fault Investigation Order 逐层定位。\n\n**关键验证点**：\n- Taxonomy：Python/API/前端 canonical 10 值锁步一致，无旧值泄漏\n- Config：GET defaults/PUT 保存/hash 冲突/旧 body 兼容\n- Preview：chart_relevant 校验 422 + saved config 合并\n- Task：configSnapshot 强制校验/拒绝顶层 params\n- Worker：backtest handler 传 category/snapshotParams；diagnostics fail-closed\n- 前端：策略总览页/配置面板/K 线图不崩，标签中文无乱码",
      "acceptanceCriteria": [
        "Tier1 Taxonomy:grep 全代码库 nonlinear_ml/high_frequency/mean_reversion/tail_risk_hedging 仅出现在 docs/plan（代码/tests 中无踪）；Python+API+Web 三 union 逐字 canonical 10",
        "Tier1 Config:GET /config(无保存)→200+persisted=false+defaults;PUT 旧 body {config,hash}→201;PUT 新 body {category,subcategory,params}→201;hash 冲突→409;不存在的策略→404",
        "Tier1 Preview:POST /preview chart_relevant=false→422;preview_params 覆盖 saved config;bars 实际返回数据非空",
        "Tier1 Task:POST /api/tasks backtest 缺 configSnapshot→400;含 configSnapshot→accepted;顶层 params→rejected",
        "Tier1 Worker:回测 handler 请求含 snapshotParams/category;诊断 handler Python 失败→任务 failed（非 echo success）",
        "Tier2 渲染:StrategyPage canonical 子分类分组展示 label 中文无乱码;KlineChart 预览 bars 渲染不崩;ConfigPanel 保存后 config 可回读;浏览器 console 无 JS/TS 异常",
        "失败路径:无 configSnapshot 提交→400(非 500/白屏);Worker Python 崩溃→failed(非 completed+假数据);DB 无配置→GET 200+defaults(非 404/崩)",
        "pnpm lint && pnpm test && pnpm build 全通过；cd packages/strategy-runtime && python -m pytest -v 通过；cd packages/strategies && python -m pytest -v 通过；若存在则 bash scripts/smoke-test.sh 通过"
      ],
      "agent": "fullstack-agent",
      "priority": 18,
      "dependsOn": [
        "story-1",
        "story-2",
        "story-3",
        "story-4",
        "story-5",
        "story-6",
        "story-7",
        "story-8",
        "story-9",
        "story-10",
        "story-11",
        "story-12",
        "story-13",
        "story-14",
        "story-15",
        "story-16",
        "story-17"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    }
  ]
}
```
