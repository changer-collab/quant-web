# QuantForge 自治 Agent 指令

你是一个自治编码 Agent，正在 quant-web 项目中工作。

## 项目规则（必须遵守）

你正在操作的项目有严格的 Agent 角色边界。请先读取以下文件了解规则：

- `AGENTS.md` — 项目全局规则、角色定义、能力边界、协作接口
- `apps/*/AGENT.md` 或 `packages/*/AGENT.md` — 对应子项目的规则

**核心约束：**

- 每个 Agent 只在自己的工作范围内操作，不越界
- 先调研现有代码和目录，再提出方案或修改代码
- **优先新增文件或函数，修改已有代码前必须说明理由**——能加就不改，改就要有明确理由
- 不修改无关文件
- 不做真实下单、券商连接、实盘交易
- 所有回复使用中文
- 遵循 KISS 原则，非必要不要过度设计
- **你运行在非交互批处理模式（`--print`），没有人会回答你的问题。不要问"是否批准"、"是否允许"、"要不要继续"——读完 prd.json 后直接执行实现，不等待回复。不要调用要求用户交互的 skill（如 brainstorming/planning），直接写代码。**

## 跨迭代错误反馈

开始工作前，检查以下文件以了解上一轮的失败：

1. **`scripts/ralph/.last-error.json`** — 如果存在，说明上一轮有错误。先读取它，分析失败原因：
   - `detectedFailures` 告诉你了哪类检查失败（test_fail / lint_error / build_fail 等）
   - `summary` 是错误摘要
   - 不要重复同样的错误

## 你的任务

1. 读取 `scripts/ralph/.last-error.json`（如果存在）— 理解上一轮为什么失败
2. 读取 `scripts/ralph/prd.json` — 你的任务清单
3. 读取 `scripts/ralph/progress.txt` — 历史记忆和已发现的模式（先看 Codebase Patterns 部分）
4. 找到 `passes: false` 的**最高优先级**故事
5. 确认你在正确的分支上（参考 prd.json 的 `branchName`）
6. **只实现这一个故事**
7. 跑质量检查
8. 执行 Doc Sync Gate：判断并更新相关 `docs/plans/*`、`docs/superpowers/specs/*`、`docs/development/*`、`.skills/ralph-harness/*`；如果不需要更新 docs，在 progress.txt 记录原因
9. 提交代码
10. 更新 prd.json（将完成的故事设为 `passes: true`）
11. 追加进度到 progress.txt

## 💡 核心规则：测试文件不可修改

**测试文件（`test_*.py` / `*.test.ts` / `*.spec.ts`）是正确行为的真相来源。**

- 测试失败 → 修复实现代码，**绝不修改测试来迎合错误的实现**
- 如果无法修复实现（超出 scope 或能力范围），保持测试红色
- 通过修改测试的断言值/跳过条件/判等逻辑来让测试通过，等同于制造假绿色

⚠️ **提交前自检**：如果本次修改的文件全是测试文件而没有对应的实现文件，这是一个危险信号。停下来检查。

⚠️ **关于修改测试的人为陷阱**：如果你发现自己同时修改了测试文件和实现文件，停下来问自己——这个测试真的是错的，还是我在找捷径让当前代码通过？如果测试预期值是8，实现返回10，这是测试错了还是实现错了？**正确答案：先修实现，测试不动。** 新的需求（预期值应该是10而不是8）属于新 story 的 scope，不要混在修复 story 里。

## 质量检查命令

完成故事后，必须运行以下检查（根据故事涉及的语言选择）：

### JavaScript/TypeScript 故事

```bash
pnpm lint
pnpm test
pnpm build
```

### Python 故事

```bash
cd packages/<相关包> && python -m pytest -v
```

### 涉及多个语言

```bash
pnpm lint && pnpm test && pnpm build
for pkg in strategy-runtime backtest-engine strategies data-client factor-lab ai-engine obsidian-sync; do
  (cd packages/$pkg && python -m pytest -v)
done
```

## 分支管理

- 检查 prd.json 中的 `branchName`
- 如果当前分支不是目标分支，切过去
- 每个故事完成后提交到当前分支
- 推送时用 `git push origin <branchName>`

## 提交规范

```
feat: [Story ID] - [Story Title]
```

## 进度日志格式

追加到 `scripts/ralph/progress.txt`（永远只追加，不替换）：

```
## [Date/Time] - [Story ID]
- 实现了什么
- 修改了哪些文件
- **后续迭代注意事项：**
  - 发现的模式（如 "这个项目用 X 做 Y"）
  - 遇到的坑（如 "改 W 的时候别忘了更新 Z"）
  - 有用的上下文（如 "评估面板在组件 X 里"）
---
```

## 代码模式汇总

如果你发现了**可复用的模式**，更新 `progress.txt` 顶部的 `## Codebase Patterns` 区域：

```
## Codebase Patterns
- 示例：所有 Python 包用 pyproject.toml + editable install
- 示例：前端测试通过 globalThis.fetch mock 脱离后端
- 示例：Worker 通过 PythonBridge 调用 Python CLI
```

## Doc Sync Gate

提交前，必须判断本 story 是否需要同步文档或开发基础设施：

- 改变执行状态、优先级、阻塞点、下一步 → 更新对应 `docs/plans/*` 的 Current Status / Decision Log / Doc Sync Log
- 改变 API payload、结果结构、DB schema、任务状态机、分类枚举、层间职责边界 → 更新对应 `docs/superpowers/specs/*`
- 发现长期开发标准或 Ralph 可复用规则 → 更新 `docs/development/*` 或 `.skills/ralph-harness/*`
- Ralph 执行的 story → 追加 `scripts/ralph/progress.txt`，并同步 `prd.json` passes
- 如果判断不需要更新 docs，也要在 `progress.txt` 中写明：`Doc Sync: no docs update needed because ...`

**不要等“之后再补文档”。文档同步是完成条件，不是收尾建议。**

## 更新 AGENTS.md

提交前，检查修改的文件所在目录是否有 AGENTS.md：

- 如果发现了有价值的可复用知识，更新对应的 AGENTS.md
- 不要添加故事特定的临时调试信息
- 不要重复 progress.txt 中已有的信息

## 前端故事验证

如果故事涉及 UI 修改：

1. 启动开发服务器：`pnpm --filter @quant/web dev`
2. 在浏览器中验证页面功能正常
3. 截图记录到 progress.txt

## 停止条件

完成一个故事后，检查 prd.json 中是否还有 `passes: false` 的故事：

- 如果**全部完成**（所有故事都是 `passes: true`），输出：`<promise>COMPLETE</promise>`
- 如果还有未完成的故事，正常结束（下一轮迭代会接手）

## 重要

- **每次迭代只做一个且仅一个故事。这是硬规则，不是建议。** 即使在同一次调用中你有能力多做，也必须停下来。原因：你是 `--print` 非交互模式，对话上下文会在长输出后被压缩(compact)，后面的故事会丢失关键代码细节导致质量下降。做完一个就提交退出，让下一轮 iteration 用干净上下文接下一个 story。
- 完成后 **5 分钟内提交并退出**，不要在非交互模式下无限扩展工作范围。
- 频繁提交
- 保持 CI 绿色
- 开始前先读 progress.txt 的 Codebase Patterns 部分


---

## 上一轮运行状态（由 Ralph Core 自动注入）

- 迭代次数: 11
- Feature: （未设置）
- 连续无进展轮数: 0
- 上一轮有错误，请先分析失败原因再行动。

```json
{
  "iteration": 10,
  "exitCode": 0,
  "timestamp": "2026-07-03T02:39:31.394Z",
  "detectedFailures": [
    "lint_error",
    "build_fail"
  ],
  "summary": "3. Current state of the target filesLet me read the current state of the three files and check what tsc errors exist:Let me check the current BarData type and how the API returns bar data:Now I have a clear picture. Let me implement **story-6c** 鈥?the highest priority non-passing story. The previous iteration had pre-existing tsc errors from `strategy-grid.tsx` (deprecated file with deleted `ResearchModeId` type). I need to: - Quality: tsc OK, 93/93 tests, build 3.21s, lint 0 errors - Quality: t"
}
```

## 任务清单（含运行时状态）

```json
{
  "feature": "backend-sync-realign-phase6-9",
  "description": "整合主线切片 3（14 story，Phase 2 延后项 + Phase 6-9 收尾，每个 story 单文件/单关注点）。Phase 2 延后：API param wire → camelCase → Phase 6.3 transitional 诊断算法 → Phase 7a Worker 去 fallback → Phase 7a Python 对齐 snapshotParams → Phase 7b report metadata → Phase 8a 前端类型+客户端 → Phase 8b ConfigPanel → Phase 8c WorkspacePage → Phase 8d KlineChart+Grid+Table → Phase 9a CLI listStrategies → Phase 9b API strategy-sync PythonBridge → Phase 9c Worker 死队列删除 → E2E 验收。",
  "branchName": "ralph/backend-sync-realign-phase6-9",
  "userStories": [
    {
      "id": "story-1",
      "title": "Phase 2 延后 — API strategy route mapMeta 输出 camelCase param wire（含 deprecated 双字段）",
      "description": "**单文件关注点**: apps/api/src/routes/strategy.ts 的 mapMeta() 函数。\n\n**当前**: mapMeta(:37-48) 直接透传 Python 的 snake_case 字段名到 HTTP response——`key/min/max/chart_relevant/ui_constraints`，UIConstraint 子字段也是 `target_field/target_value/action_value`。\n\n**目标**: Plan §1.2 contract——`name`(非 key)、`range:[number,number]`(替代 min/max)、`chartRelevant`/`uiConstraints`/`targetField`/`targetValue`/`actionValue` 全 camelCase。\n\n**任务**:\n1. 只改 strategy.ts 的 mapMeta() 函数（~30 行），不改 strategy-sync.ts 的 Python 端。\n2. 每个 param 输出：`name`(新主字段，取 p.key 值) + `range:[p.min??0, p.max??0]`(替代 min/max 平铺) + `chartRelevant`(取 chart_relevant) + `uiConstraints`(子字段改名)。\n3. 短期双字段兼容：同时保留 `key/min/max/chart_relevant/ui_constraints` deprecated 字段（与 name 同值），旧前端不崩溃。\n4. 更新 strategy.test.ts：断言每个 param 含 `name`/`range`/`chartRelevant`/`uiConstraints` camelCase + deprecated `key` 存在。",
      "acceptanceCriteria": [
        "GET /api/strategies 每个 param 含 `name`(非空 string) + deprecated `key`(与 name 同值)",
        "param 含 `range:[number,number]`，min=max=0 时 range=[0,0]",
        "param 顶级 camelCase: `chartRelevant`、`uiConstraints`",
        "每个 UIConstraint: `targetField`(非 target_field)、`targetValue`(非 target_value)、`actionValue`(非 action_value)",
        "DEPRECATED 字段仍存在: `key`/`min`/`max`/`chart_relevant`/`ui_constraints`（短期过渡）",
        "GET /api/strategies/:name 与列表项 shape 一致",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test -- tests/routes/strategy.test.ts 通过"
      ],
      "agent": "api-agent",
      "priority": 1,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-2",
      "title": "Phase 6.3 — transitional/event_sentiment_factor 诊断真实算法（替换 stub）",
      "description": "**单文件关注点**: packages/strategy-runtime/quantforge_strategy/commands/diagnostics/transitional.py。\n\n**当前**: transitional.py:29-43 返回硬编码全零 stub（sentiment_curve 不存在，返回 factor 风格的 ic_series/layered_returns 全零）。\n\n**任务**:\n1. 实现 sentiment event 加载：从 configSnapshot.params.dataSource 加载事件数据，缺省用价格波动率代理 sentiment score。\n2. 实现 exponential decay scoring：half_life 从 configSnapshot.params.sentiment_decay_half_life（默认 5 天）。\n3. 实现 outlier checks：z-score >3σ 标记为 outlier。\n4. 实现 target factor mapping：sentiment score 与 target_factor_pool 各因子的 Spearman 相关性。\n5. 输出 type='transitional' 结果：sentiment_curve([{ts,score}])、mapping_metrics({factor_id:correlation})、outlier_count、validation_passed(bool)。\n6. 无 data_source 且无价格→emit warning+返回空结果（非崩溃）。\n7. 创建/更新 test_diagnostics_transitional.py。",
      "acceptanceCriteria": [
        "transitional.py 不再返回硬编码全零 stub；sentiment_curve 为 [{ts,score}] 数组",
        "mapping_metrics 返回 {string:number}，key 为 target_factor_pool 因子 ID",
        "decay_half_life 参数生效：half_life=3 快于 half_life=10",
        "无数据→返回结构化空结果（type='transitional'字段存在，数组为空），不抛异常",
        "test_diagnostics_transitional.py: 正常流程 + 缺数据 + decay 参数 + outlier 检测各 1 条",
        "cd packages/strategy-runtime && python -m pytest tests/test_diagnostics_transitional.py -v 通过",
        "cd packages/strategy-runtime && python -m pytest -v 无回归"
      ],
      "agent": "strategy-runtime-agent",
      "priority": 2,
      "dependsOn": [],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-3a",
      "title": "Phase 7a Worker — backtest-handler 移除 payload.params fallback，单一 snapshotParams 字段",
      "description": "**单文件关注点**: apps/worker/src/handlers/backtest-handler.ts。\n\n**当前**: 52-54 行保留 `payload.params ?? {}` fallback + WARN deprecated 日志。68 行发双写 `snapshotParams`+`strategyParams`。\n\n**任务**:\n1. 删除 payload.params fallback 分支：configSnapshot 缺失时 throw Error('configSnapshot required for backtest')。\n2. 移除请求中 legacy `strategyParams` 字段，只发 `config.snapshotParams`。\n3. 更新 backtest-handler.test.ts: 新增 configSnapshot 缺失→throw 测试 + snapshotParams 单字段测试。\n\n只改 handler + 测试，不改 Python。",
      "acceptanceCriteria": [
        "grep 'payload\\.params' apps/worker/src/handlers/backtest-handler.ts 无匹配（已删除）",
        "grep 'strategyParams' apps/worker/src/handlers/backtest-handler.ts 无匹配（已删除）",
        "configSnapshot 缺失→handler throw Error('configSnapshot required for backtest')→main.ts catch→标记 failed",
        "configSnapshot.params={period:20}→bridge 请求 config.snapshotParams={period:20}（单一字段）",
        "unit test: 无 configSnapshot→throw; 有 configSnapshot.params→bridge 请求含 snapshotParams",
        "cd apps/worker && npx tsc --noEmit 无错误；pnpm --filter @quant/worker test -- tests/backtest-handler.test.ts 通过"
      ],
      "agent": "worker-agent",
      "priority": 3,
      "dependsOn": [],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-3b",
      "title": "Phase 7a Python — backtest.py 优先读 snapshotParams，降级 strategyParams 兼容",
      "description": "**单文件关注点**: packages/strategy-runtime/quantforge_strategy/commands/backtest.py。\n\n**当前**: _build_strategy(:67) 只读 `config.get('strategyParams')`，不知 snapshotParams 存在。\n\n**任务**:\n1. _build_strategy 优先读 `config.get('snapshotParams')`，降级 `config.get('strategyParams')`（兼容过渡期 Worker 双写）。\n2. _run_single 日志输出 `config.get('category','unknown')`。\n3. 无任何 params→策略 cls() 默认构造（不抛 KeyError）。\n4. 更新 test_backtest_command_market_rules.py: snapshotParams 优先 + strategyParams 降级 + 无参默认构造。",
      "acceptanceCriteria": [
        "config.snapshotParams={period:5}→策略构造用 period=5（优先读）",
        "config 无 snapshotParams 但有 strategyParams={period:10}→降级读 strategyParams",
        "config 无 snapshotParams 也无 strategyParams→策略 cls() 无参实例化（不崩溃）",
        "_run_single 日志含 category 信息",
        "Python test: 三条路径均有覆盖",
        "cd packages/strategy-runtime && python -m pytest tests/test_backtest_command_market_rules.py -v 通过"
      ],
      "agent": "strategy-runtime-agent",
      "priority": 4,
      "dependsOn": [],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-4",
      "title": "Phase 7b — report mapper + BacktestResultProcessor 传递 category/subcategory/configHash",
      "description": "**单模块关注点**: apps/api/src/services/report-mapper.ts + backtest-result-processor.ts（同 services 目录，~60 行改动）。\n\n**当前**: report-mapper.ts metadata 参数无 category/subcategory/hash。BacktestResultProcessor 不提取 configSnapshot 元数据。\n\n**任务**:\n1. report-mapper.ts mapBacktestResultToReport metadata 参数加 category?/subcategory?/configHash?（可选，不影响旧调用）。\n2. BacktestReportFull 类型加 strategyCategory?/strategySubcategory?/configHash?（API types.ts 同步）。\n3. BacktestResultProcessor 从 `ctx.task.payload.configSnapshot` 提取 category/subcategory/hash 传入 mapper。\n4. 添加 mapper unit test: 传 category='factor_based'+subcategory='linear_multi_factor'→report 含对应字段；不传→字段 undefined。\n\n仅改 report 层，不改 Worker/Python/前端。",
      "acceptanceCriteria": [
        "mapBacktestResultToReport metadata 参数可接收 category?/subcategory?/configHash?",
        "BacktestReportFull 含 strategyCategory?/strategySubcategory?/configHash? 可选字段",
        "BacktestResultProcessor.process 传入 category/subcategory/hash 到 mapper→report 含对应值",
        "mapper test: 传入→字段有值；不传→undefined（不破坏旧 report 读取）",
        "report save 不因新增可选字段失败",
        "cd apps/api && npx tsc --noEmit 无错误；pnpm --filter @quant/api test 通过"
      ],
      "agent": "api-agent",
      "priority": 5,
      "dependsOn": [
        "story-3a",
        "story-3b"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-5a",
      "title": "Phase 8a 类型层 — 前端 types.ts + appData.ts 参数类型迁移到 camelCase target contract",
      "description": "**纯类型声明层**: apps/web/src/data/types.ts + appData.ts（2 个文件，不改任何 API client 或组件）。\n\n**types.ts 改动**:\n1. StrategyParam: 新增 `name` 字段（主字段，取原 key 值）+ `range?:[number,number]`（替代 min/max）。保留 `key`/`min`/`max` 为 deprecated 可选字段（组件继续用旧字段可编译，story-6a/b/c 逐步迁移）。\n2. UIConstraint: `target_field`→`targetField`、`target_value`→`targetValue`、`action_value`→`actionValue`。\n3. StrategyRow: params 字段类型改为 `StrategyParamDefWire[]`（挂载 story-1 API 的 camelCase wire）。\n4. 确认 ConfigSnapshot/DiagnosticResult/BacktestReportFull 已对齐（story-6/7/8 产物）。\n\n**appData.ts 改动**:\n5. 重新导出新类型：确保 UIConstraint/StrategyParam 的新字段名可通过 appData 访问（组件通常 import from appData）。\n\n不新增 API client 改动，不碰任何 .tsx 组件。deprecated 字段确保组件短期间不爆编译。",
      "acceptanceCriteria": [
        "types.ts StrategyParam 含 `name`(string,required) + deprecated `key`(string,optional) + `range?:[number,number]`",
        "types.ts UIConstraint 字段全 camelCase: targetField/targetValue/actionValue（grep 'target_field|target_value|action_value' types.ts 无匹配）",
        "types.ts StrategyCategory/StrategySubcategory 已为 canonical 10 值（story-3 完成，确认无退化）",
        "appData.ts 导出 UIConstraint/StrategyParam/StrategyRow/ConfigSnapshot/DiagnosticResult 新类型",
        "cd apps/web && npx tsc --noEmit 无错误（组件侧仍用 p.key/p.min → deprecated 字段不爆编译）",
        "pnpm --filter @quant/web test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 6,
      "dependsOn": [
        "story-1",
        "story-4"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-5b",
      "title": "Phase 8a 客户端层 — 4 API client 签名对齐 API 新契约",
      "description": "**纯 API client 签名层**: apps/web/src/api/ 下 4 个文件（strategies-config.ts + tasks.ts + diagnostics.ts + preview.ts），不改类型文件。\n\n**改动**:\n1. strategies-config.ts: ApiConfigResponse→PersistedConfigResponse（`{persisted:boolean,configSnapshot:ConfigSnapshot}`，不用 config_json/hash/updated_at）。saveStrategyConfig 签名改为 `(name:string,category:StrategyCategory,subcategory:StrategySubcategory,params:Record<string,unknown>,expectedHash?:string)`，body 为 `{category,subcategory,params,expectedHash}`。\n2. tasks.ts: submitBacktest payload 移除顶层 `params` 字段，只传 `{strategy,symbol,timeframe,configSnapshot,...}`。submitDiagnostics 同理。\n3. diagnostics.ts: fetchDiagnostic 返回类型对齐 DiagnosticResultWire（resultId/resultType/category/subcategory/configSnapshot/data/expiresAt/engineVersion）。\n4. preview.ts: 确认 fetchPreview 签名与 contract 一致（preview_params 字段名、bars 返回 shape）。\n\n不改类型文件和组件文件。API client 函数返回类型依赖 story-5a 的新类型。",
      "acceptanceCriteria": [
        "strategies-config.ts: fetchStrategyConfig 返回 PersistedConfigResponse（persisted+configSnapshot），不用 config_json/hash/updated_at",
        "strategies-config.ts: saveStrategyConfig 签名 (name,category,subcategory,params,expectedHash?)，body={category,subcategory,params,expectedHash}",
        "tasks.ts: submitBacktest payload 不含顶层 params（grep 'params:' submitBacktest 调用无 params 字段）",
        "diagnostics.ts: fetchDiagnostic 返回类型含 resultId/resultType/category/subcategory/configSnapshot/data/expiresAt/engineVersion",
        "preview.ts: fetchPreview 签名不变（当前已符合 contract），无需实际改动",
        "失败路径: 所有 API client catch→返回结构化错误/null（调用方可判空，不抛未捕获异常）",
        "cd apps/web && npx tsc --noEmit 无错误；pnpm --filter @quant/web test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 7,
      "dependsOn": [
        "story-5a"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6a",
      "title": "Phase 8b — ConfigPanel 组件对齐新 API wire",
      "description": "**单组件关注点**: apps/web/src/components/config-panel.tsx。\n\n**当前状态**: 使用 `p.key` 作参数字段名、`p.min/p.max` 作范围、`target_field/target_value` 读 uiConstraints、saveStrategyConfig 用旧签名 (name,config,hash)、子分类列表缺少 index_enhancement/event_sentiment_factor。\n\n**任务**:\n1. `p.key`→`p.name`，`param.key`→`param.name`（~20 处）。handleParamChange 用 name 索引 paramValues。\n2. uiConstraints 字段迁移到 camelCase: `c.target_field`→`c.targetField`、`c.target_value`→`c.targetValue`。\n3. handleSave 调 saveStrategyConfig(name, category, subcategory, params, expectedHash)，成功后 `setConfigHash(snapshot.hash)`。\n4. CATEGORY_SUBCATEGORIES 补齐: index_enhancement∈factor_based、event_sentiment_factor∈transitional。\n5. 失败路径：保存失败→显示错误 toast（不静默失败）。\n\n只改 config-panel.tsx，不改其他组件。",
      "acceptanceCriteria": [
        "grep 'p\\.key\\b' apps/web/src/components/config-panel.tsx 无匹配；全部为 p.name",
        "grep 'target_field\\|target_value\\|action_value' apps/web/src/components/config-panel.tsx 无匹配；全部为 targetField/targetValue/actionValue",
        "handleSave 调 saveStrategyConfig(name,category,subcategory,params,expectedHash)；保存后 configHash 更新",
        "CATEGORY_SUBCATEGORIES 含 index_enhancement∈factor_based、event_sentiment_factor∈transitional",
        "失败路径: saveStrategyConfig 异常→显示错误提示（非静默失败/白屏）",
        "cd apps/web && npx tsc --noEmit 无错误；pnpm --filter @quant/web test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 8,
      "dependsOn": [
        "story-5b"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6b",
      "title": "Phase 8c — WorkspacePage + workspace.tsx 对齐新 API wire",
      "description": "**双文件关注点**: apps/web/src/components/workspace-page.tsx + workspace.tsx（同 workspace 域，~8 处 param.key 引用 + 加载/提交流程）。\n\n**当前状态**:\n- workspace-page.tsx: fetchStrategyConfig 返回旧 `ApiConfigResponse|null`、submitBacktest 传顶层 params、diagnostics 渲染用旧字段。\n- workspace.tsx: `param.key` 读 backtestConfig.params。\n\n**任务**:\n1. workspace-page.tsx mount: loadConfig 改调 fetchStrategyConfig→取 `configSnapshot`（非 config_json）。\n2. workspace-page.tsx submitBacktest: payload={strategy,symbol,timeframe,configSnapshot}，移除顶层 params。\n3. workspace-page.tsx diagnostics: 渲染真实 API DiagnosticResultWire data（ic_series/layered_returns/summary），无数据→显示空态。\n4. workspace.tsx: `param.key`→`param.name`（~4 处）。\n\n只改这两个文件。",
      "acceptanceCriteria": [
        "grep 'param\\.key' apps/web/src/components/workspace.tsx 无匹配；全部为 param.name",
        "WorkspacePage mount: fetchStrategyConfig→{persisted,configSnapshot}→setConfigSnapshot(configSnapshot)",
        "WorkspacePage submitBacktest payload={strategy,symbol,timeframe,configSnapshot}，grep 'params:' payload 构造无顶层 params",
        "WorkspacePage diagnostics 渲染: 有 data→渲染 ic_series/layered_returns/summary；无 data→显示空态'暂无诊断数据'（非白屏）",
        "失败路径: configSnapshot 加载失败→显示'暂无配置'（非崩溃/白屏）",
        "cd apps/web && npx tsc --noEmit 无错误；pnpm --filter @quant/web test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 9,
      "dependsOn": [
        "story-6a"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6c",
      "title": "Phase 8d — KlineChart + StrategyTable + StrategyGrid 对齐（preview adapter + param chip 迁移）",
      "description": "**三文件机械改动**: kline-chart.tsx + strategy-table.tsx + strategy-grid.tsx（~5 处 param key 引用 + preview bar adapter）。\n\n**任务**:\n1. strategy-table.tsx:55 `p.key`→`p.name`（1 处 React key + 1 处显示）。\n2. strategy-grid.tsx:68 `p.key`→`p.name`（1 处 React key + 1 处显示）。\n3. kline-chart.tsx: preview bars adapter——若 API 返回旧 shape `{time,open,close,high,low,volume}` 先转为 `{ts,o,h,l,c,v}` 目标 shape 再渲染；若已是目标 shape 直通。\n4. kline-chart.tsx: 副图路由已迁移 canonical 值（story-5 完成），确认无旧 snake_case 子分类引用残留。\n5. 未知子分类→默认副图（不崩溃）。\n\n三个文件全为机械改动，不加新逻辑（kline-chart adapter 除外）。",
      "acceptanceCriteria": [
        "grep 'p\\.key' apps/web/src/components/strategy-table.tsx 无匹配",
        "grep 'p\\.key' apps/web/src/components/strategy-grid.tsx 无匹配",
        "KlineChart: 旧 bar {time,open,close,high,low,volume}→映射为 {ts,o,h,l,c,v} 正常渲染",
        "KlineChart: 新 bar {ts,o,h,l,c,v} 直通渲染",
        "KlineChart: 未知 canonical 子分类→默认副图（不崩溃/不白屏）",
        "cd apps/web && npx tsc --noEmit 无错误；pnpm --filter @quant/web test 通过；pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 10,
      "dependsOn": [
        "story-6b"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-7a",
      "title": "Phase 9a — Python CLI 注册 listStrategies NDJSON 命令",
      "description": "**单文件关注点**: packages/strategy-runtime/quantforge_strategy/cli.py（+ 新 test 文件）。\n\n**任务**:\n1. cli.py _COMMANDS 注册 `listStrategies`→输出 NDJSON 策略元数据列表。\n2. NDJSON 字段用 camelCase（对齐 API wire）：name/category/subcategory/version/description/workflowReady/backtestable。params 输出 camelCase shape（与 story-1 的 mapMeta 输出一致）。\n3. 新建 test_cli_list_strategies.py: 断言输出至少 1 条策略、字段齐全。\n\n不改现有 backtest/diagnostics command 逻辑。",
      "acceptanceCriteria": [
        "CLI --command listStrategies 输出 NDJSON，每行含 name/category/subcategory/version/description/workflowReady/backtestable（camelCase）",
        "params 子数组每项含 name/range/chartRelevant/uiConstraints（与 API mapMeta 输出一致）",
        "test_cli_list_strategies.py: 输出非空、字段名 camelCase、策略数≥1",
        "cd packages/strategy-runtime && python -m pytest tests/test_cli_list_strategies.py -v 通过",
        "cd packages/strategy-runtime && python -m pytest -v 无回归"
      ],
      "agent": "strategy-runtime-agent",
      "priority": 11,
      "dependsOn": [],
      "passes": false,
      "_runtime": {
        "attempts": 1,
        "lastError": null
      }
    },
    {
      "id": "story-7b",
      "title": "Phase 9b — API strategy-sync.ts 用 PythonBridge 替换内联 Python script string",
      "description": "**单文件关注点**: apps/api/src/services/strategy-sync.ts。\n\n**当前**: 55-100 行内联 Python heredoc script string→写 tempfile→exec 获取策略列表。\n\n**任务**:\n1. 用 PythonBridge.call({command:'listStrategies'}) → 解析 NDJSON → 返回 PythonStrategyMeta[]。\n2. 删除内联 script string 和 tempfile 逻辑。\n3. mapMeta 已在 story-1 改为输出 camelCase（CLI 直接输出 camelCase 后 mapMeta 可简化或变为 pass-through）。\n4. PythonBridge 不可用→返回 [] + console.warn（不抛异常，API 启动不崩溃）。\n5. 更新 strategy-sync test。\n\n只改这一个文件。",
      "acceptanceCriteria": [
        "grep '\"\"\"' apps/api/src/services/strategy-sync.ts 无匹配（或非策略元数据用途）",
        "syncFromPython 使用 PythonBridge.call({command:'listStrategies'}) 解析 NDJSON 返回策略列表",
        "PythonBridge 不可用→返回 [] + console.warn（不抛异常→API 启动不崩溃）",
        "GET /api/strategies 返回 shape 与 story-1 一致（字段名不变，底层来源从 inline script 改为 CLI）",
        "cd apps/api && pnpm --filter @quant/api test 通过（strategy.test.ts 无回归）"
      ],
      "agent": "api-agent",
      "priority": 12,
      "dependsOn": [
        "story-7a",
        "story-6c"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-7c",
      "title": "Phase 9c — 删除 Worker 死队列 queue.ts + 类型迁移",
      "description": "**单模块关注点**: apps/worker/src/queue.ts 删除 + 类型迁移。\n\n**当前**: queue.ts（7834 bytes）定义 TaskRecord/TaskHandler/TaskEventHandler + better-sqlite3 本地队列实现。Worker main.ts 实际通过 HTTP poll `/api/internal/tasks/pending`。\n\n**任务**:\n1. 确认 TaskRecord/TaskHandler/TaskEventHandler 的所有引用方（rg 搜索）。\n2. 将这些类型定义移到 apps/worker/src/types.ts（当前已有 TaskType/TaskStatus/StreamEvent 在该文件）。\n3. 更新所有 import from './queue.js'→import from './types.js'。\n4. 删除 queue.ts。\n5. rg 'better-sqlite3' apps/worker/package.json: 若仅 queue.ts 使用则移除 dependency；若其他文件也用（如测试 fixture）则保留。\n6. 运行 worker tests 确认无回归。",
      "acceptanceCriteria": [
        "apps/worker/src/queue.ts 已删除（文件不存在）",
        "TaskRecord/TaskHandler/TaskEventHandler 类型定义在 types.ts 中（grep 验证）",
        "rg 'from.*\\./queue\\.js' apps/worker/src 无匹配（所有 import 已迁移到 types.ts）",
        "Worker 编译通过：cd apps/worker && npx tsc --noEmit 无错误",
        "rg 'better-sqlite3' apps/worker/package.json → 无匹配（仅当 queue.ts 独占时移除）",
        "cd apps/worker && pnpm --filter @quant/worker test 通过（功能不退化）"
      ],
      "agent": "worker-agent",
      "priority": 13,
      "dependsOn": [
        "story-6c"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-8",
      "title": "端到端验收 — 全链路 4 轨道验证（不改代码）",
      "description": "启动 api+worker+web，按 4 条轨道逐项验收。仅验证，不改代码。发现问题按 Fault Investigation Order 逐层定位（API response→类型定义→前端请求→组件渲染）。\n\n**轨道 A — Param Wire**: API GET /api/strategies camelCase 字段完整性\n**轨道 B — Diagnostics**: transitional 真实算法输出 + fail-closed\n**轨道 C — Backtest E2E**: configSnapshot 必需→去 fallback→report metadata\n**轨道 D — 清理确认**: queue.ts 不存在、strategy-sync 用 PythonBridge\n**Tier 2**: 前端组件渲染无崩、类型锁步一致",
      "acceptanceCriteria": [
        "轨道 A: curl GET /api/strategies → 每个 param 含 name/range/chartRelevant/uiConstraints(targetField/targetValue) camelCase + deprecated key 存在",
        "轨道 B: Python CLI diagnostics transitional → sentiment_curve 非空+mapping_metrics 非空（非硬编码 stub）",
        "轨道 B: POST /api/tasks diagnostics 含 configSnapshot→accepted",
        "轨道 C: POST /api/tasks backtest 缺 configSnapshot→400；Worker 无 configSnapshot→throw→failed",
        "轨道 C: 回测成功→report 含 strategyCategory/strategySubcategory/configHash（非 undefined）",
        "轨道 D: apps/worker/src/queue.ts 不存在；GET /api/strategies 非空且 API 日志无 inline script fallback",
        "Tier2 前端: ConfigPanel 保存用新签名；Workspace 加载 configSnapshot；KlineChart bars 正常渲染",
        "Tier2 全量: grep mean_reversion/tail_risk_hedging 全代码库仅出现在 docs/；target_field/target_value 不在组件中",
        "全量 smoke: pnpm lint && pnpm test && pnpm build 通过；pytest 全通过"
      ],
      "agent": "fullstack-agent",
      "priority": 14,
      "dependsOn": [
        "story-1",
        "story-2",
        "story-3a",
        "story-3b",
        "story-4",
        "story-5a",
        "story-5b",
        "story-6a",
        "story-6b",
        "story-6c",
        "story-7a",
        "story-7b",
        "story-7c"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    }
  ]
}
```
