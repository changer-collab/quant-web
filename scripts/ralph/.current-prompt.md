# QuantForge 自治 Agent 指令

你是一个自治编码 Agent，正在 quant-web 项目中工作。

## 项目规则（必须遵守）

你正在操作的项目有严格的 Agent 角色边界。请先读取以下文件了解规则：

- `AGENTS.md` — 项目全局规则、角色定义、能力边界、协作接口
- `apps/*/AGENT.md` 或 `packages/*/AGENT.md` — 对应子项目的规则

**核心约束：**

- 每个 Agent 只在自己的工作范围内操作，不越界
- 先调研现有代码和目录，再提出方案或修改代码
- 不修改无关文件
- 不做真实下单、券商连接、实盘交易
- 所有回复使用中文
- 遵循 KISS 原则，非必要不要过度设计

## 跨迭代错误反馈

开始工作前，检查以下文件以了解上一轮的失败：

1. **`scripts/ralph/.last-error.json`** — 如果存在，说明上一轮有错误。先读取它，分析失败原因：
   - `detectedFailures` 告诉你了哪类检查失败（test_fail / lint_error / build_fail 等）
   - `summary` 是错误摘要
   - 不要重复同样的错误
2. **`scripts/ralph/.prd.state.json`** — 如果存在，读取 `storyAttempts` 了解哪些故事已经尝试过

## 你的任务

1. 读取 `scripts/ralph/.last-error.json`（如果存在）— 理解上一轮为什么失败
2. 读取 `scripts/ralph/prd.json` — 你的任务清单
3. 读取 `scripts/ralph/progress.txt` — 历史记忆和已发现的模式（先看 Codebase Patterns 部分）
4. 找到 `passes: false` 的**最高优先级**故事
5. 确认你在正确的分支上（参考 prd.json 的 `branchName`）
6. **只实现这一个故事**
7. 跑质量检查
8. 提交代码
9. 更新 prd.json（将完成的故事设为 `passes: true`）
10. 追加进度到 progress.txt

## 质量检查命令

完成故事后，必须运行以下检查（根据故事涉及的语言选择）：

### JavaScript/TypeScript 故事

```bash
pnpm lint
pnpm test
pnpm build
```

### Python 故事

```bash
cd packages/<相关包> && python -m pytest -v
```

### 涉及多个语言

```bash
pnpm lint && pnpm test && pnpm build
for pkg in strategy-runtime backtest-engine strategies data-client factor-lab ai-engine obsidian-sync; do
  (cd packages/$pkg && python -m pytest -v)
done
```

## 分支管理

- 检查 prd.json 中的 `branchName`
- 如果当前分支不是目标分支，切过去
- 每个故事完成后提交到当前分支
- 推送时用 `git push origin <branchName>`

## 提交规范

```
feat: [Story ID] - [Story Title]
```

## 进度日志格式

追加到 `scripts/ralph/progress.txt`（永远只追加，不替换）：

```
## [Date/Time] - [Story ID]
- 实现了什么
- 修改了哪些文件
- **后续迭代注意事项：**
  - 发现的模式（如 "这个项目用 X 做 Y"）
  - 遇到的坑（如 "改 W 的时候别忘了更新 Z"）
  - 有用的上下文（如 "评估面板在组件 X 里"）
---
```

## 代码模式汇总

如果你发现了**可复用的模式**，更新 `progress.txt` 顶部的 `## Codebase Patterns` 区域：

```
## Codebase Patterns
- 示例：所有 Python 包用 pyproject.toml + editable install
- 示例：前端测试通过 globalThis.fetch mock 脱离后端
- 示例：Worker 通过 PythonBridge 调用 Python CLI
```

## 更新 AGENTS.md

提交前，检查修改的文件所在目录是否有 AGENTS.md：

- 如果发现了有价值的可复用知识，更新对应的 AGENTS.md
- 不要添加故事特定的临时调试信息
- 不要重复 progress.txt 中已有的信息

## 前端故事验证

如果故事涉及 UI 修改：

1. 启动开发服务器：`pnpm --filter @quant/web dev`
2. 在浏览器中验证页面功能正常
3. 截图记录到 progress.txt

## 停止条件

完成一个故事后，检查 prd.json 中是否还有 `passes: false` 的故事：

- 如果**全部完成**（所有故事都是 `passes: true`），输出：`<promise>COMPLETE</promise>`
- 如果还有未完成的故事，正常结束（下一轮迭代会接手）

## 重要

- 每次迭代只做**一个故事**
- 频繁提交
- 保持 CI 绿色
- 开始前先读 progress.txt 的 Codebase Patterns 部分


---

## 上一轮运行状态（由 Ralph Core 自动注入）

- 迭代次数: 3
- Feature: real-backtest-report
- 连续无进展轮数: 0
- 上一轮有错误，请先分析失败原因再行动。

```json
{
  "iteration": 2,
  "exitCode": 0,
  "timestamp": "2026-06-25T23:21:24.692Z",
  "detectedFailures": [
    "vitest_fail",
    "lint_error",
    "typescript_error",
    "build_fail"
  ],
  "summary": "﻿鎴戞潵鍒嗘瀽涓婁竴杞殑閿欒骞跺紑濮嬪伐浣溿€傞鍏堣鍙栧繀瑕佺殑鏂囦欢銆傛垜鏉ュ垎鏋愬綋鍓嶇姸鎬併€傛牴鎹?prd.json锛?- story-1/2/3 宸插畬鎴愶紙passes: true锛?- **story-4**锛堝墠绔?null/绌烘暟鎹覆鏌撲慨澶嶏級鏄渶楂樼殑鏈畬鎴愭晠浜?- 涓婁竴杞湁 `vitest_fail` 閿欒 **鍒嗘瀽涓婁竴杞け璐ュ師鍥?*锛?- 涓婁竴杞?`.last-error.json` 鎶ュ憡 `vitest_fail`锛屼絾 summary 鏄贡鐮?- 鏌ョ湅 progress.txt锛宻tory-2 + story-3 鍦?2026-06-26 鐨勮褰曚腑鏄剧ず瀹炵幇骞舵爣璁颁负 passes: true - 浣?`.last-error.json` 璇?`detectedFailures: [\"vitest_fail\"]` 鈥?杩欐剰鍛崇潃 API 渚х殑 vitest 娴嬭瘯鍙兘浠嶇劧澶辫触 Now fix the RiskBullets component to handle null var95 and annu"
}
```

## 任务清单（含运行时状态）

```json
{
  "feature": "real-backtest-report",
  "description": "打通回测报告全链路真实数据：补全 Python 引擎缺失指标、统一 API/前端两套类型、修复前端 null/空数据渲染、确保端到端数据流通。",
  "branchName": "ralph/real-backtest-report",
  "userStories": [
    {
      "id": "story-1",
      "title": "补全 Python 回测引擎缺失的衍生统计指标",
      "description": "当前 BacktestMetrics 只有 6 个基础字段（totalReturn/annualizedReturn/sharpeRatio/maxDrawdown/winRate/totalTrades），但 BacktestReportFull 需要 30+ 个指标。需要在 packages/backtest-engine/quantforge_backtest/metrics.py 的 calc_metrics() 中新增：sortinoRatio（下行偏差）、calmarRatio（年化收益/最大回撤）、annualizedVolatility（日收益标准差×√252）、maxDrawdownDuration（从回撤峰值到新高的最长天数）。同时新增 packages/backtest-engine/quantforge_backtest/types.py 的 BacktestMetrics 字段声明。同时更新 BacktestResult 的 trades 列表，从中计算：profitLossRatio（平均盈利/平均亏损）、avgHoldingDays（平均持仓天数）、maxSingleProfit、maxSingleLoss。最后更新 packages/strategy-runtime/quantforge_strategy/commands/backtest.py 的 _result_to_dict()，确保新指标通过 _to_camel 转换后正确传递。",
      "acceptanceCriteria": [
        "BacktestMetrics 新增 sortinoRatio、calmarRatio、annualizedVolatility、maxDrawdownDuration 字段",
        "BacktestResult 新增 profitLossRatio、avgHoldingDays、maxSingleProfit、maxSingleLoss 字段",
        "_result_to_dict() 正确传递所有新字段（通过 _to_camel 转换）",
        "cd packages/backtest-engine && python -m pytest -v 通过",
        "在 Python REPL 或测试中验证：sortinoRatio > 0、calmarRatio > 0、annualizedVolatility > 0"
      ],
      "agent": "backtest-agent",
      "priority": 1,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-2",
      "title": "统一 API BacktestResult 和 BacktestReportFull 类型，对齐 Web 侧",
      "description": "apps/api/src/types.ts 中：(1) BacktestResult 新增 sortinoRatio/calmarRatio/annualizedVolatility/maxDrawdownDuration/profitLossRatio/avgHoldingDays/maxSingleProfit/maxSingleLoss 字段；(2) BacktestReportFull.equityData.monthlyReturns 字段名 return → return_pct；(3) BacktestReportFull.equityData.annualReturns 字段名 return → return_pct；(4) BacktestReportFull.equityData.drawdownSeries 重命名为 drawdownCurve。同时检查 apps/api/tests/ 中相关测试是否需要更新。",
      "acceptanceCriteria": [
        "BacktestResult 包含所有新指标字段（sortinoRatio/calmarRatio/annualizedVolatility/maxDrawdownDuration/profitLossRatio/avgHoldingDays/maxSingleProfit/maxSingleLoss）",
        "BacktestReportFull.equityData.monthlyReturns 类型为 { year: number; month: number; return_pct: number }",
        "BacktestReportFull.equityData.annualReturns 类型为 { year: number; return_pct: number }",
        "BacktestReportFull.equityData.drawdownCurve 替代 drawdownSeries",
        "pnpm --filter @quant/api test 通过",
        "pnpm --filter @quant/api build 通过"
      ],
      "agent": "api-agent",
      "priority": 2,
      "dependsOn": [
        "story-1"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-3",
      "title": "修复 API report-mapper 的字段映射和 null 处理",
      "description": "apps/api/src/services/report-mapper.ts 的 mapBacktestResultToReport 中：(1) equityData.drawdownSeries → drawdownCurve；(2) 从 BacktestResult 新指标填充 returnMetrics（alpha/beta 暂填 0，后续 AI 分析覆盖）、riskMetrics（annualizedVolatility/var95/cvar95/skewness/kurtosis/maxDrawdownDuration）、riskAdjMetrics（sortinoRatio/calmarRatio/informationRatio/treynorRatio）、tradeStats（profitLossRatio/avgHoldingDays/maxSingleProfit/maxSingleLoss）；(3) 所有 nullable 字段用 null 而非 undefined，避免前端序列化问题。同时更新 apps/api/tests/services/report-mapper.test.ts，覆盖新字段的映射测试。",
      "acceptanceCriteria": [
        "report-mapper 输出 equityData.drawdownCurve（非 drawdownSeries）",
        "report-mapper 输出 riskMetrics 包含 annualizedVolatility/maxDrawdownDuration 的真实值",
        "report-mapper 输出 riskAdjMetrics 包含 sortinoRatio/calmarRatio 的真实值",
        "report-mapper 输出 tradeStats 包含 profitLossRatio/avgHoldingDays/maxSingleProfit/maxSingleLoss 的真实值",
        "pnpm --filter @quant/api test 通过",
        "pnpm --filter @quant/api build 通过"
      ],
      "agent": "api-agent",
      "priority": 3,
      "dependsOn": [
        "story-2"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-4",
      "title": "修复前端报告组件 null/空数据渲染问题",
      "description": "以下组件在数据为 null/0 时会显示乱码或误导信息，需要逐个修复：(1) ReportRiskMetrics：${m.maxDrawdownDuration} 天 显示 'null 天' → 前端判断 null 时显示 '--'；风险子弹图 var95/annualizedVolatility 为 0 时显示 0.0% → null 时不渲染该维度；(2) ReportReturnMetrics：alpha 为 0 时显示 '0.0%' → null 时卡片不显示；(3) ReportRiskAdjMetrics：sortinoRatio/informationRatio/treynorRatio 为 0 时显示 '0.00' → null 时卡片不显示；(4) ReportRiskMetrics 雷达图：4/6 维度为 0 导致图形塌缩 → null 维度不纳入 radar indicator；(5) ReportCostSensitivity：costDragRatio/annualTurnover 为 0 时显示 0.0%/0x → null 时显示 '--'。",
      "acceptanceCriteria": [
        "ReportRiskMetrics：maxDrawdownDuration 为 null 时显示 '--' 而非 'null 天'",
        "ReportRiskMetrics：风险子弹图在 var95/annualizedVolatility 为 null 时不渲染对应维度",
        "ReportReturnMetrics：alpha 为 null 时不显示该卡片",
        "ReportRiskAdjMetrics：sortinoRatio/informationRatio/treynorRatio 为 null 时不显示对应卡片",
        "ReportRiskMetrics 雷达图：null 维度不纳入 indicator，避免塌缩",
        "ReportCostSensitivity：costDragRatio/annualTurnover 为 null 时显示 '--'",
        "pnpm --filter @quant/web test 通过",
        "pnpm build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 4,
      "dependsOn": [
        "story-3"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-5",
      "title": "验证 API 报告存储/读取：reportData 包含完整真实数据",
      "description": "启动 API dev server，用 curl 测试报告端到端：(1) 提交回测任务 → 等待完成 → 检查数据库中 reportData JSON 是否包含 equityCurve/drawdownCurve/monthlyReturns/annualReturns（非空数组）以及新指标（sortinoRatio/calmarRatio/annualizedVolatility/maxDrawdownDuration）；(2) GET /api/reports/:id 返回的 reportData 是否完整。同时补充 apps/api/tests/routes/report.test.ts 的测试用例，验证 reportData 字段的存储和读取包含衍生统计。",
      "acceptanceCriteria": [
        "apps/api/tests/routes/report.test.ts 新增 reportData 存储/读取测试用例",
        "curl GET /api/reports/:id 返回的 reportData 包含 drawdownCurve（非空数组）",
        "curl GET /api/reports/:id 返回的 reportData 包含 monthlyReturns（非空数组）",
        "curl GET /api/reports/:id 返回的 reportData 包含 sortinoRatio/calmarRatio（非零值）",
        "pnpm --filter @quant/api test 通过"
      ],
      "agent": "api-agent",
      "priority": 5,
      "dependsOn": [
        "story-3"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-6",
      "title": "端到端验收：回测报告展示完整真实数据",
      "description": "启动 API + Worker + 前端，跑一次 dual_ma 回测，验证全链路：(1) SSE 推送包含 drawdownCurve/monthlyReturns/annualReturns；(2) 前端权益曲线图展示真实数据点；(3) 回撤曲线图展示非零回撤值；(4) 月度热力图展示非空月份数据；(5) 年度柱状图展示非空年度数据；(6) 风险指标卡片展示真实 sortinoRatio/calmarRatio（非 0）；(7) 交易统计卡片展示真实 profitLossRatio/avgHoldingDays；(8) 刷新页面后历史报告仍展示上述数据；(9) 控制台无 TypeScript 类型错误。这是验收故事，不改代码，只验证。",
      "acceptanceCriteria": [
        "启动 API（pnpm --filter @quant/api dev）、Worker（tsx apps/worker/src/main.ts）、前端（pnpm --filter @quant/web dev）",
        "在前端提交 dual_ma 回测任务并等待完成",
        "报告页权益曲线图展示非空数据点",
        "报告页回撤曲线图展示非零回撤值",
        "报告页月度热力图展示非空月份数据",
        "报告页年度柱状图展示非空年度数据",
        "风险指标卡片 sortinoRatio/calmarRatio 为非零真实值",
        "交易统计卡片 profitLossRatio/avgHoldingDays 为非零真实值",
        "刷新页面后历史报告仍展示上述真实数据",
        "控制台无 TypeScript 类型错误"
      ],
      "agent": "fullstack-agent",
      "priority": 6,
      "dependsOn": [
        "story-1",
        "story-2",
        "story-3",
        "story-4",
        "story-5"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    }
  ]
}
```
