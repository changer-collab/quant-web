# QuantForge 自治 Agent 指令

你是一个自治编码 Agent，正在 quant-web 项目中工作。

## 项目规则（必须遵守）

你正在操作的项目有严格的 Agent 角色边界。请先读取以下文件了解规则：

- `AGENTS.md` — 项目全局规则、角色定义、能力边界、协作接口
- `apps/*/AGENT.md` 或 `packages/*/AGENT.md` — 对应子项目的规则

**核心约束：**

- 每个 Agent 只在自己的工作范围内操作，不越界
- 先调研现有代码和目录，再提出方案或修改代码
- **优先新增文件或函数，修改已有代码前必须说明理由**——能加就不改，改就要有明确理由
- 不修改无关文件
- 不做真实下单、券商连接、实盘交易
- 所有回复使用中文
- 遵循 KISS 原则，非必要不要过度设计
- **你运行在非交互批处理模式（`--print`），没有人会回答你的问题。不要问"是否批准"、"是否允许"、"要不要继续"——读完 prd.json 后直接执行实现，不等待回复。不要调用要求用户交互的 skill（如 brainstorming/planning），直接写代码。**

## 跨迭代错误反馈

开始工作前，检查以下文件以了解上一轮的失败：

1. **`scripts/ralph/.last-error.json`** — 如果存在，说明上一轮有错误。先读取它，分析失败原因：
   - `detectedFailures` 告诉你了哪类检查失败（test_fail / lint_error / build_fail 等）
   - `summary` 是错误摘要
   - 不要重复同样的错误

## 你的任务

1. 读取 `scripts/ralph/.last-error.json`（如果存在）— 理解上一轮为什么失败
2. 读取 `scripts/ralph/prd.json` — 你的任务清单
3. 读取 `scripts/ralph/progress.txt` — 历史记忆和已发现的模式（先看 Codebase Patterns 部分）
4. 找到 `passes: false` 的**最高优先级**故事
5. 确认你在正确的分支上（参考 prd.json 的 `branchName`）
6. **只实现这一个故事**
7. 跑质量检查
8. 执行 Doc Sync Gate：判断并更新相关 `docs/plans/*`、`docs/superpowers/specs/*`、`docs/development/*`、`.skills/ralph-harness/*`；如果不需要更新 docs，在 progress.txt 记录原因
9. 提交代码
10. 更新 prd.json（将完成的故事设为 `passes: true`）
11. 追加进度到 progress.txt

## 💡 核心规则：测试文件不可修改

**测试文件（`test_*.py` / `*.test.ts` / `*.spec.ts`）是正确行为的真相来源。**

- 测试失败 → 修复实现代码，**绝不修改测试来迎合错误的实现**
- 如果无法修复实现（超出 scope 或能力范围），保持测试红色
- 通过修改测试的断言值/跳过条件/判等逻辑来让测试通过，等同于制造假绿色

⚠️ **提交前自检**：如果本次修改的文件全是测试文件而没有对应的实现文件，这是一个危险信号。停下来检查。

⚠️ **关于修改测试的人为陷阱**：如果你发现自己同时修改了测试文件和实现文件，停下来问自己——这个测试真的是错的，还是我在找捷径让当前代码通过？如果测试预期值是8，实现返回10，这是测试错了还是实现错了？**正确答案：先修实现，测试不动。** 新的需求（预期值应该是10而不是8）属于新 story 的 scope，不要混在修复 story 里。

## 质量检查命令

完成故事后，必须运行以下检查（根据故事涉及的语言选择）：

### JavaScript/TypeScript 故事

```bash
pnpm lint
pnpm test
pnpm build
```

### Python 故事

```bash
cd packages/<相关包> && python -m pytest -v
```

### 涉及多个语言

```bash
pnpm lint && pnpm test && pnpm build
for pkg in strategy-runtime backtest-engine strategies data-client factor-lab ai-engine obsidian-sync; do
  (cd packages/$pkg && python -m pytest -v)
done
```

## 分支管理

- 检查 prd.json 中的 `branchName`
- 如果当前分支不是目标分支，切过去
- 每个故事完成后提交到当前分支
- 推送时用 `git push origin <branchName>`

## 提交规范

```
feat: [Story ID] - [Story Title]
```

## 进度日志格式

追加到 `scripts/ralph/progress.txt`（永远只追加，不替换）：

```
## [Date/Time] - [Story ID]
- 实现了什么
- 修改了哪些文件
- **后续迭代注意事项：**
  - 发现的模式（如 "这个项目用 X 做 Y"）
  - 遇到的坑（如 "改 W 的时候别忘了更新 Z"）
  - 有用的上下文（如 "评估面板在组件 X 里"）
---
```

## 代码模式汇总

如果你发现了**可复用的模式**，更新 `progress.txt` 顶部的 `## Codebase Patterns` 区域：

```
## Codebase Patterns
- 示例：所有 Python 包用 pyproject.toml + editable install
- 示例：前端测试通过 globalThis.fetch mock 脱离后端
- 示例：Worker 通过 PythonBridge 调用 Python CLI
```

## Doc Sync Gate

提交前，必须判断本 story 是否需要同步文档或开发基础设施：

- 改变执行状态、优先级、阻塞点、下一步 → 更新对应 `docs/plans/*` 的 Current Status / Decision Log / Doc Sync Log
- 改变 API payload、结果结构、DB schema、任务状态机、分类枚举、层间职责边界 → 更新对应 `docs/superpowers/specs/*`
- 发现长期开发标准或 Ralph 可复用规则 → 更新 `docs/development/*` 或 `.skills/ralph-harness/*`
- Ralph 执行的 story → 追加 `scripts/ralph/progress.txt`，并同步 `prd.json` passes
- 如果判断不需要更新 docs，也要在 `progress.txt` 中写明：`Doc Sync: no docs update needed because ...`

**不要等“之后再补文档”。文档同步是完成条件，不是收尾建议。**

## 更新 AGENTS.md

提交前，检查修改的文件所在目录是否有 AGENTS.md：

- 如果发现了有价值的可复用知识，更新对应的 AGENTS.md
- 不要添加故事特定的临时调试信息
- 不要重复 progress.txt 中已有的信息

## 前端故事验证

如果故事涉及 UI 修改：

1. 启动开发服务器：`pnpm --filter @quant/web dev`
2. 在浏览器中验证页面功能正常
3. 截图记录到 progress.txt

## 停止条件

完成一个故事后，检查 prd.json 中是否还有 `passes: false` 的故事：

- 如果**全部完成**（所有故事都是 `passes: true`），输出：`<promise>COMPLETE</promise>`
- 如果还有未完成的故事，正常结束（下一轮迭代会接手）

## 重要

- **每次迭代只做一个且仅一个故事。这是硬规则，不是建议。** 即使在同一次调用中你有能力多做，也必须停下来。原因：你是 `--print` 非交互模式，对话上下文会在长输出后被压缩(compact)，后面的故事会丢失关键代码细节导致质量下降。做完一个就提交退出，让下一轮 iteration 用干净上下文接下一个 story。
- 完成后 **5 分钟内提交并退出**，不要在非交互模式下无限扩展工作范围。
- 频繁提交
- 保持 CI 绿色
- 开始前先读 progress.txt 的 Codebase Patterns 部分


---

## 上一轮运行状态（由 Ralph Core 自动注入）

- 迭代次数: 9
- Feature: （未设置）
- 连续无进展轮数: 0
- 上一轮有错误，请先分析失败原因再行动。

```json
{
  "iteration": 8,
  "exitCode": 0,
  "timestamp": "2026-06-29T17:11:56.912Z",
  "detectedFailures": [
    "lint_error"
  ],
  "summary": "- **涓婁竴杞敊璇?*锛歚lint_error` 鈥?浣?story-19锛堝洜瀛愬瀷璇婃柇绠楁硶锛夊凡瀹屾垚閫氳繃 璁╂垜鍏堝垏鎹㈠埌姝ｇ‘鍒嗘敮锛岀劧鍚庢煡鐪嬪綋鍓嶆枃浠剁粨鏋勩€傝鎴戠湅鐪?factor.py 鐨勫疄鐜版ā寮忥紙浣滀负鍙傝€冿級浠ュ強娴嬭瘯鏂囦欢銆傝鎴戞煡鐪嬬幇鏈夋祴璇曟枃浠跺拰 FormulaFactor 浜嗚В璁＄畻妯″紡銆傝鎴戞鏌ユ槸鍚﹀凡鏈夋祴璇曟枃浠跺拰褰撳墠瀹炵幇鐨勭姸鎬併€傜幇鍦ㄦ垜寮€濮嬪疄鐜?**story-20锛圥hase 3c 鈥?闈炲洜瀛愬瀷璇婃柇绠楁硶锛?*銆傞渶瑕佹浛鎹?`non_factor.py` 鐨?stub 涓虹湡瀹炵畻娉曪紝骞跺垱寤烘祴璇曟枃浠躲€傜幇鍦ㄥ垱寤烘祴璇曟枃浠讹細鐜板湪杩愯娴嬭瘯锛歚_simulate_trading` 杩斿洖 3 涓€间絾鍙В鍖呬簡 2 涓€備慨澶嶈繖涓?bug锛氬悓鏍蜂慨澶?`_fallback_simulation` 鐨勮В鍖咃細鎴戣繕闇€瑕佹鏌?`_compute_slippage_stress` 璋冪敤澶勭殑鍙傛暟锛氱幇鍦ㄩ噸鏂拌繍琛屾祴璇曪細30/30 鍏ㄩ儴閫氳繃"
}
```

## 任务清单（含运行时状态）

```json
{
  "feature": "strategy-classification-phase1-4",
  "description": "策略分类 Phase 1→4：配置链路打通 + 死代码清理 + 诊断后端 + 回测参数 UI",
  "branchName": "ralph/strategy-classification",
  "userStories": [
    {
      "id": "story-14",
      "title": "Phase 1a — ConfigSnapshot 类型导出 + WorkspacePage 配置读取挂载",
      "phase": 1,
      "description": "**任务 1.5**：在 apps/web/src/data/types.ts 中导出 ConfigSnapshot 接口 `{ strategy: string; params: Record<string, unknown> }`。\n\n**任务 1.1**：修改 apps/web/src/components/workspace-page.tsx，在组件 useEffect（deps: [strategy.name]）中调用 `fetchStrategyConfig(strategy.name)`，将结果存入 `configSnapshot` state（初始值 null）。读取失败时 console.warn，不阻塞页面渲染。\n\n验收：WorkspacePage 挂载时控制台可见 config 请求；tests/setup.ts 中 GET config mock 返回 null 时组件不崩溃。",
      "acceptanceCriteria": [
        "types.ts 导出 ConfigSnapshot 接口",
        "WorkspacePage useEffect 内调用 fetchStrategyConfig(strategy.name)",
        "configSnapshot state 初始值 null，读取成功后更新",
        "GET config 返回 404/null 时页面不崩溃",
        "cd apps/web && npx tsc --noEmit 无错误",
        "npm test 通过"
      ],
      "agent": "frontend-agent",
      "priority": 14,
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-15",
      "title": "Phase 1b — diagnostics/backtest payload 带真实 configSnapshot + API category 字段",
      "phase": 1,
      "description": "**任务 1.2**：修改 apps/web/src/components/workspace-page.tsx 中 `handleRunDiagnostics()`：POST /api/tasks body 中加入 `configSnapshot`（来自 state）和 `category`（来自 strategy.category）。\n\n**任务 1.3**：修改 `handleRunBacktest()`：POST body 加入 `configSnapshot`。同步修改 apps/web/src/api/tasks.ts 中的 BacktestPayload 类型。\n\n**任务 1.4**：POST body 中 `strategy` 字段统一使用 `strategy.name`（snake_case），与后端 API 对齐。\n\n**任务 1.6**：修改 apps/api/src/routes/task.ts：POST /api/tasks 路由 schema 中 diagnostics payload 追加可选字段 `category: z.string().optional()`，透传给 Worker。",
      "acceptanceCriteria": [
        "handleRunDiagnostics POST body 含 configSnapshot 和 category",
        "handleRunBacktest POST body 含 configSnapshot",
        "POST body strategy 字段为 strategy.name",
        "API task.ts diagnostics payload 接受 category 字段",
        "cd apps/api && npx tsc --noEmit 无错误",
        "cd apps/web && npx tsc --noEmit 无错误",
        "npm test 通过"
      ],
      "agent": "fullstack-agent",
      "priority": 15,
      "dependsOn": [
        "story-14"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-16",
      "title": "Phase 2a — 删除旧 strategy-grid.tsx + App.tsx 旧 WorkspaceContent 引用",
      "phase": 2,
      "description": "**任务 2.1**：删除 apps/web/src/components/strategy-grid.tsx（已标记 deprecated，被 strategy-grid-new.tsx 取代）。\n\n**任务 2.2**：修改 apps/web/src/App.tsx：删除对 WorkspaceContent（旧单页面）的条件渲染分支，保留 WorkspacePage（新双流程）。删除对已删文件的 import。\n\n删前先 grep 确认无其他组件仍在 import strategy-grid.tsx 和 WorkspaceContent。",
      "acceptanceCriteria": [
        "strategy-grid.tsx 已删除",
        "App.tsx 无旧 WorkspaceContent 分支",
        "grep -r 'strategy-grid' src/ 只剩 strategy-grid-new 引用",
        "cd apps/web && npx tsc --noEmit 无错误",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 16,
      "dependsOn": [
        "story-15"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-17",
      "title": "Phase 2b — ResearchMode 类型/数据/i18n/useResearchWorkflow 全清理",
      "phase": 2,
      "description": "**任务 2.3**：修改 apps/web/src/data/types.ts：删除 ResearchMode / ResearchModeId 类型定义。\n\n**任务 2.4**：修改 apps/web/src/data/appData.ts：删除 researchModes 数组及相关类型引用。修改 apps/web/src/data/accessors.ts：删除 researchModes 相关 accessor。修改国际化文件 en.ts / zh.ts：删除 researchModes 相关 key-value 条目。\n\n**任务 2.5**：重写 apps/web/src/hooks/useResearchWorkflow.ts：删除 mode 参数（'traditional'|'hft'|'ai'），改用 category（StrategyCategory）。activeMode → activeCategory。\n\n**任务 2.6**：修改 apps/web/src/data/localization.ts 和 factories.ts：删除对 ResearchMode 的引用。\n\n每删一处，立即运行 npx tsc --noEmit 确认无新错误。",
      "acceptanceCriteria": [
        "types.ts 无 ResearchMode / ResearchModeId",
        "appData.ts 无 researchModes 数组",
        "en.ts / zh.ts 无 researchModes 相关 key",
        "useResearchWorkflow.ts 无 'traditional' | 'hft' | 'ai' 字面量",
        "cd apps/web && npx tsc --noEmit 无错误",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 17,
      "dependsOn": [
        "story-16"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-17b",
      "title": "Phase 2d — diagnostic_results 表加 category 列 + transitional mock 数据修复",
      "phase": 2,
      "description": "**DB migration**：修改 apps/api/src/storage/connection.ts 建表语句，在 diagnostic_results 表中加入 `category TEXT NOT NULL DEFAULT 'non_factor'`。同步修改 apps/api/src/storage/schema.ts（Drizzle schema 加 category 字段）和 apps/api/src/types.ts（DiagnosticResult 接口加 `category: StrategyCategory`）。同步修改 apps/api/src/repositories/sqlite-diag-repo.ts 的 save/getById/listByStrategy 方法读写 category 字段。\n\n**Mock 数据修复**：修改 apps/web/src/data/en.ts 和 zh.ts 中 transitional 策略数据，将错误的 `subcategory: 'event_driven'`（非因子型子分类）改为 `subcategory: null`，`workflowReady: true` 保持不变（符合新 API 逻辑）。",
      "acceptanceCriteria": [
        "diagnostic_results 建表 SQL 含 category 列",
        "schema.ts Drizzle 定义含 category 字段",
        "DiagnosticResult 接口含 category: StrategyCategory",
        "sqlite-diag-repo.ts save/read 正确处理 category",
        "en.ts / zh.ts transitional 策略 subcategory 为 null",
        "cd apps/api && npx tsc --noEmit 无错误",
        "npm test 通过"
      ],
      "agent": "fullstack-agent",
      "priority": 17,
      "dependsOn": [
        "story-17"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-18",
      "title": "Phase 3a — Python diagnostics 框架：目录 + 入口 + CLI 注册",
      "phase": 3,
      "description": "在 packages/strategy-runtime/quantforge_strategy/commands/ 下新建：\n- `diagnostics/__init__.py`（空）\n- `diagnostics/factor.py`：DiagnosticsFactor.run(params, emit) 返回 stub FactorDiagnosticsResult（全零）\n- `diagnostics/non_factor.py`：DiagnosticsNonFactor.run(params, emit) 返回 stub NonFactorDiagnosticsResult（全零）\n- `diagnostics/transitional.py`：DiagnosticsTransitional.run(params, emit) 返回 stub TransitionalDiagnosticsResult\n- `commands/diagnostics.py`：run_diagnostics(params, emit) 按 params['category'] 分支调用对应类\n\n修改 cli.py：_COMMANDS['diagnostics'] = run_diagnostics。\n\n新增 tests/test_diagnostics_stub.py：3 种 category 均返回含 type 字段的 dict，未知 category 不崩溃。",
      "acceptanceCriteria": [
        "_COMMANDS['diagnostics'] 注册成功",
        "category='factor_based' → dict 含 type='factor_based'",
        "category='non_factor' → dict 含 type='non_factor'",
        "category='transitional' → dict 含 type='transitional'",
        "未知 category 不抛异常",
        "python -m pytest tests/test_diagnostics_stub.py -v 通过"
      ],
      "agent": "strategy-agent",
      "priority": 18,
      "dependsOn": [
        "story-17b"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-19",
      "title": "Phase 3b — 因子型诊断算法（IC / 分层收益 / 相关性矩阵）",
      "phase": 3,
      "description": "实现 commands/diagnostics/factor.py 真实算法：\n1. IC 序列：按月12期计算各因子与 T+1 期收益的 Pearson / Spearman rank IC\n2. 分层收益：按因子值均匀分 5 组（Q1~Q5），计算各组累计收益\n3. 相关性矩阵：多因子 Pearson 相关，输出 correlation_matrix + factor_labels\n4. summary：mean_ic / ic_std / ic_ir(=mean_ic/ic_std) / mean_rank_ic\n\n数据不足时返回空数组并 emit warning，不崩溃。\n\n新增 tests/test_diagnostics_factor.py：mock 数据验证 ic_series 长度、layered_returns 5 组、矩阵维度。",
      "acceptanceCriteria": [
        "ic_series 长度 = period 数",
        "layered_returns 含 Q1~Q5 共 5 组",
        "correlation_matrix 维度正确",
        "ic_ir ≈ mean_ic / ic_std",
        "数据不足时不崩溃",
        "python -m pytest tests/test_diagnostics_factor.py -v 通过"
      ],
      "agent": "strategy-agent",
      "priority": 19,
      "dependsOn": [
        "story-18"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-20",
      "title": "Phase 3c — 非因子型诊断算法（参数敏感性 / 信号质量 / 滑点压力）",
      "phase": 3,
      "description": "实现 commands/diagnostics/non_factor.py：\n1. 参数敏感性：对每个数值型参数在 [min,max] 取 5 个值，各跑简化回测（跳过 analyze/syncBacktest），记录 {param, values, returns, sharpe}\n2. 信号质量：统计 total_signals / win_rate / avg_holding_bars / profit_factor / max_consecutive_losses\n3. 滑点压力：1/3/5/10 bps 各重算收益和夏普，输出 [{bps, return, sharpe, trade_count}]\n\n参数无 min/max 时跳过该参数，不崩溃。\n\n新增 tests/test_diagnostics_non_factor.py：验证 param_sensitivity 5 值点、slippage_stress 4 档。",
      "acceptanceCriteria": [
        "param_sensitivity 每参数含 5 个值点",
        "slippage_stress 含 1/3/5/10 bps 共 4 档",
        "signal_quality 含 win_rate / profit_factor / max_consecutive_losses",
        "参数无 min/max 时跳过（不崩溃）",
        "python -m pytest tests/test_diagnostics_non_factor.py -v 通过"
      ],
      "agent": "strategy-agent",
      "priority": 20,
      "dependsOn": [
        "story-18"
      ],
      "passes": true,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    },
    {
      "id": "story-21",
      "title": "Phase 3d — Worker DiagnosticsHandler 改造 + API 结构化存储 + 前端替换 mock",
      "phase": 3,
      "description": "**Worker**：修改 apps/worker/src/handlers/diagnostics-handler.ts：从 task.payload 提取 category + configSnapshot，构建 PythonBridge.streamCall({ command: 'diagnostics', strategy, category, configSnapshot, symbol, timeframe, dataRange })。\n\n**API**：修改 apps/api/src/routes/task.ts 的 complete handler：diagnostics 任务解析 result.diagnostics（含 type 字段）存入 diagnostic_results 表，SSE result 事件追加 resultId + resultType。\n\n**前端**：修改 workspace-page.tsx：删除 det() mock 函数和 mockDiagnosticsData，改从 SSE complete 事件的 data.diagnostics 字段渲染。更新 tests/setup.ts POST /tasks mock 含 diagnostics 字段。",
      "acceptanceCriteria": [
        "DiagnosticsHandler streamCall 含 category + configSnapshot",
        "SSE result 事件含 resultId + resultType + diagnostics",
        "workspace-page.tsx 无 det() 和 mockDiagnosticsData",
        "前端按 diagnostics.type 分支渲染",
        "cd apps/api && npx tsc --noEmit 无错误",
        "cd apps/web && npx tsc --noEmit 无错误",
        "pnpm test + pnpm build 通过"
      ],
      "agent": "fullstack-agent",
      "priority": 21,
      "dependsOn": [
        "story-19",
        "story-20"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 1,
        "lastError": null
      }
    },
    {
      "id": "story-22",
      "title": "Phase 4 — WorkspacePage 回测参数表单（symbol / timeframe / 资金 / 日期）",
      "phase": 4,
      "description": "修改 workspace-page.tsx 的 WorkspaceStep2，在提交按钮上方新增参数输入区：\n- symbol 输入框（默认从 configSnapshot.params.symbol 读，无则 '600519'）\n- timeframe 下拉（'1d'|'1h'|'30m'，默认 '1d'）\n- 初始资金 number input（默认 1000000）\n- 回测区间 date picker（默认近1年）\n\n提交时 payload 带入 { symbol, timeframe, initialCapital, dataRange: { startTs, endTs } }。\n默认值优先级：configSnapshot.params > URL searchParams > 硬编码默认。",
      "acceptanceCriteria": [
        "Step 2 有 symbol / timeframe / initialCapital / 日期区间输入",
        "symbol 默认从 configSnapshot.params.symbol 读取",
        "提交 POST body 含 symbol / timeframe / initialCapital / dataRange",
        "cd apps/web && npx tsc --noEmit 无错误",
        "npm test + npm run build 通过"
      ],
      "agent": "frontend-agent",
      "priority": 22,
      "dependsOn": [
        "story-21"
      ],
      "passes": false,
      "_runtime": {
        "attempts": 0,
        "lastError": null
      }
    }
  ]
}
```
