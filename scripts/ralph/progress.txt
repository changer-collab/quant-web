# Ralph Progress Log
Started: 2026-06-26
---

## Codebase Patterns
- factor-lab 的 FormulaFactor 用受限 AST 手动递归求值，strategy-runtime 的 factorEval 只延迟导入并调用它，不在 runtime 内实现公式逻辑
- strategy-runtime 的 CLI 命令必须延迟导入下游包；测试可用 importlib 加 __import__ guard 防止顶层导入 quantforge_factor/quantforge_data 等
- backtest-engine 的 Trade 类来自 strategy-runtime 包（quantforge_strategy.order），不在 backtest-engine 内定义
- 所有 runner（BacktestRunner、MultiSymbolRunner、MultiStrategyRunner）都用 calc_metrics + calc_trade_stats 构建 BacktestResult
- _to_camel() 函数自动把 snake_case 字段转 camelCase，新增字段无需手动处理映射
- Windows 环境下时间戳用秒级（10位）测试，毫秒级（13位）在 equity_stats 模块有专门处理
- SSE 事件 data 字段：API `/complete` 路由中 `event.data = result`，result = `{ backtestResult, analysis }`
- 前端 `mapBacktestResultToReport()` 接收 `{ backtestResult: PythonBacktestResult, analysis }` 格式
- Obsidian sync 通过 Python CLI `syncBacktest` 命令由 Worker 在回测完成后触发
- Worker 的 BacktestHandler 已在第 89-100 行包含 Obsidian 同步 try-catch
- eslint 全局配置在根 `eslint.config.mjs`，需要 Node.js 运行时变量（`setTimeout`/`setInterval`/`clearTimeout`/`AbortController` 等）必须显式添加 globals
- API 路由 `task.ts` 引用了 web 端的 `ReportRiskWarnings` 类型，但该类型只在 web 包定义；API 有自己独立的 types.ts
- worker 包有三个独立的 Agent executor，构成了 "wrapper → converter → bridge" 调用链
- strategy-runtime CLI 子进程 stdout 强制 UTF-8，测试用 `subprocess.run(..., text=True)` 时也要显式 `encoding="utf-8"`，否则 Windows 中文环境会按 GBK 解码失败
- strategy-runtime 的 `commands/__init__.py` 也必须保持函数内延迟导入，避免导入单个命令时加载 sibling 命令的下游包依赖
- AI 预测策略复用 `AIPredictor` 时至少需要 21 根 bar 才有非空特征；组合择时器要按 symbol 隔离历史窗口，避免多标的 rolling 特征混用

---

## [2026-06-27 13:00] - story-7
- 端到端验证：story-7 不直接改代码，但修复了 build 阻塞问题以验证全链路
- **修复**: worker 的 python-bridge.ts 中 `stdout` 变量未声明（`let stdout = ""`），导致 lint 报 `no-undef`
- **修复**: worker 的 backtest-agent.ts 中 `request.params as BacktestParams` 需要 `as unknown as` 双转换（`Record<string, unknown>` 与 `BacktestParams` 不重叠）
- **修复**: worker 的 python-agent.ts 中 `onEvent` 回调类型从 `{ event: string; [key: string]: unknown }` 改为 `StreamEvent`，需要导入 `StreamEvent` 类型
- **修复**: API 的 task.ts 引用了 web 端的 `ReportRiskWarnings` 类型，改为 inline 类型
- **修复**: eslint.config.mjs 添加 `setInterval`/`clearTimeout`/`clearInterval`/`AbortController`/`TextDecoder` 全局变量
- 全链路 build/tests/lint 通过
- **后续迭代注意事项：**
  - eslint.config.mjs 是所有包的全局 eslint 配置，缺少的 Node.js 运行时全局变量（setTimeout 等）应往这里加
  - API 的 types.ts 有独立于 web 端的类型定义，不要在 API 代码中引用 web 端类型
  - worker 的 PythonBridge 有同步和流式两种调用模式，`call()` 方法中 `let stdout = ""` 漏掉变量声明
- 所有 story (1-7) 均已完成
---
- eslint 全局配置在根 `eslint.config.mjs`，需要 Node.js 运行时变量（`setTimeout`/`setInterval`/`clearTimeout`/`AbortController` 等）必须显式添加 globals
- API 路由 `task.ts` 引用了 web 端的 `ReportRiskWarnings` 类型，但该类型只在 web 包定义；API 有自己独立的 types.ts
- worker 包有三个独立的 Agent executor，构成了 "wrapper → converter → bridge" 调用链

---

## [2026-06-26] - PRD 重置 v2
根本原因复盘：上一版 prd.json 犯的错误：
1. **先入为主定位** — 没做诊断就直接锁定了"Worker→前端通信 bug"，实际上完全不知道故障在哪
2. **猜修复不猜诊断** — story-1 直接写了一堆"检查点 X、检查点 Y"，相当于猜了 6 个可能的位置去修，不是去观测
3. **跳过了第一性** — 不确定故障位置时，第一步永远是加日志观测，而不是猜修复

本次更新（v2）改为"先观测后修复"模式：
- story-1: 只加日志埋点，不修任何业务逻辑
- story-2: 根据 story-1 日志定位根因后精确修复
- story-3/4: 前端显示异常修复（job 名称、失败跳转）
- story-5: Obsidian 同步补齐
- story-6: 数据范围选择器
- story-7: 端到端验证
---

## ✅ ALL STORIES COMPLETE

# Branch changed to ralph/pipeline-critical-fixes

## [2026-06-27 16:12:32] - Iteration 1

- Exit code: 0
- Stories remaining: 7
---

## [2026-06-27 16:30:32] - Iteration 1

- Exit code: 0
- Stories remaining: 7
- **Detected issues:**
```
vitest_fail
```
---

## [2026-06-28 01:09:10] - story-1
- 实现了回测 CLI 默认接入 A 股市场规则：`_run_single()` 和 `_run_composite()` 创建 runner 时传入 `ASHARE_RULES`
- 修改 `MultiSymbolRunner`：新增 `market_rules` 参数，透传给 `Matcher`/`PortfolioManager`，交易日切换时调用 `unlock_t_plus_1()`，卖单撮合时传入 `available_qty`
- 新增/更新测试覆盖：多标的 T+1 当日卖出拦截、下一交易日解锁、CLI 单标的/组合回测默认传入 `ASHARE_RULES`
- 修复 Windows 中文环境下 CLI subprocess 测试解码问题：`test_cli.py` 的 `subprocess.run(..., text=True)` 显式 `encoding="utf-8"`
- 修改了哪些文件：
  - `packages/backtest-engine/quantforge_backtest/multi_runner.py`
  - `packages/backtest-engine/tests/test_multi_runner.py`
  - `packages/strategy-runtime/quantforge_strategy/commands/backtest.py`
  - `packages/strategy-runtime/tests/test_backtest_command_market_rules.py`
  - `packages/strategy-runtime/tests/test_cli.py`
  - `packages/strategy-runtime/AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- 验证：
  - `cd packages/backtest-engine && python -m pytest -v` → 68 passed
  - `cd packages/strategy-runtime && python -m pytest -v` → 54 passed
- **后续迭代注意事项：**
  - 多标的 runner 的撮合发生在策略 `on_bars()` 之前，策略本步提交的订单会在下一时间点撮合；测试同日 T+1 锁定时要控制 bars 数量，避免挂起卖单在下一交易日自然解锁成交
  - `strategy-runtime` 的 backtest 命令可延迟导入下游包，但测试里若 import 该命令模块，需要本地环境已能解析 `quantforge_backtest`/`quantforge_data`/`quantforge_strategies`
  - Windows 中文环境下，CLI 已输出 UTF-8，父进程测试也必须按 UTF-8 解码
---

## [2026-06-27 17:14:07] - Iteration 1

- Exit code: 0
- Stories remaining: 6
---

## [2026-06-28 02:13:56] - story-2
- 实现了 factor-lab 的 `FormulaFactor`：基于受限 AST 手动递归求值，支持 open/high/low/close/volume/turnover 列、四则运算、括号、`pct_change`/`rolling_mean`/`rolling_std`/`shift`/`log`/`rank`
- 修改 strategy-runtime 的 factorEval：`_make_factor()` 不再返回固定 close stub，改为构建 `FactorDefinition` 后返回 `FormulaFactor(definition)`；同时保持 quantforge_factor/quantforge_data 在命令执行路径内延迟导入
- 新增测试覆盖：日内收益公式、pct_change、均线偏离、剩余安全函数、非法公式/属性调用/布尔常量/非字符串公式拒绝、显式序列函数不依赖 close、非正窗口和多余参数拒绝、factorEval 确认不再等于 df['close']、非法公式返回 `INVALID_FACTOR_FORMULA`、缺失 formula 被拒绝
- 代码审查反馈已处理：修复了 CLI 命令顶层导入下游包问题，修复了 `rolling_mean(volume, 2)`/`shift(turnover, 1)` 这类显式序列函数误读 `close` 的问题，补充安全边界测试，把公式校验错误映射为稳定业务错误码，并禁止缺失 `factor.formula` 静默退回 close
- 修改了哪些文件：
  - `packages/factor-lab/quantforge_factor/formula.py`
  - `packages/factor-lab/quantforge_factor/__init__.py`
  - `packages/factor-lab/tests/test_formula.py`
  - `packages/strategy-runtime/quantforge_strategy/commands/factor_eval.py`
  - `packages/strategy-runtime/tests/test_factor_eval_formula.py`
  - `packages/strategy-runtime/README.md`
  - `packages/strategy-runtime/AGENT.md`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- 验证：
  - `cd packages/factor-lab && python -m pytest -v` → 18 passed
  - `cd packages/strategy-runtime && python -m pytest -v` → 58 passed
- **后续迭代注意事项：**
  - 新公式语法刻意禁止 pandas 属性调用（如 `close.shift(5)`），应使用白名单函数形式 `shift(close, 5)`；后续如要兼容历史公式，应显式新增安全转换或白名单 AST 节点
  - `pct_change(N)`/`shift(N)` 默认作用于 close；也支持显式序列形式如 `pct_change(volume, 5)`/`shift(turnover, 1)`
  - 公式窗口参数必须为正整数，避免 `shift(close, -1)` 引入未来函数/前视偏差
---


## [2026-06-28 03:20:00] - story-3
- 实现了 AI 模型持久化：`ModelTrainer.save()`/`ModelTrainer.load()` 用 joblib 保存和恢复已训练模型，并在 `AIPredictor` 上暴露 `save()`/`load()` 统一入口
- 修改 `aiTrain` 命令：训练完成后默认保存到 `data/models/randomForest.joblib`（可通过 `modelPath`/`outputPath`/`modelName` 覆盖），返回兼容旧指标字段且新增 `metrics` 与 `modelPath`；命令包入口改为函数内延迟导入，避免 sibling 命令提前加载下游依赖
- 新增 `AIPredictorStrategy`：初始化时加载模型，按 symbol 维护历史 bar，特征窗口不足 21 根时 Hold，预测后映射为 Buy/Sell/Hold，并注册为 `ai_predictor`
- 处理代码审查反馈：修复 forward return 最后一行 NaN 被转成负样本、空特征窗口调用 sklearn、单类别 `predict_proba`、默认训练/策略路径不一致、camelCase `modelPath` 参数、组合多标的历史混用、`modelName` 路径逃逸、策略包 AI 依赖声明
- 修改了哪些文件：
  - `AGENTS.md`
  - `packages/ai-engine/pyproject.toml`
  - `packages/ai-engine/quantforge_ai/model.py`
  - `packages/ai-engine/quantforge_ai/predictor.py`
  - `packages/ai-engine/tests/report_analysis/test_llm_client.py`
  - `packages/ai-engine/tests/test_model.py`
  - `packages/ai-engine/tests/test_predictor.py`
  - `packages/strategy-runtime/AGENT.md`
  - `packages/strategy-runtime/quantforge_strategy/commands/__init__.py`
  - `packages/strategy-runtime/quantforge_strategy/commands/ai_train.py`
  - `packages/strategy-runtime/tests/test_ai_train_command.py`
  - `packages/strategies/pyproject.toml`
  - `packages/strategies/quantforge_strategies/__init__.py`
  - `packages/strategies/quantforge_strategies/combined/ai_predictor.py`
  - `packages/strategies/tests/test_ai_predictor_strategy.py`
  - `packages/strategies/tests/test_registry.py`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- 验证：
  - `cd packages/ai-engine && python -m pytest -v` → 32 passed
  - `cd packages/strategy-runtime && python -m pytest -v` → 62 passed
  - `cd packages/strategies && python -m pytest -v` → 73 passed
- **后续迭代注意事项：**
  - `AIPredictor.predict()` 的 `FeatureExtractor.extract_all(...).dropna()` 可能为空；调用 sklearn 前要返回空 PredictionResult，策略侧也要 Hold
  - 二分类标签必须先按特征 index 对齐并 `dropna()`，再 `(forward_returns > 0).astype(int)`，否则最后一根未知未来收益会被当成 0 类
  - `commands/__init__.py` 顶层 re-export 会破坏单命令延迟导入，下游依赖要留在具体函数内部
  - `ai_predictor` 作为组合择时器时同一个实例会被多个 symbol 复用，所有 rolling/历史窗口状态必须按 symbol 分桶
---

## [2026-06-27 22:22:23] - Iteration 3

- Exit code: 0
- Stories remaining: 4
- **Detected issues:**
```
pytest_fail
```
---

# Branch changed to ralph/strategy-classification

## [2026-06-28 09:47:42] - Iteration 1

- Exit code: 1
- Stories remaining: 13
---

## [2026-06-28 19:58] - story-3
- 实现了 Repository 层 + Service 层 + 三层架构接线
- 新建 4 个文件：
  - `apps/api/src/repositories/sqlite-config-repo.ts` — SqliteConfigRepo (implements IConfigRepo, save() 内透明调用 historyRepo.append()) + SqliteConfigHistoryRepo (implements IConfigHistoryRepo)
  - `apps/api/src/repositories/sqlite-diag-repo.ts` — SqliteDiagnosticRepo (implements IDiagnosticRepo, purgeOlderThan 执行 DELETE WHERE)
  - `apps/api/src/services/config-service.ts` — StrategyConfigService（纯业务逻辑，只依赖 IConfigRepo 接口）
  - `apps/api/src/services/diagnostic-service.ts` — DiagnosticService（纯业务逻辑，只依赖 IDiagnosticRepo 接口）
- 修改 2 个文件：
  - `apps/api/src/app.ts` — AppOptions 追加 configService/diagnosticService，buildApp 内 app.decorate
  - `apps/api/src/index.ts` — 组合根创建 configHistoryRepo → configRepo → diagRepo → configService → diagnosticService → 注入 buildApp
- 注意：SQL.js 驱动下 Drizzle ORM 的 `.run()` 统一返回 `void`，无法获取 DELETE 影响行数，返回值暂设为0
- 验证：
  - `cd apps/api && npx tsc --noEmit` → 通过
  - `pnpm build` → 全包构建通过
  - `pnpm lint` → 全绿（仅已存在的 warning）
  - `pnpm test` → 全部 269 tests passed
- **后续迭代注意事项：**
  - Drizzle ORM + SQL.js 驱动的 `.run()` 全部返回 `void`，不要在 service/repo 层依赖返回值
  - 三层架构的业务层（services/）不应 import Drizzle 或 sql.js 相关类型
  - declare module 'fastify' 的模块增强可在多个 service 文件中共存，TypeScript 会自动合并
---

## [2026-06-28 20:12] - story-5
- 实现了策略列表扩展 + Preview 端点
- **5a. 策略列表扩展：**
  - 修改 `apps/api/src/services/strategy-sync.ts`：扩展 PythonStrategyMeta 接口（category/subcategory/扩展 params），更新嵌入的 Python 脚本提取 chart_relevant/ui_constraints/category/subcategory
  - 修改 `apps/api/src/routes/strategy.ts`：响应映射新增 category/subcategory/workflowReady/params.chartRelevant+uiConstraints，向后兼容默认值
- **5b. Preview 端点：**
  - 新建 `apps/api/src/services/preview-service.ts`：PreviewService 类，纯 TypeScript 实现 SMA/EMA/RSI/MACD + 金叉死叉/超买超卖信号检测，fingerprint = sha256(JSON(params) + last 10 close prices)
  - 新建 `apps/api/src/routes/preview.ts`：POST /:name/preview，接受 {symbol, timeframe, cursor, limit, preview_params}，从 dataCenter.repos.bars 加载 K 线，反向分页（cursor=null 返回最近 limit 根），返回 bars/overlays/signals/pagination/fingerprint/engine_version
  - 修改 `apps/api/src/app.ts`：注册 previewRoutes
- 修改了哪些文件：
  - `apps/api/src/services/strategy-sync.ts`
  - `apps/api/src/routes/strategy.ts`
  - `apps/api/src/services/preview-service.ts`（新建）
  - `apps/api/src/routes/preview.ts`（新建）
  - `apps/api/src/app.ts`
- 验证：
  - `cd apps/api && npx tsc --noEmit` → 通过
  - `pnpm build` → 5/5 successful
  - `pnpm lint` → 全绿（0 errors）
  - `pnpm test` → 全部 269 tests passed
- **后续迭代注意事项：**
  - 反向分页（cursor=null → 最近 limit 根）通过 BarRepository.queryPaged + slice(-limit) 实现，MAX_INITIAL_BATCH=10000 适用于大部分预览场景
  - PreviewService 的 computeEMA 支持输入含前导 NaN，用于 MACD 信号线计算
  - RSI 使用 Wilder 平滑法，与常见交易软件结果一致
  - fingerprint 使用 Node.js crypto 内置模块，无需额外依赖
  - data-center 的 RepoSet.bars 可通过 `app.dataCenter.repos.bars` 直接访问
---

## [2026-06-28 12:00:50] - Iteration 1

- Exit code: 0
- Stories remaining: 10
---

## [2026-06-28 12:03:22] - Iteration 2

- Exit code: 0
- Stories remaining: 9
---

## [2026-06-28 12:14:17] - Iteration 3

- Exit code: 0
- Stories remaining: 8
- **Detected issues:**
```
lint_error
build_fail
```
---

## [2026-06-28 20:20] - story-6
- 实现了 POST /api/tasks 的 configSnapshot 改造 + Diagnostics 端点
- **6a. Task 改造：**
  - `apps/worker/src/types.ts` — TaskType 追加 Diagnostics='diagnostics'
  - `apps/worker/src/handlers/backtest-handler.ts` — BacktestPayload 新增 configSnapshot 字段，strategyParams 优先从 configSnapshot.params 提取（向后兼容 params 降级）
  - `apps/worker/src/agents/backtest-agent.ts` — BacktestParams 新增 configSnapshot 字段，同样优先使用 configSnapshot.params
- **6b. Diagnostics 端点：**
  - 新建 `apps/worker/src/handlers/diagnostics-handler.ts` — DiagnosticsHandler，通过 PythonBridge 调用 diagnostics CLI（未实现时回显输入）
  - 修改 `apps/worker/src/main.ts` — createHandler 新增 diagnostics 分支
  - 修改 `apps/worker/src/index.ts` — 导出 DiagnosticsHandler
  - 新建 `apps/api/src/routes/diagnostics.ts` — GET /:resultId 和 GET /?strategy=xxx 两个端点
  - 修改 `apps/api/src/app.ts` — 注册 diagnosticRoutes
  - 修改 `apps/api/src/routes/task.ts` — complete handler 中对 diagnostics 类型任务调用 diagnosticService.storeResult()，SSE 事件附加 resultId+resultType，使用 randomUUID()
- 修改了哪些文件：
  - `apps/worker/src/types.ts`
  - `apps/worker/src/handlers/backtest-handler.ts`
  - `apps/worker/src/agents/backtest-agent.ts`
  - `apps/worker/src/handlers/diagnostics-handler.ts`（新建）
  - `apps/worker/src/main.ts`
  - `apps/worker/src/index.ts`
  - `apps/api/src/routes/diagnostics.ts`（新建）
  - `apps/api/src/app.ts`
  - `apps/api/src/routes/task.ts`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- 验证：
  - `cd apps/api && npx tsc --noEmit` → 编译通过
  - `cd apps/worker && npx tsc --noEmit` → 编译通过
  - `pnpm lint` → 全绿（仅已存在 warning）
  - `pnpm test` → API 42 passed + Worker 26 passed + 前端全部通过 = 269+
  - `pnpm build` → 5/5 successful
- **后续迭代注意事项：**
  - DiagnosticsHandler 当前为占位实现，桥接到 Python CLI 的 diagnostics 命令；Python 侧未实现时回显输入。story-12 的实现可能需要升级此 handler
  - randomUUID() 来自 node:crypto，在 Node 14.17+ 可用
  - diagnostics 的完整数据流：客户端 POST 创建 diagnostics 任务 → Worker 轮询执行 → 调用 /:id/complete → API 存储结果 → 客户端通过 GET /api/diagnostics/:resultId 查询
  - 前端 SSE stream 中 result 事件的 data.resultId 和 data.resultType 字段由 complete handler 注入，非 worker 返回
  - backtest-handler 中 strategyParams 提取改为 `payload.configSnapshot?.params ?? payload.params ?? {}`，保持向后兼容
---

## [2026-06-28 20:25] - story-7
- 扩展了 smoke-test.sh：添加 Test 4 (GET /api/strategies 含 category 字段检查)、Test 5 (PUT+GET config 读写验证)、Test 6 (GET diagnostics 空数组检查)
- 全量验证全部通过：
  - pnpm lint → 全绿（0 errors）
  - pnpm test → API 42 + Worker 26 + Web 96 + Data-center 47 + Data-collector 58 = 269 passed
  - pnpm build → 5/5 successful
  - strategy-runtime → 75 passed
  - strategies → 73 passed
  - backtest-engine → 80 passed
  - factor-lab → 18 passed
  - ai-engine → 32 passed
  - data-client → 11 passed
  - obsidian-sync → 7 passed
  - bash scripts/smoke-test.sh → 7/7 ALL PASSED
- 修改了哪些文件：
  - scripts/smoke-test.sh（扩展为 6 个端点 + 响应体内容检查）
  - scripts/ralph/prd.json（story-7 → passes: true）
  - scripts/ralph/progress.txt
- **后续迭代注意事项：**
  - smoke-test.sh 用 grep -qF 做文字匹配避免正则转义问题
  - EADDRINUSE 是 server 已运行时的正常行为，不影响测试执行
  - 测试新端点时注意区分 `curl -s`（静默/不报错）和 `curl -sf`（HTTP 错误时退出码非零）
---

## [2026-06-28 12:27:44] - Iteration 5

- Exit code: 0
- Stories remaining: 6
---

## [2026-06-28 20:31] - story-8
- 实现了前端类型同步 + API 客户端层
- **修改 `apps/web/src/data/types.ts`**：
  - 新增 StrategyCategory / StrategySubcategory 类型别名（与 Python 枚举值对齐）
  - 新增 UIConstraint 接口（kind/target_field/target_value/action_value）
  - 扩展 StrategyParam：新增 chartRelevant / uiConstraints 可选字段
  - 扩展 StrategyRow：新增 category / subcategory / workflowReady / summary 可选字段
  - 新增 PreviewResponse / BarData / ChartOverlay / PreviewSignal / Pagination / DiagnosticResult 预览和诊断类型
- **修改 `apps/web/src/api/strategies.ts`**：
  - ApiStrategy 新增 category / subcategory / workflowReady / summary
  - ApiStrategyParam 新增 chart_relevant / ui_constraints
- **新建 `apps/web/src/api/strategies-config.ts`** — fetchStrategyConfig + saveStrategyConfig
- **新建 `apps/web/src/api/preview.ts`** — fetchPreview
- **新建 `apps/web/src/api/diagnostics.ts`** — fetchDiagnostic + fetchDiagnosticsByStrategy
- **修改 `apps/web/src/hooks/useStrategies.ts`** — 移除 hardcoded mode='traditional'，改用 API category；type 改用 API subcategory；添加 category/subcategory/workflowReady/summary 字段映射
- **修改 `apps/web/tests/setup.ts`** — mock 覆盖 GET config(→null)、PUT config(→{saved:true})、POST preview(→preview空响应)、GET diagnostics(→[])
- **修改 `apps/web/src/appData.ts`** — 新增 StrategyCategory / StrategySubcategory / UIConstraint / BarData / ChartOverlay / PreviewSignal / Pagination / PreviewResponse / DiagnosticResult 类型导出
- 验证：
  - `npx tsc --noEmit` → 编译通过
  - `pnpm lint` → 全绿（0 errors，仅已存在 warning）
  - `pnpm test` (web) → 96 passed
  - `pnpm build` → 5/5 successful
- **后续迭代注意事项：**
  - setup.ts 的 mock fetch 新增了 /config / /preview / /diagnostics 路由匹配，注意顺序：先检查 POST/PUT 再检查 GET
  - useStrategies 的 mapStrategy 中 mode 字段现在使用 api.category 并 cast 为 ResearchModeId。story-13 将清理 ResearchModeId 类型
  - ui_constraints 通过 `as StrategyParam['uiConstraints']` 直接传给前端，类型对齐在 API 层
---

## [2026-06-28 20:45] - story-9
- 实现了 StrategyPage 容器 + StrategyGridNew 策略总览
- **修改了哪些文件：**
  - 新增 `apps/web/src/components/strategy-page.tsx` — 双态容器（grid/config），grid 态展示 StrategyGridNew，config 态显示左右两栏（占位ConfigPanel + KlineChart），顶部「← 策略总览」按钮
  - 新增 `apps/web/src/components/strategy-grid-new.tsx` — 按 category 三区分组（FACTOR_BASED/NON_FACTOR/TRANSITIONAL），每区按 subcategory 折叠分组，卡片含 name/description/subcategory tag/进入工作区按钮（workflowReady 时可用）
  - 新增 `apps/web/src/styles/strategy-page.module.css` — 布局样式（分类大区/子分类折叠/卡片网格/config 视图左右两栏）
  - `apps/web/src/data/types.ts` — PageId 追加 'strategy'，UiCopy 追加 backToOverview/strategyCategoryLabels/strategySubcategoryLabels/configPanelPlaceholder/klineChartPlaceholder
  - `apps/web/src/data/en.ts` — 添加 strategy nav item + page content + UiCopy + 所有 mock 策略添加 category/subcategory/workflowReady 字段
  - `apps/web/src/data/zh.ts` — 同 en.ts 中文版
  - `apps/web/src/App.tsx` — 导入 StrategyPage，state.activePage==='strategy' 时渲染 StrategyPage
  - `apps/web/tests/app-data.test.ts` — 更新 nav items 预期列表包含 'strategy'
- 验证：
  - `npx tsc --noEmit` → 通过
  - `pnpm lint` → 全绿（0 errors，仅已存在 warning）
  - `pnpm test` (web) → 96 passed
  - `pnpm build` → 5/5 successful
- **后续迭代注意事项：**
  - story-9 的 config 态使用占位组件（显示 "将在下一轮迭代中实现" 文案），story-10 (ConfigPanel) 和 story-11 (KlineChart) 将替换这些占位
  - StrategyPage 管理内部 view state（grid/config），选中策略时内部切换不触发全局导航
  - 进入工作区按钮触发 onEnterWorkspace → 调用 handleSelectStrategy → 导航到 workspace 页
  - mock 策略数据中的 category/subcategory/workflowReady 字段在 en.ts/zh.ts 中定义，与 Python 策略分类一致
  - 新 nav item 'strategy' 放在 'strategies' 和 'factor-lab' 之间
---


---

## [2026-06-28 20:52] - story-10
- 实现了 ConfigPanel — 左侧动态配置表单
- **新建文件：**
  - `apps/web/src/components/config-panel.tsx` — 完整配置面板组件。一级/二级 Tab 切换 category/subcategory；动态渲染 strategy.params（number→滑块+数字框、select→下拉、boolean→复选框、string→文本框）；因子型专属区块（因子池多选芯片 + 预处理流水线 3步2组1单选）；非因子型专属区块（时序窗口参数 + 指标工具箱 MACD/RSI/布林带 + 按子类型动态参数区：高频→盘口深度、E2E→特征序列长度、事件→T-N区间）；过渡形态专属区块（数据源绑定 + 衰减半衰期 + 映射目标因子）；ui_constraints.disable_when 灰显；300ms debounce 调 fetchPreview → onPreviewUpdate callback；底部保存/预览/提交按钮
  - `apps/web/src/styles/config-panel.module.css` — 配置面板完整样式
- **修改文件：**
  - `apps/web/src/data/types.ts` — UiCopy 追加 24 个 ConfigPanel 相关字段
  - `apps/web/src/data/en.ts` — 添加英文 ConfigPanel 标签
  - `apps/web/src/data/zh.ts` — 添加中文 ConfigPanel 标签
  - `apps/web/src/components/strategy-page.tsx` — 用 ConfigPanel 替换占位文案，集成 preview data state
- **验证：**
  - `npx tsc --noEmit` → 编译通过
  - `npx eslint ...` → 全绿（0 errors）
  - `npx vitest run` → 96 passed
  - `npx vite build` → 3.79s 成功
- **后续迭代注意事项：**
  - story-11 (KlineChart) 将通过 `onPreviewUpdate` 接收 preview 数据渲染 K 线图
  - 因子池当前使用 MOCK_FACTOR_POOL 静态因子列表，story-12（Workspace 诊断）接入真实数据后可替换
  - ui_constraints.require_when 当前仅在基础参数区显示红色星标，触发后强制勾选的联动逻辑需在 story-12 细化
  - ConfigPanel 的 strategy-reset useEffect 必须在所有 useState 声明之后（React hooks 规则）
  - 保存配置时同时保存 category 专属字段（factorPool/preprocessing/indicators 等）作为 config payload 的一部分
---

## [2026-06-28 12:51:49] - Iteration 8

- Exit code: 0
- Stories remaining: 3
- **Detected issues:**
```
lint_error
```
---

## [2026-06-28 20:58] - story-11
- 实现了 KlineChart — 右侧 Canvas K 线图 + Preview 引擎集成
- **新建文件：**
  - `apps/web/src/components/kline-chart.tsx` — Canvas 渲染完整 K 线图组件
  - `apps/web/src/styles/kline-chart.module.css` — K 线面板完整样式
- **修改文件：**
  - `apps/web/src/data/types.ts` — UiCopy 接口新增 13 个 KlineChart 字段
  - `apps/web/src/data/en.ts` — 添加英文 KlineChart 标签
  - `apps/web/src/data/zh.ts` — 添加中文 KlineChart 标签
  - `apps/web/src/components/strategy-page.tsx` — 用 <KlineChart> 替换占位
  - `eslint.config.mjs` — 新增 Canvas API 浏览器全局变量
- **Canvas 图层：** K 线 + MA 均线 + 成交量 + 策略副图(subcategory 动态切换) + 信号箭头 + tooltip
- **验证：** tsc 通过 | ESLint 0 errors | 96 tests passed | build 3.63s

## [2026-06-28 13:00:05] - Iteration 9

- Exit code: 0
- Stories remaining: 2
- **Detected issues:**
```
lint_error
```
---

## [2026-06-28 21:14] - story-12
- 实现了 WorkspacePage — 水平两步步骤条 + 动态诊断
- **新建文件：**
  - `apps/web/src/components/workspace-page.tsx` — 完整 Workspace 页面组件
  - `apps/web/src/styles/workspace-page.module.css` — Workspace 页面样式
- **修改文件：**
  - `apps/web/src/data/types.ts` — UiCopy 接口新增 30 个 WorkspacePage 相关字段
  - `apps/web/src/data/en.ts` — 新增英文 WorkspacePage 标签
  - `apps/web/src/data/zh.ts` — 新增中文 WorkspacePage 标签
  - `apps/web/src/App.tsx` — 导入 WorkspacePage，添加 workspaceEntryStrategy 状态，条件渲染新 workspace
  - `apps/web/src/components/kline-chart.tsx` — 修复 event handler 类型（React SyntheticEvent）
  - `scripts/ralph/prd.json` — story-12 标记为完成
  - `scripts/ralph/progress.txt`
- **功能实现：**
  - 水平两步步骤条（步骤1: 诊断与研究 / 步骤2: 回测与绩效），数字圆圈+连接线
  - 步骤1 按 subcategory 动态渲染诊断内容（因子型→IC/分层收益/热力图；非因子型→参数敏感性/信号分布/滑点压力；宏观→互相关/经济周期；事件→CAR/样本统计；E2E AI→SHAP/Loss曲线）
  - 诊断数据通过 SSE 实时推送进度（POST /api/tasks diagnostics → streamTask）
  - 步骤1→步骤2 确认按钮（诊断完成后可用）
  - 步骤2 回测配置摘要（只读展示）+ 提交回测 → SSE 进度 + 绩效指标卡 + 权益曲线 + 交易明细表格
  - F5 刷新恢复（URL ?diagnosticId=xxx → fetchDiagnostic → 恢复步骤1）
  - 确定性的 mock 数据生成器（使用 det() 函数，无 Math.random）
- **验证：**
  - `pnpm lint` → 0 errors（仅 11 个已存在 warning）
  - `pnpm test` → 全部 10 包测试通过
  - `pnpm build` → 5/5 successful
- **后续迭代注意事项：**
  - WorkspacePage 在 App.tsx 中通过 workspaceEntryStrategy 状态条件渲染，仅从 StrategyPage 进入时使用新 workspace
  - SSE progress 类型在 handleRunDiagnostics 中正确解析 result.resultId + result.data
  - mock 数据使用 det() 确定性伪随机函数，避免 React compiler 对 Math.random 的 warning
  - 确定性的 mock 数据生成器函数（genMockBarData/genMockHeatmap/genMockLinePoints/genMockTradeRows）在 workspace-page.tsx 文件顶部定义
  - story-13 将清理 useResearchWorkflow 中的 hardcoded mode 引用，并移除旧 workspace 组件
---

## [2026-06-28 13:15:14] - Iteration 10

- Exit code: 0
- Stories remaining: 1
- **Detected issues:**
```
vitest_fail
lint_error
build_fail
```
---

## [2026-06-28 21:26] - story-13
- 实现了端到端接线 + 清理 + 国际化
- **清理 useResearchWorkflow.ts：**
  - 移除了文件中所有硬编码的 `'traditional'`、`'hft'`、`'ai'` 字符串
  - 默认值改为从 `getResearchModes(DEFAULT_LANGUAGE)[0].id` 推导（运行时仍为 'traditional'，保障向后兼容）
  - `handleSelectStrategy` 改为使用 `strategy.mode` 属性（非硬编码值）
  - 移除了已废弃的 `getResearchMode` 调用（用 `researchModes[0]` fallback）
- **标记旧 strategy-grid.tsx 为 deprecated：**
  - 添加 `@deprecated` JSDoc 注释
  - 从 App.tsx 移除 `StrategyGrid` 导入和渲染
  - 确认无其他组件再导入 `strategy-grid.tsx`
- **App.tsx 清理：**
  - 移除了 `ResearchModeId` 导入（不需要直接引用该类型）
  - 移除了不再使用的 `handleSelectStrategy` 解构
  - 移除了旧的 'strategies' 页面 StrategyGrid 渲染
- **国际化检查：**
  - en.ts/zh.ts 中的 category/subcategory 相关文案已在 story-9/10/11/12 添加
  - 无需额外新增
- **修改了哪些文件：**
  - `apps/web/src/hooks/useResearchWorkflow.ts`
  - `apps/web/src/components/strategy-grid.tsx`
  - `apps/web/src/App.tsx`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- **验证结果：**
  - `npx tsc --noEmit` → 编译通过
  - `pnpm lint` → 0 errors（仅 11 个已存在 warning）
  - `pnpm test` → 96/96 passed（全部 13 个测试文件）
  - `pnpm build` → 5/5 successful
  - `bash scripts/smoke-test.sh` → 7/7 ALL PASSED
  - 7 个 Python 包 pytest 全部通过
  - 无新 npm 依赖引入
- **后续迭代注意事项：**
  - `ResearchModeId` 类型本身仍保留在 types.ts/appData.ts 中，供旧的 `ResearchMode`/`ResearchJob` 接口使用
  - legacy `WorkspaceContent`/`WorkspaceModeTabs` 组件仍引用 `ResearchModeId`，不影响功能
  - 旧的 `strategy-grid.tsx` 标记为 deprecated 但保留在代码库中以维护 git blame
---

## ✅ ALL STORIES COMPLETE

## [2026-06-30 00:25] - story-14
- 实现了 ConfigSnapshot 类型导出 + WorkspacePage 配置读取挂载
- **修改的文件：**
  - `apps/web/src/data/types.ts` — 新增 ConfigSnapshot 接口 `{ strategy: string; params: Record<string, unknown> }`；更新 DiagnosticResult.configSnapshot 使用该接口
  - `apps/web/src/appData.ts` — 重新导出 ConfigSnapshot 类型
  - `apps/web/src/components/workspace-page.tsx` — 新增 configSnapshot state（初始 null）；新增 useEffect(deps: [strategy.name]) 调用 fetchStrategyConfig(strategy.name)；GET config null/失败时 console.warn 不阻塞页面
- **Doc Sync**: no docs update needed because this story only adds internal type + data fetching hookup, no API schema, architectural, or behavioral changes
- **验证：**
  - `npx tsc --noEmit` → 编译通过
  - `npx eslint` → 0 errors（仅已存在 11 个 warning）
  - `npx vitest run` → 96/96 passed
  - `pnpm build` → 5/5 successful
  - `bash scripts/smoke-test.sh` → 7/7 ALL PASSED
- **后续迭代注意事项：**
  - configSnapshot state 当前 eslint-disable unused-vars（story-15 将把它接入 payload 消除此 warning）
  - setup.ts mock fetch 中 GET /config → null 已覆盖「无配置」场景，测试不会崩溃
---

## [2026-06-29 16:18:27] - Iteration 1

- Exit code: 0
- Stories remaining: 9
- **Detected issues:**
```
lint_error
build_fail
```
---

## [2026-06-30 00:20] - story-15
- **实现了 diagnostics/backtest payload 带真实 configSnapshot + API category 字段**
- **修改的文件：**
  - `apps/web/src/api/tasks.ts` — submitBacktest payload 新增 `configSnapshot?: ConfigSnapshot` 字段
  - `apps/web/src/components/workspace-page.tsx`：
    - `handleRunDiagnostics` POST body 使用 `configSnapshot` state 替代硬编码空对象 + 新增 `category` 字段
    - `handleRunBacktest` POST body 使用 `strategy.name`（替代 `strategy.id`）+ 新增 `configSnapshot` 字段
    - 移除 `configSnapshot` 的 eslint-disable 注释（现在已被使用）
    - 更新对应 useCallback 的 dependency arrays
  - `apps/worker/src/handlers/diagnostics-handler.ts` — DiagnosticsPayload 新增 `category?: string` 字段，PythonBridge 请求中传入 `category`
- **Doc Sync**: no docs update needed because this is internal payload wiring between existing components (frontend → API → Worker) — no API schema, DB schema, or architectural boundary changes
- **验证：**
  - `npx tsc --noEmit` (web + api + worker) → 编译通过
  - `pnpm lint` → 0 errors（仅 11 个已存在 warning）
  - `npx vitest run` → 96/96 passed
  - `pnpm build` → 5/5 successful
- **后续迭代注意事项：**
  - story-21 (Phase 3d) 将接管 DiagnosticsHandler 进行更深入的改造（streamCall 含完整参数、前端替换 mock），当前 story-15 仅添加了 category 透传
  - `configSnapshot` 在 `handleRunDiagnostics` 和 `handleRunBacktest` 中均以 `configSnapshot ?? { strategy: strategy.name, params: {} }` 形式 fallback，避免 null 时 payload 缺失
  - API `task.ts` 的 POST 路由当前不使用 Zod 验证，payload 以 `Record<string, unknown>` 形式存储，`category` 字段天然透传给 Worker
---

## [2026-06-29 16:22:06] - Iteration 2

- Exit code: 0
- Stories remaining: 8
- **Detected issues:**
```
lint_error
```
---

## [2026-06-30 00:24] - story-16
- 删除了已弃用的 strategy-grid.tsx（被 strategy-grid-new.tsx 取代，story-13 已移除所有 import）
- 修改了 App.tsx：
  - 移除了 WorkspaceContent 的 import（保留 WorkspaceModeTabs）
  - 移除了旧的 WorkspaceContent 条件渲染分支（旧单页面 fallback）
  - 移除了因旧分支删除而不再使用的变量解构（selectedStrategy, activeConfigSummary, backtestConfig, setBacktestConfig）
- 修改了哪些文件：
  - `apps/web/src/components/strategy-grid.tsx`（删除）
  - `apps/web/src/App.tsx`
  - `scripts/ralph/prd.json`
  - `scripts/ralph/progress.txt`
- Doc Sync: no docs update needed because this is internal dead code cleanup — no API schema, DB schema, architectural boundary, or behavioral changes
- 验证：
  - `npx tsc --noEmit` → 编译通过
  - `pnpm lint` → 0 errors（仅 11 个已存在 warning）
  - `npx vitest run` → 96/96 passed
  - `pnpm build` → 5/5 successful
- **后续迭代注意事项：**
  - WorkspaceModeTabs 仍然在 workspace 页面渲染（state.activePage === 'workspace'），但它现在是半死代码 — 渲染在新 WorkspacePage 之上且不再被 WorkspaceContent 使用。story-17 清理 ResearchMode 时可以一并处理
  - strategyGridModeLabels 国际化 key 仍被 zh.ts/en.ts/types.ts 保留（strategy-grid-new.tsx 仍在引用），不要误删
---
