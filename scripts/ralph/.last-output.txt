让我先确认分支，并调研需要修改的文件。Let me research the dependencies to understand the full scope of the cleanup.现在我全面了解了需要修改的范围。开始实施 story-13。

首先，让我删除 `strategy-grid.tsx` 的导入和渲染（已被 `StrategyGridNew` 取代），并标记为 deprecated。Now let me update `useResearchWorkflow.ts` — remove `ResearchModeId`, rename `activeMode` → `activeCategory`, and remove hardcoded old mode values.Now let me update `App.tsx` — remove `ResearchModeId` and `StrategyGrid` imports, update destructuring, and remove `StrategyGrid` rendering.Now update the `WorkspaceModeTabs` props in App.tsx to use the new names:Now let me verify the consistency of all changes by running TypeScript compilation:I need to fix the TypeScript issues. The tests reference `activeMode`/`setActiveMode` from the hook return, so I need backward compatibility. Let me read the original state and take a cleaner approach.Let me take a cleaner approach — keep backward-compatible return names (`activeMode`/`setActiveMode`) but change the values internally to avoid hardcoded old modes.Now fix the `researchMode` stub to have proper type compatibility, and fix the return values:TypeScript 编译通过。现在运行全量质量检查：I see — I need to keep the `getResearchMode` call to make `researchMode` reactive. Let me fix this:The lint error is that `handleSelectStrategy` is now unused after removing `StrategyGrid`. Let me fix this:I'll derive the default from `getResearchModes` to avoid hardcoding 'traditional'/'hft'/'ai' as strings, while maintaining runtime backward compatibility for tests.全部 96 个测试通过！运行完整的 lint 和 build：Lint: 0 errors, build: 5/5 成功。验证 `strategy-grid.tsx` 已无导入：Now let me check the smoke test and Python packages:所有检查已通过！运行剩余的 Python 包测试：所有 Python 包测试通过。验证无新依赖引入：全部验证通过！现在更新 prd.json 和 progress.txt，提交代码。全部验证通过！更新 prd.json 并提交代码：推送到远程仓库：网络不可用，推送稍后重试。所有 13 个故事全部完成。

<promise>COMPLETE</promise>
网络不可用，推送稍后重试。所有 13 个故事全部完成。

<promise>COMPLETE</promise>