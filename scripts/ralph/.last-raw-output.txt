Let me start by reading the error file and progress file to understand the current state, then find the next story to implement.Let me read the rest of the progress file to get the full picture.Now I have a clear picture. story-12 is complete and the next story is **story-13 (Phase 4a 鈥?Preview chart_relevant 鍙傛暟鏍￠獙鈫?22)**. Let me read the relevant source files.Let me read the app structure and existing preview tests to understand how services are injected.Let me check the strategy route and how it uses the strategy sync service, plus the config route pattern.Let me check the config route test file for testing patterns.Let me also check the PreviewService to understand how it handles empty bars.Good 鈥?`servePreview` returns empty overlays for empty bars (no crash). Now I have everything I need. Let me implement story-13.

**Plan:**
1. Modify `apps/api/src/routes/preview.ts` 鈥?add chart_relevant validation
2. Create `apps/api/tests/routes/preview.test.ts` 鈥?test the validationNow let me create the test file.Now let me run the quality checks.TypeScript compiles clean. Now run API tests.All 92 tests passed (14 files), including 6 new preview route tests. Now let me run lint and build.All checks pass:
- **Lint**: 0 errors (only pre-existing warnings)
- **Test**: 92/92 passed (14 files, including 6 new preview route tests)
- **Build**: 5/5 successful

Now let me do the Doc Sync Gate check and then commit.Let me check the integrated plan to see if it already covers this phase.No AGENTS.md update needed 鈥?the change is a simple validation addition, not a new pattern. Let me commit.Good 鈥?only the 2 expected files. Now let me update prd.json and commit.Now let me append progress.txt and commit.Now let me commit.Push failed due to network (connection reset), but the commit is saved locally. Since there are still stories with `passes: false` (stories 14-18), I'll end normally.

Story-13 is complete. The next `passes: false` story at the highest priority is **story-14** (Phase 4b 鈥?Preview config 鍚堝苟).
Push failed due to network (connection reset), but the commit is saved locally. Since there are still stories with `passes: false` (stories 14-18), I'll end normally.

Story-13 is complete. The next `passes: false` story at the highest priority is **story-14** (Phase 4b 鈥?Preview config 鍚堝苟).
