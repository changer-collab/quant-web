Now let me assess the current state. I need to check:
1. What branch we're on
2. What happened in the previous attempt on story-6c
3. Current state of the target filesLet me read the current state of the three files and check what tsc errors exist:Let me check the current BarData type and how the API returns bar data:Now I have a clear picture. Let me implement **story-6c** 鈥?the highest priority non-passing story. The previous iteration had pre-existing tsc errors from `strategy-grid.tsx` (deprecated file with deleted `ResearchModeId` type). I need to:

1. `strategy-table.tsx`: `p.key` 鈫?`p.name`
2. `strategy-grid.tsx`: `p.key` 鈫?`p.name` + fix `ResearchModeId` (pre-existing tsc blocker)
3. `kline-chart.tsx`: bar shape adapter (old `{open,close,high,low,volume}` 鈫?new `{o,c,h,l,v}`)

Let me start:Now let me implement the kline-chart.tsx adapter. I'll add the normalization function and update all drawing functions to use `{ts, o, h, l, c, v}` format:Now update `drawCandlesticks` to use `NormalizedBar`:Now update `drawSubChart` 鈥?signature and all field accesses:Now update the remaining `b.volume` 鈫?`b.v` references in drawSubChart:Now update `drawSignals`:Now update the draw callback's price range computation:Now update the tooltip state type:Now update `handleMouseMove` to normalize the bar data:Now update the tooltip rendering field accesses:Let me verify no remaining old field references in the modified files:Good 鈥?no `p.key` remaining, and old bar fields (`open`/`close`/etc.) only appear in the normalization function. Let me run quality checks:Actually, a cleaner approach with `as const`:TypeScript compiles cleanly! Now let me run tests:All 93 tests pass. Now let me run the build:Build passes. Let me run lint and verify acceptance criteria:All quality checks pass. Let me now do Doc Sync Gate evaluation and complete the story.All acceptance criteria verified. Doc Sync evaluation: this story is mechanical frontend migration (param key 鈫?name, bar adapter) in 3 component files 鈥?no API schema, DB schema, architectural boundary, or behavioral contract changes. No docs update needed.Now update prd.json and progress.txt, then commit:Now commit:Commit `0b38e30` successful. Story-6c complete.

**Summary:**
- **strategy-table.tsx**: `p.key` 鈫?`p.name` (React key)
- **strategy-grid.tsx**: `p.key` 鈫?`p.name` + fix `ResearchModeId` type (deprecated, converted to `as const`)
- **kline-chart.tsx**: Added `NormalizedBar` interface + `normalizeBars()` function 鈥?old bars (`{timestamp,open,...}`) normalize to `{ts,o,h,l,c,v}`, new bars passthrough. All 4 drawing functions updated to use normalized format.
- Quality: tsc OK, 93/93 tests, build 3.21s, lint 0 errors
- Doc Sync: no docs update needed (mechanical component migration only)

**Remaining stories** (still `passes: false`): story-7a (CLI listStrategies), story-7b (API strategy-sync PythonBridge), story-7c (Worker queue.ts deletion), story-8 (E2E acceptance).
Commit `0b38e30` successful. Story-6c complete.

**Summary:**
- **strategy-table.tsx**: `p.key` 鈫?`p.name` (React key)
- **strategy-grid.tsx**: `p.key` 鈫?`p.name` + fix `ResearchModeId` type (deprecated, converted to `as const`)
- **kline-chart.tsx**: Added `NormalizedBar` interface + `normalizeBars()` function 鈥?old bars (`{timestamp,open,...}`) normalize to `{ts,o,h,l,c,v}`, new bars passthrough. All 4 drawing functions updated to use normalized format.
- Quality: tsc OK, 93/93 tests, build 3.21s, lint 0 errors
- Doc Sync: no docs update needed (mechanical component migration only)

**Remaining stories** (still `passes: false`): story-7a (CLI listStrategies), story-7b (API strategy-sync PythonBridge), story-7c (Worker queue.ts deletion), story-8 (E2E acceptance).
